# Feature Matrix

Last updated: August 13, 2026

This matrix maps the master feature checklist to current implementation and source locations.
It is an inventory and chronology index, not the canonical detailed history or
current operational status. Use [`CHANGELOG.md`](../CHANGELOG.md) for the full
change record and [`PROJECT_STATUS.md`](../PROJECT_STATUS.md) for current
release, deployment, provider, risk, and acceptance state.

## Scope and Evidence (August 13, 2026)

- **First source:** the earliest identified Git commit containing the capability
  foundation. It establishes source lineage, not completeness.
- **Release containment:** an exact commit or tag contains the capability. It
  does not establish that any environment runs that revision.
- **Deployment:** a governed deployment receipt binds an exact revision to a
  target. It does not by itself prove authenticated behavior or correct
  production-data results.
- **Hosted/provider evidence:** the exact hosted role/data path or external
  provider outcome was observed. Public route reachability, configuration, and
  request acceptance remain narrower facts than end-to-end delivery or payment.
- **Human acceptance:** a named person completed and accepted the intended
  workflow. This is never inferred from source, tests, CI, deployment, or
  provider receipts.

The chronology below records first-source and release-containment evidence.
As of this snapshot, recorded production is `v0.7.0` (`fb0aacc`) and the
Ambient/staffing work continues in source after tagged `v0.8.1`. Recheck
`PROJECT_STATUS.md` before using either fact operationally.

## Status Legend
- `Implemented`: shipped and wired in the app.
- `Implemented (source)`: present in the current source with local
  implementation evidence, but not yet deployed or production-accepted;
  release-gate status remains in `PROJECT_STATUS.md`.
- `Implemented (deployed)`: present in the verified production revision; any
  remaining hosted or human-acceptance boundary is stated in the row and in
  `PROJECT_STATUS.md`.
- `Partial`: present but not complete against all desired behaviors.
- `Optional`: optional capability tracked but not required for core flow.

## Master Checklist Mapping

| # | Feature Area | Status | Primary Evidence |
|---|---|---|---|
| 1 | Data + architecture collections/model | Implemented | `src/lib/organizationService.js`, `src/context/OrganizationContext.jsx`, `firestore.rules`, `functions/index.js`, `scripts/seed-firestore-menu.mjs`, `scripts/migrate-to-multi-tenant.mjs` |
| 2 | Dynamic menu system (Firestore + event filtering + grouping + active items) | Implemented | `src/hooks/useCatalogData.js` (changed-record transactions + conflict detection), `src/lib/menuService.js`, `src/components/WizardSteps.jsx` |
| 3 | Pricing engine (`per_item`, `per_person`, `per_event`, selected package inclusions without double charge, server-owned actor/time, and confirmed-catalog authority fencing) | Implemented (deployed; catalog changes abort with recalculate/retry rather than committing stale authority) | `src/lib/quoteCalculator.js`, `functions/pricingEngine.js` (confirmed-setup gate, authoritative inclusion refs, settings fingerprint, and pre/post-read fence), `functions/index.js` (trusted boundary rechecks), `src/lib/pricingContracts.js`, `src/lib/commerceOps.js`, `src/components/LiveBreakdown.jsx`, `src/lib/__tests__/pricingEngine.authoritative.test.js`, `e2e/firebase-authoritative-pricing.smoke.spec.js` |
| 4 | 5-step quote builder wizard | Implemented | `src/lib/wizardUi.js` (`WIZARD_STEP_DEFINITIONS`), `src/components/WizardSteps.jsx`, `src/App.jsx`, `src/components/__tests__/wizardVisualSnapshots.test.jsx`, `e2e/quote-wizard.smoke.spec.js` |
| 5 | Live sticky summary panel with real-time totals | Implemented (deployed; authenticated mobile human acceptance pending) | `src/components/LiveBreakdown.jsx`, `src/App.jsx` (`MobilePricingSummary`), `src/styles.css` (`.breakdown-panel`, `.mobile-pricing-summary`), `e2e/quote-wizard.smoke.spec.js` |
| 6 | Quote system (trusted create/duplicate/edit, snapshots, statuses) | Implemented (deployed; authenticated production-data acceptance pending) | `functions/index.js` (`createQuoteDraft`, `duplicateQuoteDraft`, `updateQuoteDraft`), `functions/quoteCreation.js`, `src/lib/quoteStore.js` (`submitQuote`, `duplicateQuote`, `updateQuote`), `src/components/QuoteHistoryModal.jsx` |
| 7 | Quote history + versioning | Implemented | `src/lib/quoteStore.js` (`saveQuoteVersion`, `getQuoteHistory`), `firestore.rules` (`organizations/{orgId}/quotes/{quoteId}/versions/{versionId}`) |
| 8 | Quote management UI (list, sort, filters, exact draft-save handoff, role-safe actions, revision-bound email delivery, evidence-gated portal sharing and conversation with bounded same-session unresolved-request restoration, isolated Stripe deposit and final-balance request/reconciliation rails, provider-owned payment status) | Implemented (deployed; authenticated, production-data, and provider acceptance remain separate) | `src/components/QuoteHistoryModal.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/lib/quoteStore.js` (`duplicateQuote`, `reopenQuote`, `hardDeleteQuote`), `src/lib/portalConversationClient.js`, `src/lib/commerceOps.js`, `src/lib/proposalExport.js`, `functions/quoteDelivery.js`, `functions/portalConversation.js`, `functions/paymentApprovalScope.js`, `functions/paymentDispatchState.js`, `functions/paymentSafety.js`, `functions/paymentLedger.js`, `functions/finalBalancePayment.js`, `functions/stripeProviderState.js`, `functions/index.js` (`getQuotePortalConversation`, `sendQuotePortalConversationMessage`, `sendQuoteToCustomer`, `resolveQuoteDeliveryOutcome`, `sendPaymentRequestEmail`, `sendFinalBalanceRequestEmail`, `reconcileDepositCheckout`, `reconcileFinalBalanceCheckout`, `stripeWebhook`, disabled direct `createDepositCheckout`, `rotateQuotePortalKey`, `reopenQuote`, `hardDeleteQuote`; the legacy bulk purge callable fails closed), `firestore.rules` (`privatePaymentDispatches` and quote conversation records are server-only) |
| 9 | Admin panel tabbed UX + hierarchical menu management | Implemented | `src/components/AdminCatalogModal.jsx`, `src/styles.css` (`.admin-tabs`) |
| 10 | Inline editing with blur/enter persistence | Implemented | `src/components/AdminCatalogModal.jsx` (`handleManagedMenuItemBlur`, `handleManagedMenuItemKeyDown`) |
| 11 | Booking lifecycle (availability checks, approval-bound contract conversion with receipt-safe uncertainty/reconciliation/recovery states, customer-linked immutable source version, confirmations, staff assignments) | Implemented (deployed, including mutation-state UI and customer-linked immutable-version projection; hosted acceptance pending) | `functions/contractWorkflow.js`, `functions/quoteCreation.js` (`projectCustomerIdentityToImmutableVersion`), `functions/index.js` (`convertQuoteToContract`), `src/lib/quoteStore.js` (`checkEventAvailability`, `convertQuoteToContract`, `updateQuoteBookingConfirmation`, `updateQuoteBookingAssignment`), `src/components/EventScheduleModal.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/__tests__/quoteHistoryContractConversion.test.jsx`, `scripts/provisioning-emulator-acceptance.mjs` |
| 12 | Customer decision center (tenant theme presets + snapshotted portal branding + scope/pricing + evidence-bound quote conversation + callable-owned electronic acceptance receipt + change request/decline + evidence-bound token lifecycle + CWF-10 exact-attempt submitting/uncertain/reconciliation/receipt/stale/error/recovery presentation, focus, and reduced-motion behavior) | Implemented (deployed, including CWF-10 interaction states and the conversation/acceptance authority; hosted customer acceptance pending) | `src/data/portalThemePresets.js`, `src/components/AdminCatalogModal.jsx`, `src/components/CustomerPortalView.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/lib/quoteStore.js` (`getPortalQuote`, `updatePortalDecision`, `rotateQuotePortalKey`), `src/lib/portalConversationClient.js`, `functions/quoteCreation.js`, `functions/proposalAcceptance.js`, `functions/quoteDelivery.js`, `functions/portalConversation.js`, `functions/index.js` (`getQuotePortalConversation`, `sendQuotePortalConversationMessage`, `acceptQuoteProposal`), `firestore.rules` (`customerPortalQuotes`, `portalConversationMessages`, `proposalAcceptanceReceipts`), `src/components/__tests__/customerPortalDecisionInteraction.test.jsx` |
| 13 | Admin-only provider operations + scoped integration audit logging | Implemented (deployed; individual provider activation and acceptance pending where noted) | `src/components/IntegrationOpsModal.jsx`, `src/lib/quoteStore.js` (`recordQuoteIntegrationSync`; browser CRM sends fail closed), `src/lib/commerceOps.js`, `functions/index.js` (admin-only provider callables) |
| 14 | Reporting dashboard (pipeline, conversion, accepted/booked quote value, and Firebase-source/provider-timestamp-verified paid-deposit totals; CWF-09 adds a 500-record same-tenant cap, source/freshness/truncation, explicit denominators, missing-money exclusion, stale/partial/error/retry, and UTC six-month trends; neither money family is accounting revenue). The current candidate source additionally exposes privacy-bounded client-observed Ambient dead-click, exact server-authoritative Firebase saved priced-draft, and same-session exact-category issue-resolution measures through the existing product-event summary. | Deployed for the bounded commercial evidence rail and payment qualification; hosted acceptance pending. The Ambient metric extension is implemented only in the `v0.8.0` source candidate and is neither deployed nor hosted/human accepted. It excludes quote/customer identifiers and free text, shows no rate or duration from local fallback or zero samples, and is client observation rather than server timing telemetry. | `src/components/ReportingDashboardModal.jsx`, `src/lib/quoteStore.js` (`getQuoteHistory`), `src/lib/productAnalytics.js`, `functions/productAnalytics.js`, `src/components/__tests__/reportingDashboardEvidence.test.jsx`, `src/components/__tests__/operationalWorkspaceViews.test.jsx`, product-analytics client/server tests |
| 15 | Event schedule board (month/week, conflicts, assignments, production checklist, and separately labeled quote/booking-confirmation states) | Implemented (deployed; hosted operator acceptance pending) | `src/components/EventScheduleModal.jsx`, `src/lib/statusSemantics.js`, `src/lib/quoteStore.js` (`getQuoteHistory`, `updateQuoteBookingAssignment`, `updateQuoteProductionChecklist`) |
| 16 | Session diagnostics (runtime capture + staff export/clear tools) | Implemented | `src/lib/sessionDiagnostics.js`, `src/components/DiagnosticsModal.jsx`, `src/main.jsx`, `src/App.jsx` |
| 17 | Global event + organization context across surfaces | Implemented | `src/context/EventTypeContext.jsx`, `src/context/OrganizationContext.jsx`, `src/main.jsx`, `src/App.jsx`, `src/components/AdminCatalogModal.jsx` |
| 18 | Verified-email auth, email/password recovery with same-confirmation UI and QuotePilot return URL, org bootstrap, explicit organization-owner authority, and role-gated controls | Implemented (deployed code/rules for the existing auth surface; the explicit owner-binding and browser role-write denial are source-only, while exact hosted Auth configuration and human acceptance remain pending). Newly provisioned owner invitations carry a dedicated purpose; exact-email verified activation atomically binds the organization owner and writes a browser-private receipt. Generic legacy admin/sales invitations remain role-only, and browsers cannot mint or rewrite role documents. The return policy permits only the canonical production host, the fixed isolated staging return host used for governed candidate acceptance, and loopback development; arbitrary previews and lookalikes fail closed. | `src/lib/authClient.js`, `src/hooks/useAuthSession.js`, `src/components/AuthGate.jsx`, `e2e/firebase-auth-rules.smoke.spec.js`, `functions/index.js` (`ensureOrganizationBootstrap`, `provisionCustomerOrder`), `functions/organizationAuthority.js`, `firestore.rules`, `scripts/provisioning-emulator-acceptance.mjs` |
| 19 | Optional guided selling recommendations + rules | Implemented / Optional | `src/lib/recommendations.js`, `src/components/AdminCatalogModal.jsx`, `src/data/mockCatalog.js` |
| 20 | Good/Better/Best scenario compare workflow | Implemented / Optional | `src/lib/quoteWorkflow.js` (`buildQuoteScenarios`), `src/components/QuoteCompareModal.jsx`, `src/App.jsx` |
| 21 | Optional admin security/audit depth (beyond role gating) | Partial / Optional | Role-gated access and rules are shipped in `src/hooks/useAuthSession.js`, `src/components/AuthGate.jsx`, `firestore.rules`; sync log audit trail exists in `src/lib/quoteStore.js`, but full cross-surface audit pipeline remains limited |
| 22 | QA acceptance tests and checks, including independent lazy-surface recovery, embedded-route semantics, and focus-contained contextual/legacy dialogs | Implemented (core) | Unit tests under `src/lib/__tests__/`, UI snapshots and recovery contracts under `src/components/__tests__/`, Firestore rules tests under `src/rules/__tests__/`, Playwright smoke and real chunk-failure accessibility lanes under `e2e/`, `scripts/provisioning-emulator-acceptance.mjs`, and CI scripts in `package.json` |
| 23 | Sales workflow (attention queue/count, readiness, follow-ups, request-ID-bound current change handling, lifecycle, approval queue and exact action execution) | Implemented (deployed; hosted acceptance pending) | `src/App.jsx`, `src/lib/quoteWorkflow.js`, `src/components/SalesWorkflowModal.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/lib/quoteStore.js`, `functions/approvalWorkflow.js`, `functions/contractWorkflow.js`, `functions/index.js` (approval and governed-action callables), `firestore.rules` |
| 24 | Tenant Import Studio (customer/catalog CSV recognition, validation, receipts, rollback) with stable unresolved batch identity and locked reconciliation controls | Implemented (deployed, including the trusted customer import/rollback callables; hosted smoke pending) | `src/components/ImportStudioModal.jsx`, `src/lib/importStudio.js`, `src/lib/importBatchService.js`, `functions/index.js` (`createCustomerImportBatch`, `rollbackCustomerImportBatch`), `firestore.rules` (`customers`, `importBatches`) |
| 25 | Platform tenant provisioning (verified owner, neutral defaults, atomic create, explicit entitlements, repair, cleanup) | Implemented (deployed; owner acceptance pending) | `functions/index.js` (`preflightCustomerOrder`, `provisionCustomerOrder`, `repairCustomerProvisioningOrder`, cleanup callables), `src/components/IntegrationOpsModal.jsx`, `scripts/provisioning-emulator-acceptance.mjs` |
| 26 | Public $1 invoice-first buyer onboarding on `tonicatering` (Turnstile, pre-identity HMAC-keyed create reservation and status abuse controls, request-scoped exact retry plus signed or audited operator provider-void replacement, Stripe API `2024-06-20` Hosted Invoice Page and signed invoice lifecycle, terminal unpaid Invoice recovery, paid workspace preparation, pending invite, optional provider-accepted activation instructions, exact verified-email user activation, quote-rail isolation) | Implemented (deployed but intentionally dormant with `BUYER_ACCESS_ENABLED=false`; dedicated test-mode Stripe credential, provider/hosted acceptance, and live launch pending) | `src/components/BuyerAccessPage.jsx`, `src/lib/buyerAccess.js`, `functions/buyerAccess.js`, `functions/index.js` (`repairBuyerAccessInvoice`, `createBuyerAccessInvoice`, `getBuyerAccessInvoiceStatus`, `buyerAccessStripeWebhook`), `src/components/IntegrationOpsModal.jsx`, Firebase Secret Manager bindings, `firestore.rules` (`buyerAccessOrders`), `e2e/buyer-access.spec.js`, `scripts/provisioning-emulator-acceptance.mjs` |
| 27 | Staff Kitchen BEO content and legacy download-time provenance (timing, staffing, checkpoints, selections, checklist, source revision, declared-input digest, contacts, allergens, sign-off, notes) | Implemented (deployed foundation; the browser-only fingerprint remains provenance rather than retained freshness, while row 44 owns the newer trusted generation/receipt authority; hosted operator acceptance pending) | `src/components/QuoteHistoryModal.jsx`, `src/lib/beoPayload.js`, `src/lib/beoArtifactFingerprint.js`, `src/lib/beoExport.js`, `src/lib/quoteWorkflow.js`, focused BEO tests |
| 28 | Trusted customer projection from quote create/edit (organization scoped, duplicate-safe, import-preserving, browser-forgery denied) | Implemented (deployed Functions/rules; hosted production-data acceptance pending) | `functions/index.js`, `functions/quoteCreation.js`, `firestore.rules` (`customers`), `src/lib/__tests__/quoteCreation.server.test.js`, `src/rules/__tests__/firestore.rules.test.js` |
| 29 | Temporary-flagged customer-centered staff shell and Commercial Command Center: dependency-free `/app` route parsing/navigation, Home default, sticky in-memory quote builder, direct/deep routes, customer-name-to-360 links, exact quote/payment and Workflow focus, authenticated 404, portal-query precedence, one generation-guarded Home/header snapshot over existing read contracts/data sources, a Home-scoped tenant/read/source/last-complete-read evidence rail with incomplete/stale/truncation truth, human-readable first-release staff formatting, primary-route heading focus, route-versus-modal return language, shared semantic status chips, and staff-scoped neutral chrome | Implemented (deployed with the production workspace flag enabled; exact authenticated acceptance, wider evidence-rail expansion, and flag removal pending) | `src/lib/workspaceRoutes.js`, `src/lib/workspacePresentation.js`, `src/hooks/useBrowserLocation.js`, `src/hooks/useWorkspaceRouteHeadingFocus.js`, `src/context/WorkspaceNavigationContext.jsx`, `src/components/WorkspaceRoute.jsx`, `src/hooks/useCommercialWorkspaceSnapshot.js`, `src/components/CommandCenterHome.jsx`, `src/components/StaffEvidenceRail.jsx`, `src/components/StatusChip.jsx`, `src/components/WorkspaceNotFound.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/lib/statusSemantics.js`, `src/App.jsx`, `src/styles.css`, focused tests under `src/lib/__tests__`, `src/hooks/__tests__`, `src/context/__tests__`, and `src/components/__tests__` |
| 30 | Stable same-tenant customer identity and Internal Customer 360: generated server-owned `customerId` on trusted new quote/version writes, private server-only normalized-email ownership claims, collision-safe edits, normalized customer search, rules-bounded paginated directory, surface-scoped source/freshness/bounds rails, a bounded relationship briefing, recorded commercial timeline, advisory immutable-version comparison, derived Overview/Quotes & Proposals/Events/Money/Conversations, per-quote conversation summaries, portal-safe staff preview, emulator-only legacy binding apply, canonical quote staff-only reads, and retired browser customer-role self-bootstrap | Implemented (deployed; timeline omits provider delivery/bounce receipts not exposed by the current DTO; production normalization/backfill, hosted staff acceptance, and persistent customer accounts excluded) | `functions/quoteCreation.js`, `functions/index.js`, `src/lib/customerWorkspace.js`, `src/lib/customerCommercialTimeline.js`, `src/lib/quoteVersionComparison.js`, `src/components/CustomerDirectoryView.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/CustomerCommercialTimeline.jsx`, `src/components/QuoteVersionComparison.jsx`, `src/components/StaffEvidenceRail.jsx`, `scripts/customer-id-backfill-plan.mjs`, `scripts/backfill-customer-ids.mjs`, `firestore.rules`, focused component/lib tests, and rules/emulator coverage |
| 31 | Routed operational workspaces for Schedule, Reporting, Catalog, Imports, Integrations, and Diagnostics with recoverable lazy loading, sticky route state, embedded-region focus, retained role/feature gates, and compatible contextual/legacy modal wrappers | Implemented (deployed with the production workspace flag enabled; authenticated hosted acceptance pending) | `src/App.jsx`, `src/components/EventScheduleModal.jsx` (`EventScheduleView`), `src/components/ReportingDashboardModal.jsx` (`ReportingDashboardView`), `src/components/AdminCatalogModal.jsx` (`AdminCatalogView`), `src/components/ImportStudioModal.jsx` (`ImportStudioView`), `src/components/IntegrationOpsModal.jsx` (`IntegrationOpsView`), `src/components/DiagnosticsModal.jsx` (`DiagnosticsView`), `src/components/__tests__/operationalWorkspaceViews.test.jsx`, `src/components/__tests__/adminCatalogStarterChoice.test.jsx`, `src/components/__tests__/importStudioPresentation.test.jsx`, `src/components/__tests__/uiRecovery.test.jsx` |
| 32 | Mechanical no-orphan-capability delivery gate: fail-closed branch/PR/push authority-path review including deletions, same-upstream fallback, broad runtime-client authority discovery, chained/modular Firestore writes, exact ownership for directly changed/new/removed Firebase Function exports, shared-helper affected-export review across every classification, real frontend/manual/matrix locators, assertion-bearing tests, canonical per-state read/mutation markers, one narrow stale-read exception, and tested non-callable headless outcomes | Implemented (source; structural traceability only, not semantic/visual/hosted acceptance) | `scripts/check-capability-surfacing.mjs`, `docs/capability-surfacing-contracts.json`, `src/lib/__tests__/capabilitySurfacingGate.test.js`, `scripts/orchestration-lanes.sh` (`lane:core`), `package.json` (`check:capability-surfaces`) |
| 33 | Commercial Dependency Graph v1 pure foundation: frozen validated registry, deterministic downstream traversal, `qp-canonical-json-v1` browser/Node SHA-256 parity, schema-bound declared-input fingerprint, and exact source revision | Implemented (deployed foundation; the graph itself still performs no pricing, persistence, mutation, or freshness decision; rows 42-45 own the newer trusted authorities that consume it; hosted operator acceptance pending) | `src/lib/commercialDependencyGraphCore.cjs`, `functions/commercialDependencyGraphCore.cjs`, `src/lib/commercialDependencyGraph.js`, `src/lib/beoArtifactFingerprint.js`, `docs/COMMERCIAL_DEPENDENCY_GRAPH_ADR.md`, graph/parity/BEO tests |
| 34 | Bounded universal Workspace search and keyboard command palette: recoverably lazy `Ctrl`/`Command`+`K` staff entry, same-tenant customer-prefix and latest-50 quote reads, source/truncation/error/retry states, stale-generation protection, opaque navigation, transient query state, and portal/modal/role/feature guards | Implemented (deployed; broader proposal/event index, hosted keyboard/mobile acceptance, and temporary-flag removal pending) | `src/lib/commercialSearch.js`, `src/lib/commercialSearchShell.js`, `src/components/CommercialSearchPalette.jsx`, `src/App.jsx`, `src/lib/__tests__/commercialSearch.test.js`, `src/components/__tests__/commercialSearchPalette.test.jsx` |
| 35 | CWF-11 anniversary Attention and exact-version rebook draft: tenant-calendar Home/Workflow cues from the bounded latest-200 booked-quote read, explicit source/display incompleteness, one-click stable-customer handoff to Customer 360, booked/accepted immutable-source validation there, stable-customer continuity, deterministic request and draft identity, current-contact overlay, current-catalog authoritative repricing with catalog-authority fencing, uncertainty/reconciliation/recovery, trusted `updateQuoteDraft` staff-review completion evidence, and delivery blocking until a reviewed new event date is saved | Implemented (deployed; the central cue is read-only and does not itself verify acceptance or create a draft; no message, lead, acceptance, booking, payment, or revenue outcome; hosted staff acceptance pending) | `src/lib/anniversaryRebookingAttention.js`, `src/lib/customerRebookingRadar.js`, `src/lib/rebookQuoteClient.js`, `src/hooks/useCommercialWorkspaceSnapshot.js`, `src/components/CommandCenterHome.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/CustomerRevenueOpportunities.jsx`, `src/components/CustomerRebookDraftAction.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/App.jsx`, `functions/rebookQuoteDraft.js`, `functions/quoteCreation.js`, `functions/pricingEngine.js`, `functions/index.js` (`createRebookQuoteDraft`, `updateQuoteDraft`, delivery guard), `scripts/rebook-quote-emulator-acceptance.mjs`, `e2e/customer-centered-workspace.spec.js`, `src/lib/__tests__/anniversaryRebookingAttention.test.js`, `src/lib/__tests__/customerRebookingRadar.test.js`, `src/lib/__tests__/rebookQuoteClient.test.js`, `src/lib/__tests__/rebookQuoteDraft.server.test.js`, `src/lib/__tests__/quoteCreation.server.test.js`, `src/components/__tests__/commandCenterAttentionRouting.test.jsx`, `src/components/__tests__/customerRebookDraftAction.test.jsx` |
| 36 | CWF-13 Customer 360 bounded commercial measures: quoted, exact-state accepted/booked, Firebase-source-gated provider-confirmed deposit/final-balance, and repeat-event evidence with visible denominators, complete-versus-displayed-record scope, bounds, and loading/empty/partial/stale/error/retry presentation | Implemented (deployed; provider payment measures fail closed for browser-local, mixed, or unknown-source reads; derived read-only operational measures, not accounting revenue, cash reconciliation, forecasts, or a persisted rollup; hosted staff acceptance pending) | `src/lib/customerCommercialMeasures.js`, `src/components/CustomerCommercialMeasures.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/lib/__tests__/customerCommercialMeasures.test.js`, `src/components/__tests__/customerCommercialMeasuresPresentation.test.jsx` |
| 37 | CWF-12 Revenue Autopilot eligibility preview: one canonical quote from the bounded Workflow read, organization-configured IANA business time zone, deterministic quote-follow-up/deposit/final-balance/unread-reply evaluations, explicit safety gates and proof boundaries, and loading/empty/partial/stale/error states | Implemented (deployed read-only/non-sending foundation; row 46 owns the newer independently gated scheduler/job/provider/Attention authority; this preview itself makes no provider or recovered-revenue claim) | `src/lib/revenueAutopilotPreview.js`, `src/components/RevenueAutopilotPreviewPanel.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/AdminCatalogModal.jsx`, `src/data/mockCatalog.js`, `src/App.jsx`, `src/lib/__tests__/revenueAutopilotPreview.test.js`, `src/components/__tests__/revenueAutopilotPreviewPanel.test.jsx`, `src/components/__tests__/salesWorkflowRevenueAutopilot.test.jsx`, `src/lib/__tests__/catalogBusinessTimeZone.test.js` |
| 38 | CWF-15B read-only Commercial Change Impact preview: sub-slice 15B-b adds an expected active-version precondition, same-tenant saved canonical revision plus server-authoritatively repriced proposed-form snapshots, catalog-authority fencing, exact fact and total/deposit deltas, deterministic affected dependency nodes classified `REVIEW`/`STALE`, request-generation safety, and complete preview presentation states | Implemented (deployed simulation foundation; preview itself remains read-only, while rows 42-45 own newer default-off apply, generation, reconciliation, and Debt authority) | `functions/commercialChangeImpactPreview.js`, `functions/pricingEngine.js`, `functions/index.js` (`calculateQuotePricing` preview option), `src/lib/commerceOps.js`, `src/lib/commercialChangeImpact.js`, `src/components/CommercialChangeImpactPanel.jsx`, `src/App.jsx`, `e2e/firebase-authoritative-pricing.smoke.spec.js`, `src/lib/__tests__/commercialChangeImpactPreview.server.test.js`, `src/lib/__tests__/commercialChangeImpact.test.js`, `src/components/__tests__/commercialChangeImpactPanel.test.jsx` |
| 39 | CWF-07 Workflow timing cues and internal completion receipts: bounded timestamp-derived due-today, overdue, upcoming, and aging guidance plus exact stored internal completion evidence, with source/bounds and loading/empty/partial/stale/error/retry presentation | Implemented (deployed, read-only derivation; internal receipts are not customer contact, provider delivery, proposal resolution, payment, or booking proof; persisted ownership/SLA/escalation/handoffs and hosted acceptance pending) | `src/lib/workflowTimingCues.js`, `src/components/WorkflowTimingPanel.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/lib/__tests__/workflowTimingCues.test.js`, `src/components/__tests__/workflowTimingPanel.test.jsx` |
| 40 | CWF-08 bounded event run of show: Schedule derives an expandable accepted/booked event sequence from recorded event, staffing, checklist, and BEO inputs, labels timing sources and unknowns, and preserves independent acceptance, booking, and readiness semantics | Implemented (deployed, generated read-only view; no collaborative task, roster, resource, vendor, inventory, attendance, or portal-timing model; hosted operator acceptance pending) | `src/lib/eventRunOfShow.js`, `src/components/EventRunOfShowPanel.jsx`, `src/components/EventScheduleModal.jsx`, `src/lib/__tests__/eventRunOfShow.test.js`, `src/components/__tests__/eventRunOfShowPanel.test.jsx` |
| 41 | CWF-11 authoritative post-event closeout: eligible governed booking verifies the private acceptance snapshot and atomically creates one deterministic record bound to the exact accepted immutable version and stable customer, due seven tenant-calendar days after the event; legacy sources remain booked with a visible review block, and Customer 360 plus Workflow expose bounded scheduled/due/overdue/configuration-blocked/completed state, exact configuration recovery, and four explicit idempotent internal review/reopen receipts | Implemented (deployed; internal review evidence only, not an outbound thank-you/review request, provider delivery/open, customer reply, lead, booking, payment, or revenue outcome; hosted staff acceptance pending) | `functions/postEventCloseout.js`, `functions/index.js` (`convertQuoteToContract`, `recordPostEventCloseoutReview`, `refreshPostEventCloseoutConfiguration`), `firestore.rules`, `src/lib/postEventCloseoutClient.js`, `src/lib/customerRebookingRadar.js`, `src/lib/quoteWorkflow.js`, `src/components/PostEventCloseoutReviewAction.jsx`, `src/components/CustomerRevenueOpportunities.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/CommandCenterHome.jsx`, `scripts/provisioning-emulator-acceptance.mjs`, `src/lib/__tests__/postEventCloseout.server.test.js`, `src/lib/__tests__/postEventCloseoutClient.test.js`, `src/components/__tests__/postEventCloseoutReviewAction.test.jsx`, `src/rules/__tests__/firestore.rules.test.js` |
| 42 | Commercial Change Authority | Implemented with separate evidence levels. The trusted edit authority is deployed but intentionally dormant: exact-revision simulation, sales authorization request, admin authorization, gated atomic quote/version apply, immutable apply/invalidation receipts, and exact transport-ambiguous outcome reconciliation are present; both enforcement gates remain off, and no production apply or tenant gate promotion is claimed. The Living Opportunity guest-count counterfactual is implemented in source only for `admin`/`sales` staff who also have ordinary edit authority and an exact loaded package/catalog: local mode returns a deterministic advisory `ImpactPreview` without a receipt, while connected Firebase mode delegates to the governed simulation and may return an immutable receipt. It previews only the reversible local guest scenario; an explicit handoff may stage that scenario in the priced editor, but the preview never saves, authorizes, or applies it. Uncertain connected requests retain the exact tenant, quote, base revision, and request identity for reconciliation. No ambient-host deployment, hosted acceptance, or human acceptance is claimed. | `functions/commercialChangeAuthority.js`, `functions/commercialChangeImpactEvaluator.js`, `functions/index.js` (`simulateCommercialQuoteChange`, `requestCommercialQuoteChangeAuthorization`, `getCommercialQuoteChangeAuthorizationState`, `authorizeCommercialQuoteChange`, `reconcileCommercialQuoteChangeApplyOutcome`, governed `updateQuoteDraft`), `src/lib/commercialChangeAuthorityClient.js`, `src/lib/ambientPricingPreview.js`, `src/lib/impactPreviewAdapter.js`, `src/components/CommercialChangeImpactPanel.jsx`, `src/components/AmbientLivingOpportunityRoute.jsx`, `src/components/AmbientLivingOpportunity.jsx`, `src/App.jsx`, `firestore.rules`, focused server/client/component/integration/emulator tests |
| 43 | Commercial Dependency State and reconciliation | Implemented (deployed; bounded staff projection, exact named invalidation reconciliation, immutable receipt, and derived publication eligibility only—no automatic generation, publication, delivery, acceptance, booking, payment, or completion; hosted acceptance pending) | `functions/commercialChangeAuthority.js`, `functions/index.js` (`getCommercialDependencyState`, `reconcileCommercialDependencyState`), `src/lib/commercialChangeAuthorityClient.js`, `src/components/CommercialDependencyStatePanel.jsx`, `src/components/QuoteHistoryModal.jsx`, `firestore.rules`, focused server/client/component/integration tests |
| 44 | Trusted Kitchen BEO generation and artifact freshness | Implemented (deployed; canonical server generation, immutable actor/time/fingerprint/PDF receipt, strict retained-byte base64/length/SHA validation on replay, response, status, and download, exact current and prior-receipt download, five-state freshness, and qualifying Kitchen BEO invalidation reconciliation; hosted operator acceptance and publication evidence pending) | `functions/kitchenBeoAuthority.js`, `functions/kitchenBeoPdf.js`, `functions/index.js` (`getKitchenBeoArtifactStatus`, `generateKitchenBeo`, `downloadKitchenBeoReceipt`), `src/lib/kitchenBeoClient.js`, `src/components/KitchenBeoArtifactPanel.jsx`, `src/components/QuoteHistoryModal.jsx`, `firestore.rules`, focused server/PDF/client/component/integration tests |
| 45 | Decision Debt and policy controls | Implemented (deployed; bounded server-derived non-predictive priority from persisted unresolved dependencies, tenant calendar, exact factors and source, plus versioned admin-only policy beside Workflow; missing authoritative commercial exposure produces explicit unknown factor/score/urgency rather than a guessed multiplier; non-admin staff are read-only, hosted acceptance is pending, and no accounting/recovered-revenue claim is made) | `functions/decisionDebt.js`, `functions/index.js` (`getDecisionDebtSnapshot`, `configureDecisionDebtPolicy`), `src/lib/decisionDebtClient.js`, `src/components/DecisionDebtPanel.jsx`, `src/components/QuoteDecisionDebtPanel.jsx`, `src/components/SalesWorkflowModal.jsx`, `firestore.rules`, focused server/client/component/integration tests |
| 46 | Revenue Autopilot operations, post-event review requests, and customer unsubscribe | Implemented (deployed but intentionally dormant; four visible runtime/tenant/send/provider gates; provider-independent deterministic preparation with bounded per-lane receipts; evidence-preserving stop/reconciliation behavior with visibly distinct withheld-versus-provider-accepted receipts; exact latest-message Attention supersession, staff-reply resolution, scheduled repair, and operations reads limited to currently open unread replies without narrowing job history; four email lanes, Customer 360 controls, public unsubscribe, and 15-minute scheduler foundations; runtime/sends remain off and no Resend provider, production-data, recovered-revenue, or human acceptance evidence is claimed) | `functions/revenueAutopilot.js`, `functions/revenueAutopilotAuthority.js`, `functions/revenueAutopilotTemplates.js`, `functions/index.js` (`getRevenueAutopilotOperations`, policy/control/materialization/ack/reconcile/unsubscribe callables, `sendQuotePortalConversationMessage`, `runRevenueAutopilotSchedule`, `revenueAutopilotResendWebhook`), `src/lib/revenueAutopilotClient.js`, `src/components/RevenueAutopilotOperations.jsx`, `src/components/RevenueAutopilotCustomerControls.jsx`, `src/components/RevenueAutopilotUnsubscribePage.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/CustomerWorkspaceView.jsx`, `firestore.rules`, focused server/client/component/conversation/schedule/webhook tests |
| 47 | CWF-16 Event Workspace: event-first quote detail with exact identity, bounded quote/Workflow condition and Needs You action, proposal-scoped readiness, stable `Why?` reason codes, explicit unavailable Flexibility/Alignment, existing context/action routes, selected sold scope, lifecycle, governed edit boundary, and responsive desktop/mobile presentation | Implemented (deployed in `v0.5.0`; one deterministic presentation selector reuses existing Workflow Attention and proposal-readiness facts; presentation/navigation only, no new backend/data authority or event-wide readiness, inventory/capacity/payment/booking/completion inference; public route reachability verified, authenticated hosted use and human acceptance pending) | `src/components/eventWorkspacePresentation.js`, `src/components/EventWorkspaceView.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/App.jsx`, `src/styles.css`, focused model/component/integration tests, `e2e/customer-centered-workspace.spec.js`, `docs/design/cwf-16-implementation/design-qa.md` |
| 48 | Event Messaging Station at `/app/messages`: temporary-flagged central staff inbox segregated by canonical quote/event, bounded latest-50 same-tenant quote listener ordered by body-free conversation summary, normalized event/thread presentation, fail-closed portal eligibility, search/filter/event grouping, exact selected staff or portal signal, callable-owned canonical-body reload, and explicit live/cache/paused states | Implemented (deployed in `v0.5.0` with the governed production workspace flag enabled; `Live updates` is a listener-state claim, not guaranteed latency, delivery, read receipt, typing, or presence; direct message reads remain denied; public deep-link reachability verified, authenticated hosted use, production-data behavior, and human acceptance pending) | `src/lib/workspaceRoutes.js`, `src/lib/conversationInbox.js`, `src/lib/conversationSignalClient.js`, `src/hooks/useConversationInbox.js`, `src/components/MessagingStation.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/App.jsx`, `functions/index.js` (`sendQuotePortalConversationMessage` body-free exact-portal projection), focused route/inbox/component/server tests, `docs/MESSAGING_STATION_ARCHITECTURE.md` |
| 49 | Structured change-request record: with the flag-gated client-request panel, staff can record which deterministically parsed proposals were staged from one exact customer change request through the callable-only `recordChangeRequestParse` boundary — server-validated against the exact stored request identity and message hash, staff-attributed, bound to the quote revision at record time, create-only and replay-stable, with browser writes denied by rules. A second callable, `linkChangeRequestResolutionVersion`, performs the record's only permitted follow-up mutation: fired best-effort right after a quote save that already fully succeeded on its own, it binds the record to the resulting version once — write-once, idempotent on replay, server-verified as newer than the version on file at record time | Implemented (both callables and their deny-only browser rules are deployed in `v0.7.0`; they are internal audit attestations only — they do not reply to the customer, mutate the quote, portal, or versions, or claim delivery, acceptance, booking, or payment; a missed link is a one-step-short audit trail, not a lost or corrupted one; authenticated hosted staff acceptance pending) | `functions/changeRequestRecord.js`, `functions/index.js` (`recordChangeRequestParse`, `linkChangeRequestResolutionVersion`), `firestore.rules`, `src/lib/changeRequestRecordClient.js`, `src/components/ChangeRequestPanel.jsx`, `src/App.jsx`, `src/lib/__tests__/changeRequestRecord.server.test.js`, `src/components/__tests__/changeRequestRecordAction.test.jsx`, `src/rules/__tests__/firestore.rules.test.js` |
| 50 | Pilot NOW and Event Room decision presentation: bounded Home evidence is restated as decision cards; quote detail adds proposal completeness, advisory decisions, and an evidence-bounded accepted/booked cascade receipt chain | Implemented (deployed in `v0.6.0` with the production build gates enabled; public route reachability verified and authenticated staff acceptance pending; presentation only, with no inferred delivery, payment, booking, or readiness) | `src/components/NowView.jsx`, `src/components/nowPresentation.js`, `src/components/EventWorkspaceView.jsx`, `src/components/decideStackPresentation.js`, `src/components/cascadePresentation.js`, focused component/selector tests, production-flag Playwright matrix |
| 51 | Deterministic CREATE and progressive guest certainty: browser-only intent extraction fills the ordinary draft after review, while approximate/ranged guest counts show a draft-only band priced through the existing client calculator | Implemented (deployed in `v0.6.0` with the production build gates enabled; authenticated staff acceptance pending; saved authority remains the exact recorded count and server pricing) | `src/components/CreateIntake.jsx`, `src/components/intentExtraction.js`, `src/components/pricingBand.js`, `src/components/LiveBreakdown.jsx`, focused tests, production-flag Playwright matrix |
| 52 | Pilot guided selling and command grammar: existing recommendations become decision cards and plain-language staff commands share the deterministic client-change grammar with preview-before-apply staging | Implemented (deployed in `v0.6.0` with the production build gates enabled; authenticated staff acceptance pending; staged draft edits only, normal save remains authority) | `src/components/WizardSteps.jsx`, `src/components/guidedSellingPresentation.js`, `src/components/PilotCommandBar.jsx`, `src/components/changeRequestParse.js`, focused tests, production-flag Playwright matrix |
| 53 | Fail-closed staff margin presentation from tenant-recorded package, item, rental, add-on, and applicable labor costs, with optional target policy | Implemented (deployed in `v0.6.0` with the production build gate enabled; authenticated staff acceptance pending; unavailable when any required input is absent, never estimated or customer-facing) | `src/components/marginPresentation.js`, `src/components/LiveBreakdown.jsx`, `src/components/__tests__/marginPresentation.test.js`, production-flag Playwright matrix |
| 54 | Catalog cost entry and margin advisory, and margin's spread across every other pricing surface built after `v0.6.0`: staff record optional tenant item/labor costs and a target margin % policy in Catalog Admin, blank always meaning not-recorded rather than $0; row 53's fail-closed presentation and a below-target commercial advisor card compute from these costs, and the same fail-closed `buildMarginPresentation` now also prices the CREATE band-pricing range, Scenario Compare's good/better/best cards and comparison table, and the change-request/command-bar impact preview — each independently unavailable until every line it covers has a recorded cost | Implemented (deployed in `v0.7.0` behind `VITE_PILOT_MARGINS_ENABLED`; costs are staff-only catalog data and never reach any customer-facing projection; Catalog Admin preserves explicit save reconciliation, revision-conflict, and reload-failure recovery; no new callable, export, or pricing authority; authenticated hosted staff acceptance pending) | `src/data/mockCatalog.js`, `src/hooks/useCatalogData.js`, `src/components/AdminCatalogModal.jsx`, `src/components/marginPresentation.js`, `src/components/LiveBreakdown.jsx`, `src/components/pricingBand.js`, `src/components/QuoteCompareModal.jsx`, `src/components/changeRequestParse.js`, `src/components/PilotCommandBar.jsx`, `src/components/ChangeRequestPanel.jsx`, `src/data/__tests__/mockCatalog.costFields.test.js`, `src/hooks/__tests__/useCatalogData.savePlan.test.js`, `src/components/__tests__/marginPresentation.test.js`, `src/components/__tests__/adminCatalogSaveCapabilityState.test.jsx`, `src/components/__tests__/pricingBand.test.js`, `src/components/__tests__/liveBreakdownBand.test.jsx`, `src/components/__tests__/quoteCompareModalMargin.test.jsx`, `src/components/__tests__/changeRequestParse.test.js`, `src/components/__tests__/pilotCommandBar.test.jsx`, `src/components/__tests__/changeRequestPanel.test.jsx` |
| 55 | Customer decision room: the exact-token portal presents the existing customer-safe projection as a calm, content-first room with Event, Menu and service, Pricing, Planning assumptions, tenant terms, Optional additions, Response, and Questions sections. Contextual **Ask a question** actions open the one existing conversation composer and acknowledge whether the starter text was staged, an existing draft was preserved, a prior send still needs reconciliation, or the thread is read-only/unavailable. Staff-marked add-ons and rentals remain bounded name-and-price suggestions; choosing one prepares a reversible `Please add X.` line in the ordinary change-request note. Only a line generated during the current session can be removed by the same control, and switching to accept or decline discards still-generated addition lines with an explicit acknowledgement. Customer-authored words are preserved. Browser-local exact-token fallback now mirrors the canonical bounded terms and option shape. | Implemented (source/local candidate requiring both `VITE_PILOT_DECISION_ROOM_ENABLED` and default-off `VITE_AMBIENT_UI_ENABLED`; the released `v0.7.0` subset remains when only Decision Room is enabled. No new callable, message field, catalog exposure, direct quote mutation, pricing authority, or trust boundary. Options do not change selection, totals, revision, payment, booking, or provider evidence; customers must review and send the note, staff must review it, and a trusted save remains authoritative. Dedicated local proof passes at 390, 768, and 1440px with axe, 44px targets, overflow, clipping, and collision checks. Deployment, connected exact-token behavior, production data, provider outcomes, and human acceptance remain open.) | `src/components/CustomerPortalView.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/lib/customerDecisionRoom.js`, `src/lib/quoteStore.js`, `src/data/mockCatalog.js`, `src/hooks/useCatalogData.js`, `src/components/AdminCatalogModal.jsx`, `functions/quoteCreation.js` (`buildPortalDecidableOptions`), `src/styles.css`, `e2e/customer-decision-room.spec.js`, `src/lib/__tests__/customerDecisionRoom.test.js`, `src/lib/__tests__/quoteStore.portal.test.js`, `src/components/__tests__/customerPortalAskAbout.flagOff.test.jsx`, `src/components/__tests__/customerPortalAskAbout.flagOn.test.jsx`, `src/components/__tests__/quoteConversationPanelPrefill.test.jsx`, `src/data/__tests__/mockCatalog.costFields.test.js`, `src/hooks/__tests__/useCatalogData.savePlan.test.js`, `src/lib/__tests__/quoteCreation.server.test.js` |
| 56 | Model-assisted intent parse lane (owner-approved, OpenAI + Anthropic): the pure core module — config gate (enabled flag, provider allowlist, per-provider default models), bounded request sanitization, deterministic strict-JSON prompt, provider request builders that never read the environment, untrusted-output validation allowlisting fact fields and forcing every surviving fact to low confidence with a model source — now ships together with its full user-reachable surface: the `functions/` runtime twin, the staff-only same-organization `parseIntentDraft` callable, a client boundary distinguishing the designed dormant reply from a retryable provider outage, and a Model assist button in the CREATE canvas whose suggestions each require explicit per-fact Confirm before touching the draft | Implemented (deployed dormant in `v0.7.0`; remains unavailable until an administrator enables `INTENT_PARSER_ENABLED`, selects a provider, and binds that provider's Secret Manager key; until then the button reports the lane off and typed structuring is untouched; persists nothing; `model-assisted-intent-parse` contract revision 2 with full six-state evidence, `stale` excepted as impossible for a stateless parse; hosted staff/provider acceptance pending) | `functions/index.js` (`parseIntentDraft`), `functions/intentParserCore.cjs`, `src/lib/intentParserCore.cjs`, `src/lib/intentParseClient.js`, `src/components/CreateIntake.jsx`, `src/lib/__tests__/intentParser.server.test.js`, `src/components/__tests__/createIntakeModelAssist.test.jsx`, `docs/INTENT_INTAKE_ADR.md` |
| 57 | Stronger deterministic CREATE reader, built to an owner-directed "strongest on the market" bar and hardened by two rounds of adversarial verification that executed the real code rather than reasoning abstractly: staff counts (servers/chefs/bartenders, digits/word-numbers/ranges, role synonyms); time ranges filling both start time and computed service hours with cross-midnight support; reversed guest phrasing ("party of 50"); reversed dates ("the 12th of September"); and confirm-only relative weekdays | Implemented (source/local candidate; browser-only, no provider, no I/O; every verifier-confirmed false positive or misfire — contact-hours context, month-day digit theft, meridiem-wraparound, month-prefix word collisions, relative-weekday precedence — is fixed and locked in as a named regression test; the model-assist lane's fact allowlist gained the same fields in both core-module copies) | `src/components/intentExtraction.js`, `src/components/__tests__/intentExtraction.test.js`, `src/lib/intentParserCore.cjs`, `functions/intentParserCore.cjs` |
| 58 | Event-shape memory (design §4.10, first memory slice): once a CREATE reading yields both an event type and a guest count, a flag-gated deterministic aggregate of the tenant's own accepted/booked quote history — same event type, same fixed guest band — reports median servers/chefs/bartenders and half-hour-rounded hours, plus any rental in a strict majority of matches, as a provenance-labeled suggestion | Implemented (source/local candidate; `VITE_PILOT_MEMORY_ENABLED` default off and deliberately not production-bound yet, unlike the other eight pilot gates; no AI, no cross-tenant learning — `getQuoteHistory` is already tenant-scoped server-side; below a minimum sample of 3 the honest reply is "not enough history yet"; applying writes only staffing/hours to the draft, rentals stay a read-only mention so an existing selection is never silently overwritten; `event-shape-memory` contract, six-state evidence, `stale` excepted as impossible for an always-fresh read) | `src/lib/eventShapeMemory.js`, `src/components/CreateIntake.jsx`, `src/styles.css`, `src/lib/__tests__/eventShapeMemory.test.js`, `src/components/__tests__/createIntakeEventShapeMemory.flagOn.test.jsx`, `src/components/__tests__/createIntakeEventShapeMemory.flagOff.test.jsx` |
| 59 | Authoritative operational staffing and Staff workspace: exact-revision staffing requirements, bounded same-tenant operational profiles with `operator_recorded` availability, direct `operator_confirmed` assignments, explicit coverage gaps, cross-date schedule fences, and immutable profile/plan receipts; plus an admin-only private record for contact/photo, qualifications, availability preferences, rates, travel, briefing defaults, attendance summaries, reliability and notes. Revision-bound briefings can be printed or handed off to the default email app. Administrators can preview and manually dispatch one exact-assignment invitation to the verified private email; signed provider events and bearer-link accept/decline acknowledgements appear as separate evidence. | Implemented (source; independently default-off behind the global `OPERATIONAL_STAFFING_AUTHORITY_ENABLED`, tenant `operationalStaffingAuthorityEnabled`, and presentation `VITE_OPERATIONAL_STAFFING_ENABLED` gates; provider dispatch also requires the approved Resend sender and token secret; admins configure records and invitations, admins and sales staff read/apply plans, and local fallback stays `local_draft`; provider acceptance is not delivery, and acknowledgement is not attendance or payroll evidence; no deployment, provider event, recipient use, hosted behavior, or human acceptance is claimed) | `functions/operationalStaffingAuthority.js`, `functions/operationalStaffingRuntime.js`, `functions/staffDirectoryAuthority.js`, `functions/staffInvitationAuthority.js`, `functions/index.js` (staffing, directory, invitation, response, and shared signed-webhook routing), `src/lib/operationalStaffingClient.js`, `src/lib/staffDirectoryClient.js`, `src/lib/staffInvitationClient.js`, `src/components/OperationalStaffingPanel.jsx`, `src/components/StaffWorkspace.jsx`, `src/components/StaffInvitationResponsePage.jsx`, `src/components/AmbientLivingOpportunity.jsx`, `firestore.rules`, focused server/client/component/rules/emulator tests |
| 60 | Ambient orientation, NOW, Opportunities, Clients, Library, exact arrivals, Living Opportunity, intelligent objects, sensory feedback, deterministic Pilot, customer decision-room foundations, purpose-bearing editorial grammar, accepted/booked receipt domains, and equivalent mobile manipulation (AIUI-01–50) | Implemented in the post-`v0.8.1` source candidate behind the governed Ambient/Pilot presentation boundaries; 50/50 items are material and 0/50 are formally closed. Canonical routes, role gates, tenant scope, server pricing, catalog save authority, exact-token portal authority, and provider evidence boundaries remain unchanged. The current completion slice separates Opportunities/Event Room/action-controller concerns, registers the shared editorial surface grammar, keeps customer acceptance, contract, payment, BEO, authoritative staffing, and closeout evidence distinct, and sends swipe/drag/button/keyboard reordering through the same unsaved-review path. Duplicated focused route implementations, the replaced Command Center, redundant search palette, and legacy Clients table are absent from the Ambient build graph while the compatibility app remains intact; all other AIUI-48 deletion waits for parity, accepted rollback, release, and promotion evidence. NOW limits itself to bounded priorities; Opportunities keeps proposal, pricing, customer, and operational state separate; Clients and Library retain their exact scoped reads and guarded authorities. Pilot v1 remains limited to navigation, query, draft mutation, and simulation. No deployment, real microphone/service result, production-data result, provider outcome, retirement authorization, or human acceptance is claimed. | `src/App.jsx`, `vite.config.js`, `src/components/WorkspaceRoute.jsx`, `src/components/WorkspaceShell.jsx`, `src/components/AmbientNowView.jsx`, `src/components/AmbientOpportunitiesStream.jsx`, `src/components/AmbientCustomerDirectoryView.jsx`, `src/components/AmbientClientsView.jsx`, `src/components/AmbientLibraryRoute.jsx`, `src/components/AmbientLivingOpportunity.jsx`, `src/components/AmbientOperationalReceipts.jsx`, `src/components/AmbientGlobalPilotSurface.jsx`, `src/components/PilotCommandBar.jsx`, `src/components/CustomerPortalView.jsx`, `src/lib/commandCenterEvidence.js`, `src/lib/quoteHistoryController.js`, `src/lib/ambientSurfaceGrammar.js`, `src/lib/ambientOperationalReceipts.js`, `src/lib/ambientGestureController.js`, `src/lib/ambientLegacyRetirement.js`, Ambient object/Pilot helpers and contexts, focused unit/component/browser tests, `docs/AMBIENT_WORKSPACE_ARRIVAL_ADR.md` |
| 61 | Public marketing and system-overview routes: hospitality-first acquisition at `/`, a saved dark product/operating-system overview at `/system`, responsive navigation, product explanations, and guarded entry into the staff or buyer flows | Implemented (deployed; public route reachability is verified, while marketing effectiveness and human acceptance are separate) | `src/components/MarketingPage.jsx`, `src/components/SystemMarketingPage.jsx`, `src/App.jsx`, `src/landing.css`, `src/marketing.css`, public-route browser coverage |
| 62 | Progressive Web App scaffold: install manifest, production service-worker registration, versioned core-shell cache, same-origin asset caching, and navigation fallback | Partial (deployed scaffold; it does not make authenticated Firebase data or quote operations fully offline, and installability/device acceptance is not claimed) | `index.html`, `public/manifest.webmanifest`, `public/sw.js`, `src/main.jsx` |
| 63 | Operations Audit: admin-only same-tenant delivery health, seven-day integration trend, role counts, and role-stamped sensitive-action history with retry candidates kept distinct from resend proof | Implemented (deployed; server-derived operational evidence only, not external CRM activity, provider delivery, or human acceptance) | `functions/operationsAudit.js`, `functions/index.js` (`getOperationsAuditSnapshot`), `src/lib/commerceOps.js`, `src/components/IntegrationOpsModal.jsx`, `src/lib/__tests__/operationsAudit.server.test.js` |
| 64 | Motion, sound, and reduced-motion preferences: centralized optional cue routing with persisted workspace mute, tokenized one-shot interaction motion, and CSS/JavaScript reduced-motion guards that preserve final state without making sound or animation operational evidence | Implemented (the base interaction system is deployed in `v0.7.0`; the Ambient shell preference and semantic-feedback extensions are in the `v0.8.0` source candidate, default-off, and not hosted or human accepted) | `src/components/soundKit.js`, `src/components/shimmerChime.js`, `src/components/wizardMotion.css`, `src/components/scheduleMotion.css`, `src/components/portalCeremony.css`, `src/components/ShimmerReveal.jsx`, `src/components/DigitRoll.jsx`, `src/components/WorkspaceShell.jsx`, `src/components/ambient/ambientFeedback.js`, focused motion/sound tests |
| 65 | Stripe Connect isolated control-plane foundation: explicit owner authority and backfill; named, separately pinned Firebase codebases; provider-disabled staging infrastructure; a current-role authority projection; redacted status and replay-stable owner handoff contracts; exact `connect-control` repository and HMAC-scoped limits; an immutable edge-command/leased-worker receipt bridge; private post-provider quarantine evidence; and an injected Accounts v2 Sandbox adapter with exact platform/mode preflight | Partial (source/local only). Every edge action must match a receipt-bound authority projection of the current enabled, verified admin set and canonical owner; projections expire within ten minutes. Owner onboarding also requires recent authentication, an exact expected App Check application contract, revision/generation/payload bindings, and one active deterministic ten-minute POST handoff whose bearer value is never stored. The repository reserves generation, authority digest, and a 30-day recovery deadline, while the immutable command's `qpcmd` value is the sole provider idempotency identity. The leased worker rechecks authority/state before provider access and the repository rechecks again after account creation before binding. Post-create validation failure, authority drift, or collision retains provider identity and occurrence only in private quarantine evidence; exact retry after command-receipt interruption reconstructs that quarantine without another Stripe call. Account Link disclosure also requires a post-provider authority/state receipt; `provider_withheld` returns only recovery and never exposes the Stripe URL. Exact rates are refresh 6/principal and 6/organization per five minutes with a ten-second organization interval, begin 6/principal and 10/organization per day, and handoff 3/principal per 15 minutes and 10/organization per day. The adapter verifies the exact platform account and Sandbox balance before access, validates provider-shaped merchant application timestamps, and requires both card-payment and payout capability activity before `ready`. `functions-connect/index.js` still exports nothing; the manifest remains unbound/provider-disabled, and no authority publisher, edge/worker runtime, App Check enforcement/consumption, cloud resource, credential, callable, HTTP route, connected account, Account Link, Stripe request, deployment, hosted Sandbox UAT, production behavior, or human acceptance exists. | `functions/organizationAuthority.js`, `scripts/organization-owner-backfill-plan.mjs`, `scripts/backfill-organization-owner.mjs`, `functions-connect/`, `config/stripe-connect/staging-foundation.json`, `firebase.json`, `firebase.connect.staging.json`, `firestore.connect-control.rules`, `infra/stripe-connect/`, `scripts/check-stripe-connect-foundation.mjs`, `scripts/check-stripe-connect-infra.mjs`, `scripts/check-stripe-connect-onboarding.mjs`, `src/lib/__tests__/stripeConnectAuthorityProjection.test.js`, `src/lib/__tests__/stripeConnectCommandBridge.test.js`, `src/lib/__tests__/stripeConnectCommandOrchestration.test.js`, `src/lib/__tests__/stripeConnectRuntimeAdapters.test.js`, explicit default-codebase deploy scripts, focused tests, `docs/STRIPE_CONNECT_PROGRAM.md` |
| 66 | Owner/admin team role authority: one in-flow Team access surface infers the exact same-organization role, explains why the change is available, its consequence, the do-nothing result, and confidence/provenance before an outcome-named command. Canonical owners manage admin and sales authority; non-owner admins manage sales authority only. The trusted callable binds exact verified email, expected role, one replay-stable request, immutable private receipt, and custom-claim synchronization. | Implemented (source/local only). Password and Google accounts can establish a fresh five-minute authentication proof. App Check is wired as opt-in reCAPTCHA Enterprise monitoring with environment-specific public-key configuration; enforcement and limited-use token replay protection remain off until hosted observation and explicit promotion. Emulator acceptance proves owner/admin boundaries, idempotent replay, owner-demotion denial, receipt privacy, and claim synchronization. No provider registration, hosted role change, deployment, production-data mutation, or human acceptance is claimed. | `functions/organizationRoleAuthority.js`, `functions/index.js` (`getOrganizationRoleRoster`, `mutateOrganizationRole`), `src/components/OrganizationRoleAuthorityPanel.jsx`, `src/components/IntegrationOpsModal.jsx`, `src/lib/authClient.js`, `src/lib/firebase.js`, `src/lib/organizationService.js`, `firestore.rules`, `scripts/customer-centered-authority-emulator-acceptance.mjs`, focused server/client/component/rules tests |
| 67 | Deployment-owned owner SMS provider choice: admin-safe, non-secret runtime-field completeness and one bounded diagnostic over the existing one-way owner-alert rail, with explicit `none`, `twilio`, or `pingram` runtime selection. Quote and payment events reserve a private transactional outbox; one worker claim permits one provider call; organization-scoped versioned HMACs protect destinations; signed Pingram callbacks enter a durable reprocessable inbox; and evidence keeps request acceptance, indeterminate dispatch, carrier delivery, failure, opt-out hold, and recipient-device receipt distinct. | Implemented (source/local candidate only; production remains `NOTIFICATIONS_SMS_PROVIDER=none`). No customer SMS or two-way inbox is implied. Provider credential presence is never inferred, the worker fails closed when its bound secret is unavailable, a claimed or indeterminate send is never automatically resent, and signed unsubscribe or exact inbound STOP creates an indefinite cross-provider hold with no browser or callable clear path. Pingram deployment, webhook registration, provider traffic, sender/compliance acceptance, controlled recipient UAT, hosted behavior, and human acceptance remain pending. | `functions/index.js` (`getIntegrationSetupStatus`, `sendIntegrationTestSms`, `notifyOwnerNewQuote`, provider workers/reconciliation, `pingramSmsWebhook`), `functions/ownerSmsAttempts.js`, `functions/pingramSms.js`, `src/components/SmsProviderPanel.jsx`, `src/components/IntegrationOpsModal.jsx`, `src/lib/commerceOps.js`, `scripts/materialize-functions-env.mjs`, `scripts/owner-sms-emulator-acceptance.mjs`, `firestore.rules`, focused provider/attempt/panel/materializer/rules tests |

Ambient NOW source note for row 60: when both the default-off Ambient and NOW
presentation gates are enabled, `AmbientNowView` replaces the denser card
dashboard with at most three already-ranked priorities from the current bounded
staff snapshot. It withholds caught-up language for stale, partial, unknown, or
truncated reads and when recorded payment steps remain, and limits **Quiet
progress** to timestamp-backed internal workflow receipts. The ordinary NOW
surface remains the flag-off rollback. This is `v0.8.0` candidate source only;
it adds no Firebase, provider, payment, pricing, role, or mutation authority and
has no hosted or human-acceptance evidence.

## Implementation Chronology

Dates below are Git author dates unless a merge/integration distinction is
stated. IDs may recur when a later cohort materially hardened or expanded an
earlier feature. Detailed history remains canonical in `CHANGELOG.md`.

| Source / integration cohort | Git evidence | Functions served | Feature IDs represented |
|---|---|---|---|
| 2026-02-26 | `1ce0a73` | Initial quote builder, live totals, catalog/history, reporting, and proposal/PDF foundation | 4–8, 14, 27 |
| 2026-02-27 | `3b5f902`, `fa4e6a7` | Scenario comparison, recommendations, and editable pricing administration | 10, 19–20 |
| 2026-03-08 | `d628cc4` | Verified-role authentication, customer portal foundation, and PWA shell | 12, 18, 62 |
| 2026-03-09–11 | `219fc4d`, `7e82941`, `1222c4a`, `35a1868`, `7a99d13`, `9b455ae`, `d82ee9f`, `b767f20`, `95105c9` | Branding, booking/contract operations, schedule, integrations, diagnostics, guided selling, and deposit initiation | 11, 13, 15–17 |
| 2026-03-17–20 | `69323d0`, `466c775`, `413d9b4`, `33889ad`, `c4ab915`, `f9980f0` | Firestore menu, five-step/admin consolidation, flags, multi-tenancy, authoritative pricing, versions, and QA | 1–4, 7, 9, 22 |
| 2026-03-21–27 | `712f124`, `aaad80b`, `4f3da37` | Portal rotation/UX and hardened tenant provisioning | 12, 18, 25 |
| 2026-04-02 authored; 2026-05-29 merged | `a18cfa6`, `3d0fffc` | Canonical menu sync, staffing parity, and guarded permanent quote deletion | 2, 8, 15 |
| 2026-08-03–05 | `dc460e3`, `1ae6c23`, `d2747c6` | Public product surfaces, workflow/approvals, import, provisioning, authoritative delivery, and starter catalogs | 8, 23–25, 61 |
| 2026-08-04 authored; integrated through 2026-08-06 | `e725e61`, `5f2915b`, `b27560c`…`d6518eb` | Separate deposit/final-balance payment rails and invoice-first buyer onboarding | 8, 26 |
| 2026-08-05–06 | `2a812ab`, `ed9a6f2`, `1886c19`, `c389f65`, `2ab6b5f`, `1b02cea`, `41e0a27` | Proposal acceptance, analytics, Operations Audit, BEO, customer projection, themes, and portal conversation | 12, 14, 21, 27–28, 63 |
| 2026-08-08–09 | `a539d00`…`d1b5d6b`; first `v0.4.0`, production workspace flag in `v0.4.2` | Customer-centered shell/360/search, commercial intelligence, governed operations, and Event Workspace integration | 29–47 |
| 2026-08-10 | `3cca8cc` (`v0.5.0`) | Event Workspace and central Event Messaging Station | 47–48 |
| 2026-08-10 | `c9819f9`; deployed as `4f4e00d` (`v0.6.0`) | Post-competitive Pilot decision, CREATE, guided-command, margin, and sensory presentation layer | 49–53, 64 |
| 2026-08-11 | `fb0aacc` (`v0.7.0`, deployed through governed Firebase `all` run `31529170963` and Vercel run `31530050353`) | Structured-request follow-up, catalog costs, decision room, model/deterministic intake, and event-shape memory | 49, 54–58 |
| Observed 2026-08-12 | Uncommitted working tree; no Git commit or tag | Privacy-bounded Ambient analytics extension, authoritative staffing, Ambient workspace/orientation, and preference extensions | 14, 59–60, 64 |
| Observed 2026-08-13 | Source branches; no Connect deployment or provider evidence | Explicit organization-owner authority, owner/admin team role authority, recent-auth proof, App Check monitoring contract, deploy-empty Stripe Connect runtime isolation, and apply-disabled staging infrastructure source | 18, 65–66 |

## Functional-Domain Index

| Functional domain | Function served | Feature IDs |
|---|---|---|
| Platform, identity, safety, and governance | Tenant model/context, authentication, security/audit depth, validation, provisioning, capability traceability, install shell, owner/admin team access authority, and isolated Connect control-plane foundation | 1, 17–18, 21–22, 25, 32, 62, 65–66 |
| Catalog, pricing, intake, and commercial intelligence | Menu/admin, authoritative calculations, builder guidance, dependency/change authority, margin, parsing, and memory | 2–5, 9–10, 19–20, 33, 38, 42–43, 45, 51–54, 56–58 |
| Quote, proposal, and customer relationship | Quote lifecycle/versioning, decision portal, customer projection/360/search/rebook/measures, structured change records, and decision-room requests | 6–8, 12, 28, 30, 34–36, 49, 55 |
| Booking, events, workflow, and operations | Booking/schedule, diagnostics, Workflow/import, BEO, routed workspace, timing/run-of-show/closeout, Event Workspace/Messaging, staffing, Ambient operations, and audit | 11, 15–16, 23–24, 27, 29, 31, 39–41, 44, 47–48, 59–60, 63 |
| Revenue, providers, reporting, and acquisition | Provider operations, commercial reporting, buyer onboarding, Autopilot preview/operations, owner SMS evidence, and public marketing/system overview | 13–14, 26, 37, 46, 61, 67 |
| Decision experience and accessibility | Pilot NOW/Event Room presentation plus motion, sound, and reduced-motion preferences | 50, 64 |

## Guided Flow (Where It Lives)
- Wizard flow entry and steps: `src/App.jsx`
- Wizard step model + validation + template defaults: `src/lib/wizardUi.js`
- Step components: `src/components/WizardSteps.jsx`
- Guided recommendation engine: `src/lib/recommendations.js`
- Guided selling toggle and rules config: `src/components/AdminCatalogModal.jsx`
- Default guided rules and normalization: `src/data/mockCatalog.js`

## Notes
- Non-dev fail-fast catalog behavior is implemented in `src/hooks/useCatalogData.js` and surfaced by blocking UI in `src/App.jsx`.
- New-tenant quote creation remains gated until a named positive-price package,
  an event type, and explicit pricing review are saved. Authoritative pricing
  rejects unconfirmed settings.
- Catalog persistence compares the originally loaded fingerprints and patches
  only changed records/settings; stale edits and identifier collisions require
  a reload instead of overwriting remote changes.
- Trusted Firebase edits re-price from current tenant configuration and
  atomically update quote/portal state with a new immutable version. Reopen and
  permanent cleanup are admin-callable operations; direct quote/portal deletes
  are denied, and terminal commercial evidence cannot be reopened or
  overwritten.
- Quote History derives expiry without a staff write for non-admin readers. An
  admin expiry persists only status, lifecycle, and update time to the quote and
  matching portal in one rules-enforced batch; a quote-only or portal-only
  transition is denied. Unresolved deliveries stay visible with persistence
  deferred until provider review, and one malformed legacy portal cannot blank
  the history list. The admin `Reopen` action restores an eligible expired quote
  as a draft with a new portal issuance.
- Firebase payment-request email, contract conversion, portal rotation, and
  permanent deletion require an exact approved request. Functions record the
  server-owned execution outcome in the quote workflow and in an admin-readable,
  server-write-only organization audit record.
- Stripe deposit and final-balance requests use separate exact approvals and
  payment rails. Deposit scope binds the quote revision, portal issuance,
  recipient, currency, and amount. Final balance additionally requires a
  booked contract and verified provider-paid deposit, derives the remainder
  from authoritative totals, and binds the contract and deposit evidence. A
  versioned ledger plus `payment.finalBalance` projection prevents one rail
  from rewriting the other.
  Both resumable server operations register a prepared Session with no
  browser-readable link, keep QuotePilot's URL copy only in a client-denied
  private dispatch record, and publish to the quote/portal only after durable
  email-provider acceptance. Ambiguous creation or send outcomes reuse the
  exact approval and executing-admin/provider identities; durable acceptance
  resumes publication without another send, while definite failure requires
  safe checkout neutralization before a new approval. Direct standalone
  checkout creation and browser payment-evidence writes fail closed.
  Explicit `STRIPE_MODE` must match the key and provider objects. Four signed
  Checkout Session events drive monotonic, payment-kind-isolated state, and
  same-tenant admins can reconcile the exact server-recorded Session for each
  rail. This source has no hosted Stripe test/live acceptance. Refund
  initiation/status and dispute handling remain manual or unimplemented.
- Generic Resend and quote Stripe API/webhook credentials are strict Firebase
  Secret Manager bindings on only their consuming Functions; the generic
  webhook does not receive the API key. The Functions dotenv materializer
  rejects all three values. Fresh provider rotation, ordered webhook overlap,
  exact hosted UAT, and revocation of the known exposed/cached values remain a
  hard release blocker.
- The public `/start` path uses the existing `tonicatering` Firebase project but
  remains an independently disabled Stripe test rail. Browser route and CTA
  flags require a syntactically valid non-placeholder public Turnstile site key;
  Cloudflare setup and human review remain separate evidence. The server
  separately verifies exact hosts/actions, reserves a request-scoped HMAC-keyed
  create lease before identity/order reads, consumes the public status network
  lease once per request before its first buyer-order read, and holds its
  Turnstile, buyer Stripe, and rate-limit secrets in Firebase Secret Manager.
  Exact create retries recharge
  network capacity without a duplicate email charge during the 24-hour
  reservation; only a prior signed-void order may be superseded after that
  email window.
  `createBuyerAccessInvoice` creates a true fixed $1 Hosted Invoice Page, and
  the dedicated buyer API client and webhook endpoint are pinned to Stripe API
  version `2024-06-20`; the generic quote Stripe client remains unchanged. Only
  the four supported signed, deduplicated invoice events may establish payment
  state. `invoice.paid` prepares the organization, neutral settings, Starter
  workspace plan entitlements, provisioning record, and pending invitation, but
  no user membership, admin role, claims, or access. `activation_sent` requires
  durably recorded onboarding-email provider acceptance. Independently,
  token-bound `provisioning` with `workspaceReady=true` offers a manual `/app`
  exact-email Firebase verification path and stops automatic polling without
  claiming Resend acceptance, membership, claims, or access. Only an exact
  matching verified Firebase email may consume the invite, and only `active`
  is access-ready.
  Controlled test-mode markers require exclusion from live revenue and paid-
  customer reporting. The quote-payment mode, credentials, `stripeWebhook`,
  deposit, and final-balance rails remain isolated. This is branch/source
  evidence, not Turnstile, Stripe, Firebase-delivery, hosted, or live-launch
  acceptance.
- Firebase quote email is bound to the saved revision and portal issuance. Only
  server-recorded provider acceptance owns the `sent` transition, and only
  acceptance for the exact current valid issuance activates its portal. If the
  provider accepts a message whose issuance is invalid or expired, QuotePilot
  retains that evidence as `requires_rotation` while keeping the portal
  inactive; guarded rotation and a separate accepted send are required.
- A definite provider failure whose safe retry window has expired starts a
  fresh delivery generation. An ambiguous outcome remains locked for same-key
  retry or audited provider review, and known server-observed acceptance cannot
  be reconciled as no-send. Generic staff updates cannot claim `sent` or
  `viewed`; the customer portal owns actual view evidence.
- Copy Portal and the PDF portal link require evidence for the exact current
  issuance and remain withheld after rotation. Legacy portal projections
  without `deliveryEvidence` fail closed and must recover through an approved
  resend or truthful provider reconciliation; projection backfill never
  fabricates acceptance evidence.
- Proposal acceptance is callable-owned and revision-preconditioned. The server
  requires the displayed portal projection to match the complete saved quote,
  records typed signer and versioned consent evidence, stores all receipt money
  in integer USD minor units, hashes the signed proposal snapshot, and creates
  a tenant-readable/server-write-only receipt. Firestore denies direct browser
  acceptance; payment and booking remain separate states.
- Menu backfill for `pricingType` + `active` is implemented in `scripts/seed-firestore-menu.mjs`.
- Multi-tenant org scoping is authoritative for protected catalog and quote
  writes. Direct quote creation is denied in Firestore; trusted Functions
  create canonical drafts, and existing write/version flows require an
  org-scoped quote instead of auto-migrating legacy global data.
- Feature flags for optional surfaces (portal/schedule/integrations/diagnostics/reporting/compare/guided selling) are normalized in `src/App.jsx` and managed in `src/components/AdminCatalogModal.jsx`.
- Firebase browser smoke coverage now includes both auth+rules and authoritative-pricing lanes under `e2e/`.
