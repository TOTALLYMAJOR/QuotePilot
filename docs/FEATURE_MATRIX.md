# Feature Matrix

Last updated: 2026-09-09 06:49:55 CDT

This matrix maps the master feature checklist to current implementation and source locations.
It is an inventory and chronology index, not the canonical detailed history or
current operational status. Use [`CHANGELOG.md`](../CHANGELOG.md) for the full
change record and [`PROJECT_STATUS.md`](../PROJECT_STATUS.md) for current
release, deployment, provider, risk, and acceptance state.

## Commercial platform and pricing-v2 candidate (September 4, 2026)

| Capability | Source status | Evidence boundary |
|---|---|---|
| Configurable Offers | Implemented through a compatibility adapter over existing Packages, with bounded choice groups, exact component references, active-reference validation, and authoritative anti-spoof checks. | Source/unit evidence only; no hosted tenant migration or human acceptance. |
| Commercial Templates | Implemented through existing Event Templates with shared metadata, explicit-edit preservation, provenance, and collision detection. | Templates remain inputs to one quote draft and never pricing authority. |
| Configuration Rules | Implemented as bounded data-only conditions/effects with deterministic evaluation, conflict detection, provenance, and the existing catalog revision/publication boundary. | No arbitrary formulas, scripts, workflow engine, or automatic quote mutation. |
| Price Waterfall and pricing-v2 | Implemented with integer USD minor units, half-up line/fee/tax/deposit rounding, fail-closed policy validation, exact reconciliation, immutable waterfall evidence, and explicit pricing-v1 compatibility. | Browser remains preview; server remains authoritative. Local 5,000-case parity is not hosted proof. |
| Exact payment amount provenance | Implemented through stored `totalCents`/`depositCents`, proposal snapshot validation, payment-ledger consistency checks, and exact final-balance subtraction. | Provider identity, idempotency, settlement, and reconciliation authority are unchanged. |
| Library commercial convergence and starter packs | The role-safe Library presents Offers, Components, Templates, and Pricing & Rules in a broad commercial ledger with contextual readiness secondary. Offers retain derived margin and project recorded choices read-only; Add-ons/Rentals expose visible derived Usage with stable IDs subordinate; Templates are independently collapsible across eight business groups; Rules are structured-first with Advanced raw recovery; Pricing separates Base, adjustments/context, Fees, Tax, and Deposit before Advanced policy. The readiness rail uses supplemental semantic icons with text as authority, ordinary text is at least 12px, recovery has one save/retry owner, embedded-tab changes synchronize breadcrumb/title without remounting or draft loss, mobile retains ledger-first order, and sales can inspect without administrator actions. Versioned starter packs remain setup inputs to the same draft. | Presentation-only local implementation `b593fe4d2c45ff5db20bafe205461157952b9ab4`: existing Package records, catalog draft/save/revision/publication, template/rule evaluation, server pricing, and role authority remain unchanged. **Your bundle** is derived from one quote draft and has no independent record or save path. Local Chromium passes Ambient Library 17/17 and Calm Four 10/10 with `VITE_OPERATIONAL_STAFFING_ENABLED=true`; exact 390/768/1440 captures and combined comparisons received a **9.5/10 — GO** local visual-review verdict. Full unit, build, capability/document governance, bundle, and CWV checks pass; `check:env` remains blocked by six absent local Firebase web configuration values. No push, exact-head CI, hosted, production, assistive-technology, or human acceptance is claimed. |

See the complete [commercial platform acceptance matrix](acceptance/commercial-platform-acceptance-matrix.md).

## Scope and Evidence (August 19, 2026)

- **First source:** the earliest identified Git commit containing the capability
  foundation. It establishes source lineage, not completeness.
- **Release containment:** an exact commit or tag contains the capability. It
  does not establish that any environment runs that revision.
- **Deployment:** a governed deployment receipt binds an exact revision to a
  target. It does not by itself prove authenticated behavior or correct
  production-data results.
- **Hosted/provider evidence:** the exact hosted role/data path or external
  provider outcome was observed. Public route reachability, configuration, and
  request acceptance remain narrower facts than end-to-end delivery or payment.
- **Protected operational controls:** row 59's tenant gate remains a separate
  operator mutation after deployment. Its source now requires a distinct
  workload-identity service account and short-lived Datastore token; that is an
  authentication boundary, not tenant activation or staffing acceptance proof.
- **Human acceptance:** a named person completed and accepted the intended
  workflow. This is never inferred from source, tests, CI, deployment, or
provider receipts.

## UX Convergence source candidate (September 4, 2026)

- **Commercial Workbench:** the existing `ProposalComposer` now presents the
  same draft, Guided form, calculation path, explicit save/version authority,
  and client-safe preview through Event, Customer, Experience, Staffing, and
  Commercials domains. The model is presentation-only; switching domains does
  not persist, reprice, version, fetch, or mutate catalog state.
- **Calendar-first Operations:** `/app/operations` now composes the existing
  `EventScheduleView` and its accepted/booked projection, month/week models,
  conflict and capacity logic, staff-lead mutation, checklist, and run-of-show
  context. Month uses the broad primary canvas for the calendar and places its
  selected-day/event workspace below. Week uses a vertical time axis, seven
  dated columns, start/duration geometry, visible collision lanes, and a
  secondary contextual rail. Operational sections begin collapsed, and
  conflict comparison is contextual to derived conflict review. There is no
  manual conflict-resolution state: authoritative date, time, duration, venue,
  guest-count, or lifecycle changes cause the existing model to recompute.
  `/app/schedule` remains a compatibility
  route to that same Calendar capability. Phone width derives an agenda from
  the same event/conflict model rather than creating separate Calendar logic.
- **Exact handoffs and authority boundary:** Now and the canonical Living
  Opportunity can hand the exact quote identity to Calendar through the
  existing arrival contract; Calendar can return to the exact Opportunity.
  These entries and both Calendar routes fail closed when `eventSchedule` is
  disabled. Reporting remains independently controlled by
  `reportingDashboard`, while People retains the existing administrator and
  staffing-feature gates.
  No Event record, persistence authority, lifecycle value, pricing rule,
  staffing authority, provider integration, or live-telemetry claim was added.
- **Converged navigation:** the local candidate promotes Calendar-first
  **Operations** after **Opportunities** when Calendar is enabled, producing
  five persistent primary destinations: **Now**, **Opportunities**,
  **Operations**, **Clients**, and role-gated **Library**. The duplicate
  Operations header menu is removed. Workspace & tools retains the concise
  daily-execution group **Operations**, **Clear the Deck**, and **Staff** only
  while Calendar is available; **Workflow**, **Messages**, and **Pilot** remain
  Frequent tools; independently gated **Reporting Dashboard** plus
  **Integrations Ops**, **Import Studio**, and **Session Diagnostics** remain
  progressively disclosed Administration tools. Event and Event Schedule
  remain contextual/direct routes, and `/app/schedule` retains compatibility
  with Calendar when enabled. Exact-head CI, hosted role-aware reachability,
  and human acceptance remain separate; source and local evidence do not
  establish deployment or production readiness.

### UX convergence surface classification (local source candidate)

| Surface | Classification | Preserved authority and route |
|---|---|---|
| Commercial Workbench | Canonical quote composition | `ProposalComposer` retains the one App-owned form, calculation, Menu, staffing, Preview, and explicit save/version path. |
| Guided | Alternate composition mode | Guided reads and edits the same draft and returns to Workbench without a second save or pricing authority. |
| Flag-off quote wizard and connected Quote Workspace | Compatibility / rollback | Existing role, quote identity, pricing, and save behavior remain available when Ambient or Workbench presentation gates are off. |
| Calendar-first Operations | Capability-gated canonical operational index candidate and fifth primary destination | When `eventSchedule` is enabled, `/app/operations` reuses `EventScheduleView` as a full-width Month calendar with context below, a seven-day Week time grid with a secondary detail rail, and a same-model phone agenda; `/app/schedule` is a compatibility path. When disabled, both routes and all Calendar handoffs fail closed without mounting the surface. |
| Event Focus | Contextual / direct deep link | Exact `/app/events/:quoteId` routes remain available without duplicating Events or Event Schedule in Operations navigation. |
| Workflow | Frequent global attention and exact contextual continuation | `/app/workflow` and exact Workflow focus remain authoritative outside Operations ownership. |
| Messages | Frequent aggregate review and exact opportunity/client continuation | `/app/messaging` and exact conversation handoffs retain the existing message authority outside Operations ownership. |
| Reporting and system administration | Progressive Administration | Reporting, Integrations, Import, and Diagnostics retain their existing routes and role/feature gates without becoming daily navigation; the Operations Reporting continuation follows `reportingDashboard` independently of Calendar. |
| Legacy Operations switchboard | Superseded and removed from the Ambient graph | `ambientLegacyRetirement` records the Calendar-first replacement, zero-consumer retirement condition, and preserved live-route disposition. No capability or route was deleted. |

## Production email sender identity (September 3, 2026)

- **Exact provider identity:** the default-off Resend activation path accepts
  only `QuotePilot by MBMApps <quotepilot@quietpilot.us>`, matching the
  provider-verified `quietpilot.us` domain. The Functions runtime, environment
  materializer, and release-candidate policy share that allowlist; a stale
  LeaguePilot identity or arbitrary sender fails closed. This is source and
  provider-domain metadata evidence, not deployment, delivery, inbox receipt,
  or human acceptance.

## Production Resend acceptance test (September 3, 2026)

- **Bounded operator surface:** a verified platform administrator can use
  Integrations Ops to send one server-authored acceptance message to an exact,
  explicitly confirmed `quietpilot.us` recipient. Organization administrators
  who are not platform administrators never receive the control.
- **One-call authority:** the server accepts only `requestId`, `recipientEmail`,
  and the exact confirmation token; it persists the private request before
  contacting Resend and reuses a stable provider idempotency key. An existing
  request ID is never sent again, and an ambiguous outcome stays locked.
- **Evidence boundary:** the direct result exposes the Resend provider message
  ID and acceptance timestamp. Operations Audit retains a bounded receipt row
  without exposing the provider message ID. Exact v0.16.5 production evidence
  now records one controlled non-customer request: QuotePilot returned
  `provider_accepted`, the control locked, one audit row appeared, and
  independent Resend readback reported `delivered`. That evidence does not
  prove recipient inbox arrival, sender/subject/body review, ordinary customer-
  email readiness, spam placement, or human acceptance.

## Library readiness and Menu Builder (September 4, 2026)

- **Setup readiness:** `src/lib/businessReadiness.js` continues to project
  business-to-quote, catalog-draft-to-publish, quote-draft-to-save,
  proposal-to-send, margin evidence, and provider-connection readiness
  independently. The standalone Library presentation suppresses absent quote
  and proposal context, expands unresolved setup, and compresses healthy
  evidence. Cost gaps and provider state do not block ordinary quote creation.
- **Role-safe Library:** `src/components/BusinessSetupCenter.jsx` is the
  secondary readiness rail for admins and sales staff. Administrators receive
  one ranked unresolved action; sales sees the same business outcomes plus one
  administrator-managed boundary and no edit capability.
- **Menu Builder:** `src/components/AdminCatalogModal.jsx` uses Event type,
  Menu section, Item, and Price basis language in a responsive workbench. Its
  visually distinct context rail progressively discloses structural maintenance;
  its compact item list opens one focused editor; and bulk operations appear
  only after selection. All edits retain the private catalog setup draft and
  stable-ID authority. Setup presets and `src/components/ImportStudioModal.jsx`
  use that same staged authority.
- **Cost truth:** managed menu `costMinor` now round-trips through catalog reads,
  import preview, server publication, readiness coverage, authoritative pricing
  context, and the staff-only margin model. Customer proposal, export, and
  portal projections remain cost-free.
- **Evidence boundary:** focused unit/component/server tests and a local build
  qualify source behavior. Local component and Playwright checks cover the
  context/item/editor hierarchy, focused item switching, progressive bulk
  actions, keyboard focus, axe, overflow containment at 390, 768, and 1440
  pixels, and staged-menu isolation. Hosted readback and human acceptance remain
  separate gates.

## Revision-safe quote review (August 31, 2026)

- **Persisted authority:** trusted quote creation and edits copy the pricing
  engine's exact `pricingCatalogAuthority` to the canonical quote and immutable
  version. The customer portal projection explicitly omits it.
- **Five review states:** `functions/quoteCatalogRevisionReview.js` returns
  `current`, `newer_catalog_no_selected_impact`, `review_required`,
  `legacy_unknown`, or `unavailable` from same-tenant server evidence. It
  compares selected item identity/presentation, availability, price basis,
  price, staffing rate/policy, and rental quantity rules.
- **No silent reconciliation:** the edit runtime preserves saved missing and
  inactive selections. `src/components/QuoteCatalogRevisionReviewPanel.jsx`
  presents quoted/current differences and exact rate provenance before save.
- **Explicit outcomes:** Keep quoted values records a private immutable receipt
  tied to the active quote version and current catalog revision and freezes
  commercial inputs. Review and update records its own fence, stages current
  catalog rates, then uses the existing authoritative Change Impact simulation,
  authorization, and version-write path. Terminal quotes remain immutable.
- **Evidence boundary:** local server/model/component tests cover the old
  `$24/$32` versus `$48/$62` scenario, legacy authority, unrelated revisions,
  inactive/missing records, terminal status, and receipt fences. Hosted and
  authenticated human acceptance remain unclaimed.

## Unified commercial consequence review (August 31, 2026)

- **One authority:** `src/lib/unifiedCommercialConsequenceReview.js` and
  `src/components/UnifiedCommercialConsequenceReview.jsx` extend the existing
  server-authoritative Commercial Change Impact flow; they do not introduce a
  second pricing or quote-write path.
- **Six grouped consequences:** Price and deposit, Staffing, Rentals, Guided
  recommendations, Margin evidence, and Proposal readiness remain visibly
  separate and name their authoritative or advisory source.
- **Three bounded outcomes:** Apply all and Apply selected build an exact
  proposed form and rerun authoritative simulation before the existing governed
  version action can run. Keep quoted plan restores the loaded form and discards
  the unsaved proposal without writing the quote.
- **Revision fences:** quote revision, proposed-form identity, and catalog
  revision must still match. Any change invalidates the review and disables its
  apply actions until a fresh simulation succeeds.
- **Evidence boundary:** focused model/component/integration tests and a local
  build qualify source behavior only; hosted callable behavior and authenticated
  operator acceptance remain separate gates.

The chronology below records first-source and release-containment evidence.
Its `v0.7.0` row is a historical deployment receipt, not a current-runtime
claim. Current production is the exact deployed `v0.15.0` artifact; recheck
`PROJECT_STATUS.md` before using that fact operationally.

## Status Legend
- `Implemented`: shipped and wired in the app.
- `Implemented (source)`: present in the current source with local
  implementation evidence, but not yet deployed or production-accepted;
  release-gate status remains in `PROJECT_STATUS.md`.
- `Implemented (deployed)`: present in the verified production revision; any
  remaining hosted or human-acceptance boundary is stated in the row and in
  `PROJECT_STATUS.md`.
- `Partial`: present but not complete against all desired behaviors.
- `Optional`: optional capability tracked but not required for core flow.

## Master Checklist Mapping

**Catalog setup draft and publication authority:** implemented in the local
source candidate. Administrator edits can be coalesced into one private,
server-backed organization draft without changing the live catalog. Review is
read-only; publication validates generation, base revision, record baselines,
dependencies, and a 400-record bound in one transaction, then advances one
revision, records one attributed pricing confirmation and immutable receipt,
and closes the draft. Browser access to draft and receipt records is denied.
Hosted, production-data, and human acceptance remain unclaimed.

| # | Feature Area | Status | Primary Evidence |
|---|---|---|---|
| 1 | Data + architecture collections/model | Implemented | `src/lib/organizationService.js`, `src/context/OrganizationContext.jsx`, `firestore.rules`, `functions/index.js`, `scripts/seed-firestore-menu.mjs`, `scripts/migrate-to-multi-tenant.mjs` |
| 2 | Dynamic menu system (Firestore + event filtering + grouping + active items) | Implemented | `src/hooks/useCatalogData.js` (changed-record transactions + conflict detection), `src/lib/menuService.js`, `src/components/WizardSteps.jsx` |
| 3 | Pricing engine (`per_item`, `per_person`, `per_event`, selected package inclusions without double charge, server-owned actor/time, and confirmed-catalog authority fencing) | Implemented (deployed; catalog changes abort with recalculate/retry rather than committing stale authority) | `src/lib/quoteCalculator.js`, `functions/pricingEngine.js` (confirmed-setup gate, authoritative inclusion refs, settings fingerprint, and pre/post-read fence), `functions/index.js` (trusted boundary rechecks), `src/lib/pricingContracts.js`, `src/lib/commerceOps.js`, `src/components/LiveBreakdown.jsx`, `src/lib/__tests__/pricingEngine.authoritative.test.js`, `e2e/firebase-authoritative-pricing.smoke.spec.js` |
| 4 | 5-step quote builder wizard with scoped catalog-read recovery that keeps safe outlining available, names incomplete catalog-dependent choices, confirms no save/reprice occurred, and suppresses raw provider text | Implemented; the catalog-recovery presentation is source/local evidence with 390/768/1440 blocked-state real-route proof and component contracts for the continue-outlining state, and is not yet deployed or human accepted | `src/lib/wizardUi.js` (`WIZARD_STEP_DEFINITIONS`), `src/components/WizardSteps.jsx`, `src/App.jsx`, `src/LegacyApp.jsx`, `src/components/CatalogReadNotice.jsx`, `src/components/__tests__/catalogReadNotice.test.jsx`, `src/components/__tests__/wizardVisualSnapshots.test.jsx` |
| 5 | Live sticky summary panel with real-time totals | Implemented (deployed; authenticated mobile human acceptance pending) | `src/components/LiveBreakdown.jsx`, `src/App.jsx` (`MobilePricingSummary`), `src/styles.css` (`.breakdown-panel`, `.mobile-pricing-summary`), `e2e/quote-wizard.smoke.spec.js` |
| 6 | Quote system (trusted create/duplicate/edit, snapshots, statuses) | Implemented (deployed; authenticated production-data acceptance pending) | `functions/index.js` (`createQuoteDraft`, `duplicateQuoteDraft`, `updateQuoteDraft`), `functions/quoteCreation.js`, `src/lib/quoteStore.js` (`submitQuote`, `duplicateQuote`, `updateQuote`), `src/components/QuoteHistoryModal.jsx` |
| 7 | Quote history + versioning | Implemented | `src/lib/quoteStore.js` (`saveQuoteVersion`, `getQuoteHistory`), `firestore.rules` (`organizations/{orgId}/quotes/{quoteId}/versions/{versionId}`) |
| 8 | Quote management UI (list, sort, filters, exact draft-save handoff, role-safe actions, revision-bound email delivery, evidence-gated portal sharing and conversation with bounded same-session unresolved-request restoration, isolated Stripe deposit and final-balance request/reconciliation rails, provider-owned payment status) | Implemented in source candidate (prior quote-management baseline deployed; configured-action v2 compiles both commercial state and the surface's real provider, approval, rebook, conversation, in-flight, and recovery readiness before ranking one primary action. The rail fails closed for unmanaged actions and enforces compiled visibility and disabled state; browser-local alternate drafts use a nested schema projection and create their own tested `v0001`. Hosted and human acceptance remain pending.) | `src/components/QuoteHistoryModal.jsx`, `src/components/ConfiguredQuoteActionRail.jsx`, `src/lib/quoteActionState.js`, `src/lib/quoteHistoryController.js`, `src/lib/__tests__/quoteActionState.test.js`, `src/lib/__tests__/quoteStore.versioning.test.js`, `src/components/__tests__/configuredQuoteActionRail.test.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/lib/quoteStore.js` (`duplicateQuote`, `reopenQuote`, `hardDeleteQuote`), `src/lib/portalConversationClient.js`, `src/lib/commerceOps.js`, `src/lib/proposalExport.js`, `functions/quoteDelivery.js`, `functions/portalConversation.js`, `functions/paymentApprovalScope.js`, `functions/paymentDispatchState.js`, `functions/paymentSafety.js`, `functions/paymentLedger.js`, `functions/finalBalancePayment.js`, `functions/stripeProviderState.js`, `functions/index.js` (`getQuotePortalConversation`, `sendQuotePortalConversationMessage`, `sendQuoteToCustomer`, `resolveQuoteDeliveryOutcome`, `sendPaymentRequestEmail`, `sendFinalBalanceRequestEmail`, `reconcileDepositCheckout`, `reconcileFinalBalanceCheckout`, `stripeWebhook`, disabled direct `createDepositCheckout`, `rotateQuotePortalKey`, `reopenQuote`, `hardDeleteQuote`; the legacy bulk purge callable fails closed), `firestore.rules` (`privatePaymentDispatches` and quote conversation records are server-only) |
| 9 | Admin panel tabbed UX + hierarchical menu management | Implemented | `src/components/AdminCatalogModal.jsx`, `src/styles.css` (`.admin-tabs`) |
| 10 | Inline editing with blur/enter persistence | Implemented | `src/components/AdminCatalogModal.jsx` (`handleManagedMenuItemBlur`, `handleManagedMenuItemKeyDown`) |
| 11 | Booking lifecycle (availability checks, approval-bound contract conversion with receipt-safe uncertainty/reconciliation/recovery states, customer-linked immutable source version, confirmations, staff assignments) | Implemented (deployed, including mutation-state UI and customer-linked immutable-version projection; hosted acceptance pending) | `functions/contractWorkflow.js`, `functions/quoteCreation.js` (`projectCustomerIdentityToImmutableVersion`), `functions/index.js` (`convertQuoteToContract`), `src/lib/quoteStore.js` (`checkEventAvailability`, `convertQuoteToContract`, `updateQuoteBookingConfirmation`, `updateQuoteBookingAssignment`), `src/components/EventScheduleModal.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/__tests__/quoteHistoryContractConversion.test.jsx`, `scripts/provisioning-emulator-acceptance.mjs` |
| 12 | Customer decision center (tenant theme presets + snapshotted portal branding + scope/pricing + evidence-bound quote conversation + neutral-by-default intentional response selection + callable-owned electronic acceptance receipt + change request/decline + evidence-bound token lifecycle + CWF-10 exact-attempt submitting/uncertain/reconciliation/receipt/stale/error/recovery presentation, focus, and reduced-motion behavior) | Implemented in source; the previously deployed decision authority remains intact, while neutral-by-default presentation is local/source evidence and hosted customer acceptance is pending | `src/data/portalThemePresets.js`, `src/components/AdminCatalogModal.jsx`, `src/components/CustomerPortalView.jsx`, `src/components/LegacyCustomerPortalView.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/lib/quoteStore.js` (`getPortalQuote`, `updatePortalDecision`, `rotateQuotePortalKey`), `src/lib/portalConversationClient.js`, `functions/quoteCreation.js`, `functions/proposalAcceptance.js`, `functions/quoteDelivery.js`, `functions/portalConversation.js`, `functions/index.js` (`getQuotePortalConversation`, `sendQuotePortalConversationMessage`, `acceptQuoteProposal`), `firestore.rules` (`customerPortalQuotes`, `portalConversationMessages`, `proposalAcceptanceReceipts`), `src/components/__tests__/customerPortalDecisionInteraction.test.jsx`, `e2e/quote-wizard.smoke.spec.js` |
| 13 | Admin-only provider operations + scoped integration audit logging | Implemented (deployed; individual provider activation and acceptance pending where noted) | `src/components/IntegrationOpsModal.jsx`, `src/lib/quoteStore.js` (`recordQuoteIntegrationSync`; browser CRM sends fail closed), `src/lib/commerceOps.js`, `functions/index.js` (admin-only provider callables) |
| 14 | Reporting dashboard (pipeline, conversion, accepted/booked quote value, and Firebase-source/provider-timestamp-verified paid-deposit totals; CWF-09 adds a 500-record same-tenant cap, source/freshness/truncation, explicit denominators, missing-money exclusion, stale/partial/error/retry, and UTC six-month trends; neither money family is accounting revenue). Exact `v0.15.0` additionally exposes privacy-bounded client-observed Ambient dead-click, exact server-authoritative Firebase saved priced-draft, and same-session exact-category issue-resolution measures through the existing product-event summary. | Deployed for the bounded commercial evidence rail, payment qualification, and Ambient metric extension; authenticated hosted and human acceptance remain pending. The extension excludes quote/customer identifiers and free text, shows no rate or duration from local fallback or zero samples, and is client observation rather than server timing telemetry. | `src/components/ReportingDashboardModal.jsx`, `src/lib/quoteStore.js` (`getQuoteHistory`), `src/lib/productAnalytics.js`, `functions/productAnalytics.js`, `src/components/__tests__/reportingDashboardEvidence.test.jsx`, `src/components/__tests__/operationalWorkspaceViews.test.jsx`, product-analytics client/server tests |
| 15 | Event schedule board (month/week, conflicts, assignments, production checklist, and separately labeled quote/booking-confirmation states) | Implemented (deployed; hosted operator acceptance pending) | `src/components/EventScheduleModal.jsx`, `src/lib/statusSemantics.js`, `src/lib/quoteStore.js` (`getQuoteHistory`, `updateQuoteBookingAssignment`, `updateQuoteProductionChecklist`) |
| 16 | Privacy-bounded session diagnostics (runtime route/error capture + staff export/clear tools) | Implemented (source/local; diagnostic records are local browser evidence only and do not establish hosted, provider, production, or human acceptance) | `src/lib/sessionDiagnostics.js`, `src/components/DiagnosticsModal.jsx`, `src/main.jsx`, `src/App.jsx`, `src/lib/__tests__/sessionDiagnostics.test.js` |
| 17 | Global event + organization context across surfaces | Implemented | `src/context/EventTypeContext.jsx`, `src/context/OrganizationContext.jsx`, `src/main.jsx`, `src/App.jsx`, `src/components/AdminCatalogModal.jsx` |
| 18 | Verified-email auth, email/password recovery with same-confirmation UI and QuotePilot return URL, deliberate one-time verification confirmation, org bootstrap, explicit organization-owner authority, and role-gated controls | Implemented (deployed code/rules for the existing auth surface; the explicit owner-binding and browser role-write denial plus the QuotePilot-owned verification handler are source-only, while staging handler binding, exact hosted Auth behavior, and human acceptance remain pending). Newly provisioned owner invitations carry a dedicated purpose; exact-email verified activation atomically binds the organization owner and writes a browser-private receipt. Generic legacy admin/sales invitations remain role-only, and browsers cannot mint or rewrite role documents. The return policy permits only the canonical production host, the fixed isolated staging return host used for governed candidate acceptance, and loopback development; arbitrary previews and lookalikes fail closed. The custom `/app/auth/action` surface validates the exact local Firebase project, `verifyEmail` mode, one-time-code presence, and approved continuation, removes the code from the visible URL, requires a person to choose **Verify email**, sends the code only to Firebase's verify-email endpoint, requires a verified provider receipt, and preserves submitting, uncertain, reconciliation, receipt, error, and recovery as different outcomes. | `src/lib/authClient.js`, `src/lib/firebaseEmailActionPolicy.js`, `src/lib/firebaseEmailAction.js`, `src/hooks/useAuthSession.js`, `src/components/AuthGate.jsx`, `src/components/FirebaseEmailActionPage.jsx`, `src/components/WorkspaceRoute.jsx`, `src/main.jsx`, `src/lib/__tests__/firebaseEmailAction.test.js`, `src/components/__tests__/firebaseEmailActionPage.test.jsx`, `e2e/firebase-email-action.smoke.spec.js`, `e2e/firebase-auth-rules.smoke.spec.js`, `functions/index.js` (`ensureOrganizationBootstrap`, `provisionCustomerOrder`), `functions/organizationAuthority.js`, `firestore.rules`, `scripts/provisioning-emulator-acceptance.mjs` |
| 19 | Optional guided selling recommendations + rules | Implemented / Optional | `src/lib/recommendations.js`, `src/components/AdminCatalogModal.jsx`, `src/data/mockCatalog.js` |
| 20 | Good/Better/Best scenario compare workflow | Implemented / Optional | `src/lib/quoteWorkflow.js` (`buildQuoteScenarios`), `src/components/QuoteCompareModal.jsx`, `src/App.jsx` |
| 21 | Bounded cross-surface security audit beyond role gating: immutable final quote-approval execution and organization-role-change receipts, same-tenant admin projection, replay/foreign-tenant defense, privacy-safe fields, source bounds, and explicit indefinite server retention with no browser export/clear workflow | Implemented in source candidate; delivery reconciliation and catalog confirmation remain labeled legacy server observations rather than immutable receipts, provider outcomes remain separate, and deployment plus authenticated hosted role/tenant and human acceptance are pending | `functions/operationsAudit.js`, `functions/organizationRoleAuthority.js`, `functions/index.js` (`getOperationsAuditSnapshot`), `firestore.rules`, `src/lib/operationsAuditPresentation.js`, `src/components/IntegrationOpsModal.jsx`, `src/lib/__tests__/operationsAudit.server.test.js`, `src/lib/__tests__/operationsAuditPresentation.test.js`, `docs/capability-surfacing-contracts.json` |
| 22 | QA acceptance tests and checks, including independent lazy-surface recovery, embedded-route semantics, and focus-contained contextual/legacy dialogs | Implemented (core) | Unit tests under `src/lib/__tests__/`, UI snapshots and recovery contracts under `src/components/__tests__/`, Firestore rules tests under `src/rules/__tests__/`, Playwright smoke and real chunk-failure accessibility lanes under `e2e/`, `scripts/provisioning-emulator-acceptance.mjs`, and CI scripts in `package.json` |
| 23 | Sales workflow (attention queue/count, readiness, follow-ups, request-ID-bound current change handling, lifecycle, approval queue and exact action execution, plus a resolved empty state that returns directly to first-quote intake) | Implemented; the deployed workflow authority is unchanged, while the first-quote empty-state handoff is local/source evidence and hosted acceptance is pending | `src/App.jsx`, `src/LegacyApp.jsx`, `src/lib/quoteWorkflow.js`, `src/components/SalesWorkflowModal.jsx`, `src/components/LegacySalesWorkflowModal.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/lib/quoteStore.js`, `functions/approvalWorkflow.js`, `functions/contractWorkflow.js`, `functions/index.js` (approval and governed-action callables), `firestore.rules`, `e2e/quote-wizard.smoke.spec.js` |
| 24 | Tenant Import Workbench: customer and catalog CSV plus searchable-PDF catalog extraction; every inferred row reachable through paged exact-value/provenance review; explicit mapping, relationship, and exclusion resolution; exact server or shared-draft preflight; stable transaction-safe customer sessions; receipt-bounded partial resume/undo; awaitable catalog refresh/retry; exact catalog-save projection; durable staged/published reconciliation; and catalog draft or active-catalog review | Source/local candidate implemented. Customer preflight has a 15-minute issue window and the first accepted child activates a 24-hour exact-session continuation. Catalog mutation receipts bind the exact tenant, actor, request, revision/generation fences, and normalized patches; an exact replay reports Staged only while those patches remain in the same open draft, or Published only when publication lineage and currently active fields both match. Catalog recovery retains the same batch through completed refresh or visible read failure and permits explicit release only for a definitive no-write abort. The earlier customer/catalog CSV callables are deployed, while this searchable-PDF, preflight/session, field-state, shared-draft, durable-reconciliation, and recovery expansion is not yet promoted or authenticated-hosted-smoked; source and local automated evidence do not establish hosted, production, assistive-technology, or human acceptance. | `src/components/ImportStudioModal.jsx`, `src/hooks/useCatalogData.js`, `src/lib/importWorkbenchModel.js`, `src/lib/pdfImport.js`, `src/lib/importBatchService.js`, `src/lib/catalogSetupDraftService.js`, `functions/index.js` (`preflightCustomerImportBatch`, `createCustomerImportBatch`, `rollbackCustomerImportBatch`, catalog setup-draft callables), `functions/catalogSetupDrafts.js`, `src/components/__tests__/importWorkbenchFlow.test.jsx`, `src/components/__tests__/importStudioPresentation.test.jsx`, `src/hooks/__tests__/useCatalogData.observation.test.jsx`, `src/lib/__tests__/importWorkbenchModel.test.js`, `src/lib/__tests__/pdfImport.test.js`, `src/lib/__tests__/catalogSetupDraftImport.test.js`, `src/lib/__tests__/catalogSetupDrafts.server.test.js`, `src/lib/__tests__/customerImport.emulator.test.js`, `e2e/import-workbench-pdf.spec.js` |
| 25 | Platform tenant provisioning (verified owner, neutral defaults, atomic create, explicit entitlements, repair, cleanup) | Implemented (deployed; owner acceptance pending) | `functions/index.js` (`preflightCustomerOrder`, `provisionCustomerOrder`, `repairCustomerProvisioningOrder`, cleanup callables), `src/components/IntegrationOpsModal.jsx`, `scripts/provisioning-emulator-acceptance.mjs` |
| 26 | Public $1 invoice-first buyer onboarding on `tonicatering` (Turnstile, pre-identity HMAC-keyed create reservation and status abuse controls, request-scoped exact retry plus signed or audited operator provider-void replacement, Stripe API `2024-06-20` Hosted Invoice Page and signed invoice lifecycle, terminal unpaid Invoice recovery, paid workspace preparation, pending invite, optional provider-accepted activation instructions, exact verified-email user activation, quote-rail isolation) | Implemented (deployed but intentionally dormant with `BUYER_ACCESS_ENABLED=false`; dedicated test-mode Stripe credential, provider/hosted acceptance, and live launch pending) | `src/components/BuyerAccessPage.jsx`, `src/lib/buyerAccess.js`, `functions/buyerAccess.js`, `functions/index.js` (`repairBuyerAccessInvoice`, `createBuyerAccessInvoice`, `getBuyerAccessInvoiceStatus`, `buyerAccessStripeWebhook`), `src/components/IntegrationOpsModal.jsx`, Firebase Secret Manager bindings, `firestore.rules` (`buyerAccessOrders`), `e2e/buyer-access.spec.js`, `scripts/provisioning-emulator-acceptance.mjs` |
| 27 | Staff Kitchen BEO content and legacy download-time provenance (timing, staffing, checkpoints, selections, checklist, source revision, declared-input digest, contacts, allergens, sign-off, notes) | Implemented (deployed foundation; the browser-only fingerprint remains provenance rather than retained freshness, while row 44 owns the newer trusted generation/receipt authority; hosted operator acceptance pending) | `src/components/QuoteHistoryModal.jsx`, `src/lib/beoPayload.js`, `src/lib/beoArtifactFingerprint.js`, `src/lib/beoExport.js`, `src/lib/quoteWorkflow.js`, focused BEO tests |
| 28 | Trusted customer projection from quote create/edit (organization scoped, duplicate-safe, import-preserving, browser-forgery denied) | Implemented (deployed Functions/rules; hosted production-data acceptance pending) | `functions/index.js`, `functions/quoteCreation.js`, `firestore.rules` (`customers`), `src/lib/__tests__/quoteCreation.server.test.js`, `src/rules/__tests__/firestore.rules.test.js` |
| 29 | Temporary-flagged customer-centered staff shell and Commercial Command Center: dependency-free `/app` route parsing/navigation, Home default, sticky in-memory quote builder, direct/deep routes, canonicalizing `/app/clients` list/detail aliases for the visible Clients language, customer-name-to-360 links, exact quote/payment and Workflow focus, authenticated 404, portal-query precedence, one generation-guarded Home/header snapshot over existing read contracts/data sources, a Home-scoped tenant/read/source/last-complete-read evidence rail with incomplete/stale/truncation truth, human-readable first-release staff formatting, primary-route heading focus, route-versus-modal return language, shared semantic status chips, and staff-scoped neutral chrome. The v0.16 source candidate applies Calm Four: Now, Opportunities, Clients, and role-safe Library are the only persistent primary destinations; New quote remains an action; Search, Operations, workspace identity, account settings, and sign-out remain secondary. The focus-contained mobile Workspace & tools sheet keeps those utilities reachable without another bottom-navigation item; account settings exposes password reset only through an explicit action. The one-principal/one-organization model does not imply same-account workspace switching, and private history metadata preserves exact Back/Forward entries while an unsaved contextual draft can guard then replay the requested traversal. | Implemented with separate evidence levels (the prior customer-centered shell is deployed; Calm Four, account settings, the mobile utility sheet, and guarded history behavior are exact frozen local v0.16 candidate changes. Integrated Phase 1 local browser, build, and governance evidence has passed; candidate deployment, authenticated hosted recovery, production-data exercise, and founder acceptance remain unclaimed) | `src/lib/workspaceRoutes.js`, `src/lib/workspacePresentation.js`, `src/lib/authClient.js`, `src/hooks/useBrowserLocation.js`, `src/hooks/useWorkspaceRouteHeadingFocus.js`, `src/context/WorkspaceNavigationContext.jsx`, `src/components/WorkspaceRoute.jsx`, `src/components/WorkspaceShell.jsx`, `src/hooks/useCommercialWorkspaceSnapshot.js`, `src/components/CommandCenterHome.jsx`, `src/components/AmbientNowView.jsx`, `src/components/AmbientOpportunitiesStream.jsx`, `src/components/StaffEvidenceRail.jsx`, `src/components/StatusChip.jsx`, `src/components/WorkspaceNotFound.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/lib/statusSemantics.js`, `src/App.jsx`, `src/styles.css`, focused component/context/hook tests, and `e2e/v16-calm-four-acceptance.spec.js` |
| 30 | Stable same-tenant customer identity and Internal Customer 360: generated server-owned `customerId` on trusted new quote/version writes, private server-only normalized-email ownership claims, collision-safe edits, normalized customer search, rules-bounded paginated directory, surface-scoped source/freshness/bounds rails, a bounded relationship briefing, recorded commercial timeline, advisory immutable-version comparison, derived Overview/Quotes & Proposals/Events/Money/Conversations, per-quote conversation summaries, portal-safe staff preview, emulator-only legacy binding apply, canonical quote staff-only reads, and retired browser customer-role self-bootstrap. The current Clients presentation keeps the sparse start state, then presents a populated directory without dashboard metrics and an internal relationship ledger led by identity, one supported next step, the existing Client → Opportunity → Proposal → Event evidence, the living opportunity, and de-duplicated recorded history. Conversations, source detail, additional opportunities, and the existing detailed record remain progressively disclosed. | Implemented (deployed same-tenant authority with a locally qualified presentation convergence; existing Client 360 correction, role, opaque-ID, tenant, source, action, and evidence boundaries remain authoritative, and no derived memory becomes immutable fact. Exact 390/768/1440 Chromium coverage passes route, focus, no-write, responsive, target-size, layout-containment, and scoped axe checks; the selected reference and exact implementation were inspected together at a local 9.5/10. Timeline still omits provider delivery/bounce receipts not exposed by the current DTO; production normalization/backfill, hosted staff acceptance, and persistent customer accounts remain excluded. Under explicit local auth bypass, an explicitly seeded tenant quote directory owns its exact populated or empty acceptance state; only an absent directory falls back to the 24-record August-through-October `local_fixture`. Both remain development-only, write nothing to Firebase or a provider, and prove no production client, engagement, booking, payment, or delivery state) | `functions/quoteCreation.js`, `functions/index.js`, `src/lib/customerWorkspace.js`, `src/lib/localCustomerDirectoryFixture.js`, `src/lib/customerCommercialTimeline.js`, `src/lib/quoteVersionComparison.js`, `src/components/CustomerDirectoryView.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/AmbientClientsView.jsx`, `src/components/CustomerCommercialTimeline.jsx`, `src/components/QuoteVersionComparison.jsx`, `src/components/StaffEvidenceRail.jsx`, `src/components/__tests__/ambientClientsView.test.jsx`, `e2e/customer-centered-workspace.spec.js`, `e2e/ambient-clients.spec.js`, `e2e/staff-app-return-context.spec.js`, `e2e/v16-calm-four-acceptance.spec.js`, `scripts/customer-id-backfill-plan.mjs`, `scripts/backfill-customer-ids.mjs`, `firestore.rules`, focused component/lib tests, and rules/emulator coverage |
| 31 | Routed operational workspaces for Schedule, Reporting, Catalog, Imports, Integrations, and Diagnostics with recoverable lazy loading, sticky route state, embedded-region focus, retained role/feature gates, and compatible contextual/legacy modal wrappers | Implemented (deployed with the production workspace flag enabled; authenticated hosted acceptance pending) | `src/App.jsx`, `src/components/EventScheduleModal.jsx` (`EventScheduleView`), `src/components/ReportingDashboardModal.jsx` (`ReportingDashboardView`), `src/components/AdminCatalogModal.jsx` (`AdminCatalogView`), `src/components/ImportStudioModal.jsx` (`ImportStudioView`), `src/components/IntegrationOpsModal.jsx` (`IntegrationOpsView`), `src/components/DiagnosticsModal.jsx` (`DiagnosticsView`), `src/components/__tests__/operationalWorkspaceViews.test.jsx`, `src/components/__tests__/adminCatalogStarterChoice.test.jsx`, `src/components/__tests__/importStudioPresentation.test.jsx`, `src/components/__tests__/uiRecovery.test.jsx` |
| 32 | Mechanical no-orphan-capability delivery gate: fail-closed branch/PR/push authority-path review including deletions, same-upstream fallback, broad runtime-client authority discovery, chained/modular Firestore writes, exact ownership for directly changed/new/removed Firebase Function exports, shared-helper affected-export review across every classification, real frontend/manual/matrix locators, assertion-bearing tests, canonical per-state read/mutation markers, one narrow stale-read exception, and tested non-callable headless outcomes | Implemented (source; structural traceability only, not semantic/visual/hosted acceptance) | `scripts/check-capability-surfacing.mjs`, `docs/capability-surfacing-contracts.json`, `src/lib/__tests__/capabilitySurfacingGate.test.js`, `scripts/orchestration-lanes.sh` (`lane:core`), `package.json` (`check:capability-surfaces`) |
| 33 | Commercial Dependency Graph v1 pure foundation: frozen validated registry, deterministic downstream traversal, `qp-canonical-json-v1` browser/Node SHA-256 parity, schema-bound declared-input fingerprint, and exact source revision | Implemented (deployed foundation; the graph itself still performs no pricing, persistence, mutation, or freshness decision; rows 42-45 own the newer trusted authorities that consume it; hosted operator acceptance pending) | `src/lib/commercialDependencyGraphCore.cjs`, `functions/commercialDependencyGraphCore.cjs`, `src/lib/commercialDependencyGraph.js`, `src/lib/beoArtifactFingerprint.js`, `docs/COMMERCIAL_DEPENDENCY_GRAPH_ADR.md`, graph/parity/BEO tests |
| 34 | Bounded universal Workspace search and keyboard command palette: recoverably lazy `Ctrl`/`Command`+`K` staff entry, same-tenant customer-prefix and latest-50 quote reads, source/truncation/error/retry states, stale-generation protection, opaque navigation, transient query state, and portal/modal/role/feature guards | Implemented (deployed; broader proposal/event index, hosted keyboard/mobile acceptance, and temporary-flag removal pending) | `src/lib/commercialSearch.js`, `src/lib/commercialSearchShell.js`, `src/components/CommercialSearchPalette.jsx`, `src/App.jsx`, `src/lib/__tests__/commercialSearch.test.js`, `src/components/__tests__/commercialSearchPalette.test.jsx` |
| 35 | CWF-11 anniversary Attention and exact-version rebook draft: tenant-calendar Home/Workflow cues from the bounded latest-200 booked-quote read, explicit source/display incompleteness, one-click stable-customer handoff to Customer 360, booked/accepted immutable-source validation there, stable-customer continuity, deterministic request and draft identity, current-contact overlay, current-catalog authoritative repricing with catalog-authority fencing, uncertainty/reconciliation/recovery, trusted `updateQuoteDraft` staff-review completion evidence, and delivery blocking until a reviewed new event date is saved | Implemented (deployed; the central cue is read-only and does not itself verify acceptance or create a draft; no message, lead, acceptance, booking, payment, or revenue outcome; hosted staff acceptance pending) | `src/lib/anniversaryRebookingAttention.js`, `src/lib/customerRebookingRadar.js`, `src/lib/rebookQuoteClient.js`, `src/hooks/useCommercialWorkspaceSnapshot.js`, `src/components/CommandCenterHome.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/CustomerRevenueOpportunities.jsx`, `src/components/CustomerRebookDraftAction.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/App.jsx`, `functions/rebookQuoteDraft.js`, `functions/quoteCreation.js`, `functions/pricingEngine.js`, `functions/index.js` (`createRebookQuoteDraft`, `updateQuoteDraft`, delivery guard), `scripts/rebook-quote-emulator-acceptance.mjs`, `e2e/customer-centered-workspace.spec.js`, `src/lib/__tests__/anniversaryRebookingAttention.test.js`, `src/lib/__tests__/customerRebookingRadar.test.js`, `src/lib/__tests__/rebookQuoteClient.test.js`, `src/lib/__tests__/rebookQuoteDraft.server.test.js`, `src/lib/__tests__/quoteCreation.server.test.js`, `src/components/__tests__/commandCenterAttentionRouting.test.jsx`, `src/components/__tests__/customerRebookDraftAction.test.jsx` |
| 36 | CWF-13 Customer 360 bounded commercial measures: quoted, exact-state accepted/booked, Firebase-source-gated provider-confirmed deposit/final-balance, and repeat-event evidence with visible denominators, complete-versus-displayed-record scope, bounds, and loading/empty/partial/stale/error/retry presentation | Implemented (deployed; provider payment measures fail closed for browser-local, mixed, or unknown-source reads; derived read-only operational measures, not accounting revenue, cash reconciliation, forecasts, or a persisted rollup; hosted staff acceptance pending) | `src/lib/customerCommercialMeasures.js`, `src/components/CustomerCommercialMeasures.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/lib/__tests__/customerCommercialMeasures.test.js`, `src/components/__tests__/customerCommercialMeasuresPresentation.test.jsx` |
| 37 | CWF-12 Revenue Autopilot eligibility preview: one canonical quote from the bounded Workflow read, organization-configured IANA business time zone, deterministic quote-follow-up/deposit/final-balance/unread-reply evaluations, explicit safety gates and proof boundaries, and loading/empty/partial/stale/error states | Implemented (deployed read-only/non-sending foundation; row 46 owns the newer independently gated scheduler/job/provider/Attention authority; this preview itself makes no provider or recovered-revenue claim) | `src/lib/revenueAutopilotPreview.js`, `src/components/RevenueAutopilotPreviewPanel.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/AdminCatalogModal.jsx`, `src/data/mockCatalog.js`, `src/App.jsx`, `src/lib/__tests__/revenueAutopilotPreview.test.js`, `src/components/__tests__/revenueAutopilotPreviewPanel.test.jsx`, `src/components/__tests__/salesWorkflowRevenueAutopilot.test.jsx`, `src/lib/__tests__/catalogBusinessTimeZone.test.js` |
| 38 | CWF-15B read-only Commercial Change Impact preview: sub-slice 15B-b adds an expected active-version precondition, same-tenant saved canonical revision plus server-authoritatively repriced proposed-form snapshots, catalog-authority fencing, exact fact and total/deposit deltas, deterministic affected dependency nodes classified `REVIEW`/`STALE`, request-generation safety, and complete preview presentation states | Implemented (deployed simulation foundation; preview itself remains read-only, while rows 42-45 own newer default-off apply, generation, reconciliation, and Debt authority) | `functions/commercialChangeImpactPreview.js`, `functions/pricingEngine.js`, `functions/index.js` (`calculateQuotePricing` preview option), `src/lib/commerceOps.js`, `src/lib/commercialChangeImpact.js`, `src/components/CommercialChangeImpactPanel.jsx`, `src/App.jsx`, `e2e/firebase-authoritative-pricing.smoke.spec.js`, `src/lib/__tests__/commercialChangeImpactPreview.server.test.js`, `src/lib/__tests__/commercialChangeImpact.test.js`, `src/components/__tests__/commercialChangeImpactPanel.test.jsx` |
| 39 | CWF-07 Workflow timing cues and internal completion receipts: bounded timestamp-derived due-today, overdue, upcoming, and aging guidance plus exact stored internal completion evidence, with source/bounds and loading/empty/partial/stale/error/retry presentation | Implemented (deployed, read-only derivation; internal receipts are not customer contact, provider delivery, proposal resolution, payment, or booking proof; persisted ownership/SLA/escalation/handoffs and hosted acceptance pending) | `src/lib/workflowTimingCues.js`, `src/components/WorkflowTimingPanel.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/lib/__tests__/workflowTimingCues.test.js`, `src/components/__tests__/workflowTimingPanel.test.jsx` |
| 40 | CWF-08 bounded event run of show: Schedule derives an expandable accepted/booked event sequence from recorded event, staffing, checklist, and BEO inputs, labels timing sources and unknowns, and preserves independent acceptance, booking, and readiness semantics | Implemented (deployed, generated read-only view; no collaborative task, roster, resource, vendor, inventory, attendance, or portal-timing model; hosted operator acceptance pending) | `src/lib/eventRunOfShow.js`, `src/components/EventRunOfShowPanel.jsx`, `src/components/EventScheduleModal.jsx`, `src/lib/__tests__/eventRunOfShow.test.js`, `src/components/__tests__/eventRunOfShowPanel.test.jsx` |
| 41 | CWF-11 authoritative post-event closeout: eligible governed booking verifies the private acceptance snapshot and atomically creates one deterministic record bound to the exact accepted immutable version and stable customer, due seven tenant-calendar days after the event; legacy sources remain booked with a visible review block, and Customer 360 plus Workflow expose bounded scheduled/due/overdue/configuration-blocked/completed state, exact configuration recovery, and four explicit idempotent internal review/reopen receipts | Implemented (deployed; internal review evidence only, not an outbound thank-you/review request, provider delivery/open, customer reply, lead, booking, payment, or revenue outcome; hosted staff acceptance pending) | `functions/postEventCloseout.js`, `functions/index.js` (`convertQuoteToContract`, `recordPostEventCloseoutReview`, `refreshPostEventCloseoutConfiguration`), `firestore.rules`, `src/lib/postEventCloseoutClient.js`, `src/lib/customerRebookingRadar.js`, `src/lib/quoteWorkflow.js`, `src/components/PostEventCloseoutReviewAction.jsx`, `src/components/CustomerRevenueOpportunities.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/CommandCenterHome.jsx`, `scripts/provisioning-emulator-acceptance.mjs`, `src/lib/__tests__/postEventCloseout.server.test.js`, `src/lib/__tests__/postEventCloseoutClient.test.js`, `src/components/__tests__/postEventCloseoutReviewAction.test.jsx`, `src/rules/__tests__/firestore.rules.test.js` |
| 42 | Commercial Change Authority / QP-UXR-001 Governed Commercial Amendment Experience | Implemented on the `v0.18.0` release candidate with separate evidence levels. Eligible Quote Edit records now compose the exact current commitment, lifecycle requirement, reversible proposal, semantic difference, authoritative money delta, grouped business consequences, preserved-evidence boundary, required authority, atomic apply, named invalidation, exact uncertain-outcome reconciliation, focused `Changed / Preserved / Needs attention / Next` receipt, and explicit continuation to the resulting revision. Every connected save remains fenced to a current simulation, quote revision, proposed form, catalog authority, policy, role, and tenant. Dormant review does not claim persisted invalidations; both enforcement gates remain off unless separately promoted. The trusted editor still permits only draft/sent/viewed records; accepted, booked, paid, and other terminal evidence remains locked. Living Opportunity Quick Updates and guest-count preview keep their narrower contracts. Production deployment, connected production-data behavior, provider outcomes, and human acceptance are not established until the release receipt records them. | `functions/commercialChangeAuthority.js`, `functions/commercialChangeImpactEvaluator.js`, `functions/index.js` (existing Commercial Change callables and governed `updateQuoteDraft`), `src/lib/commercialChangeAuthorityClient.js`, `src/lib/commercialAmendmentContext.js`, `src/lib/quoteDraftRuntimeBase.js`, `src/components/CommercialAmendmentWorkspace.jsx`, `src/components/CommercialChangeImpactPanel.jsx`, `src/App.jsx`, `src/LegacyApp.jsx`, `src/components/__tests__/governedCommercialAmendment.test.jsx`, `src/lib/__tests__/commercialAmendmentContext.test.js`, `src/lib/__tests__/quoteDraftRuntime.test.js`, existing server/client/integration/emulator tests, `firestore.rules` |
| 43 | Commercial Dependency State and reconciliation | Implemented (deployed; bounded staff projection, exact named invalidation reconciliation, immutable receipt, and derived publication eligibility only—no automatic generation, publication, delivery, acceptance, booking, payment, or completion; hosted acceptance pending) | `functions/commercialChangeAuthority.js`, `functions/index.js` (`getCommercialDependencyState`, `reconcileCommercialDependencyState`), `src/lib/commercialChangeAuthorityClient.js`, `src/components/CommercialDependencyStatePanel.jsx`, `src/components/QuoteHistoryModal.jsx`, `firestore.rules`, focused server/client/component/integration tests |
| 44 | Trusted Kitchen BEO generation and artifact freshness | Implemented (deployed; canonical server generation, immutable actor/time/fingerprint/PDF receipt, strict retained-byte base64/length/SHA validation on replay, response, status, and download, exact current and prior-receipt download, five-state freshness, and qualifying Kitchen BEO invalidation reconciliation; hosted operator acceptance and publication evidence pending) | `functions/kitchenBeoAuthority.js`, `functions/kitchenBeoPdf.js`, `functions/index.js` (`getKitchenBeoArtifactStatus`, `generateKitchenBeo`, `downloadKitchenBeoReceipt`), `src/lib/kitchenBeoClient.js`, `src/components/KitchenBeoArtifactPanel.jsx`, `src/components/QuoteHistoryModal.jsx`, `firestore.rules`, focused server/PDF/client/component/integration tests |
| 45 | Decision Debt and policy controls | Implemented (deployed server authority with a source-qualified client hardening patch pending release; bounded server-derived non-predictive priority comes from persisted unresolved dependencies, tenant calendar, exact factors and source, plus versioned admin-only policy beside Workflow; exact-quote reads with a missing or invalid tenant IANA time zone now stop locally and direct setup to Library pricing instead of making a predictably rejected callable; missing authoritative commercial exposure produces explicit unknown factor/score/urgency rather than a guessed multiplier; non-admin staff are read-only, hosted acceptance is pending, and no accounting/recovered-revenue claim is made) | `functions/decisionDebt.js`, `functions/index.js` (`getDecisionDebtSnapshot`, `configureDecisionDebtPolicy`), `src/lib/decisionDebtClient.js`, `src/components/DecisionDebtPanel.jsx`, `src/components/QuoteDecisionDebtPanel.jsx`, `src/components/SalesWorkflowModal.jsx`, `firestore.rules`, focused server/client/component/integration tests |
| 46 | Revenue Autopilot operations, post-event review requests, and customer unsubscribe | Implemented (deployed but intentionally dormant; four visible runtime/tenant/send/provider gates; provider-independent deterministic preparation with bounded per-lane receipts; evidence-preserving stop/reconciliation behavior with visibly distinct withheld-versus-provider-accepted receipts; exact latest-message Attention supersession, staff-reply resolution, scheduled repair, and operations reads limited to currently open unread replies without narrowing job history; four email lanes, Customer 360 controls, public unsubscribe, and 15-minute scheduler foundations; runtime/sends remain off and no Resend provider, production-data, recovered-revenue, or human acceptance evidence is claimed) | `functions/revenueAutopilot.js`, `functions/revenueAutopilotAuthority.js`, `functions/revenueAutopilotTemplates.js`, `functions/index.js` (`getRevenueAutopilotOperations`, policy/control/materialization/ack/reconcile/unsubscribe callables, `sendQuotePortalConversationMessage`, `runRevenueAutopilotSchedule`, `revenueAutopilotResendWebhook`), `src/lib/revenueAutopilotClient.js`, `src/components/RevenueAutopilotOperations.jsx`, `src/components/RevenueAutopilotCustomerControls.jsx`, `src/components/RevenueAutopilotUnsubscribePage.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/CustomerWorkspaceView.jsx`, `firestore.rules`, focused server/client/component/conversation/schedule/webhook tests |
| 47 | CWF-16 Event Workspace: event-first quote detail with exact identity, bounded quote/Workflow condition and Needs You action, proposal-scoped readiness, stable `Why?` reason codes, explicit unavailable Flexibility/Alignment, existing context/action routes, selected sold scope, lifecycle, governed edit boundary, and responsive desktop/mobile presentation | Implemented (deployed in `v0.5.0`; one deterministic presentation selector reuses existing Workflow Attention and proposal-readiness facts; presentation/navigation only, no new backend/data authority or event-wide readiness, inventory/capacity/payment/booking/completion inference; public route reachability verified, authenticated hosted use and human acceptance pending) | `src/components/eventWorkspacePresentation.js`, `src/components/EventWorkspaceView.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/App.jsx`, `src/styles.css`, focused model/component/integration tests, `e2e/customer-centered-workspace.spec.js`, `docs/design/cwf-16-implementation/design-qa.md` |
| 48 | Event Messaging Station at `/app/messages`: temporary-flagged central staff inbox segregated by canonical quote/event, bounded latest-50 same-tenant quote listener ordered by body-free conversation summary, normalized event/thread presentation, fail-closed portal eligibility, search/filter/event grouping, exact selected staff or portal signal, callable-owned canonical-body reload, explicit live/cache/paused states, and a quote-scoped Ambient arrival that remains pending through duplicate in-flight body reads until the exact event heading is focused | Implemented (deployed in `v0.5.0` with the governed production workspace flag enabled; the exact-arrival/freshness/default-follow-up correction is source plus connected-emulator evidence only; `Live updates` is a listener-state claim, not guaranteed latency, delivery, read receipt, typing, or presence; direct message reads remain denied; public deep-link reachability verified, authenticated hosted use, production-data behavior, and human acceptance pending) | `src/lib/workspaceRoutes.js`, `src/lib/workspaceArrivalContract.js`, `src/lib/conversationInbox.js`, `src/lib/conversationSignalClient.js`, `src/lib/ambientConversationObject.js`, `src/hooks/useConversationInbox.js`, `src/components/QuoteHistoryModal.jsx`, `src/components/MessagingStation.jsx`, `src/components/QuoteConversationPanel.jsx`, `src/App.jsx`, `functions/index.js` (`sendQuotePortalConversationMessage` body-free exact-portal projection), focused route/inbox/component/server tests, `e2e/firebase-ambient-arrival.smoke.spec.js`, `docs/MESSAGING_STATION_ARCHITECTURE.md` |
| 49 | Structured change-request record: with the flag-gated client-request panel, staff can record which deterministically parsed proposals were staged from one exact customer change request through the callable-only `recordChangeRequestParse` boundary — server-validated against the exact stored request identity and message hash, staff-attributed, bound to the quote revision at record time, create-only and replay-stable, with browser writes denied by rules. A second callable, `linkChangeRequestResolutionVersion`, performs the record's only permitted follow-up mutation: fired best-effort right after a quote save that already fully succeeded on its own, it binds the record to the resulting version once — write-once, idempotent on replay, server-verified as newer than the version on file at record time | Implemented (both callables and their deny-only browser rules are deployed in `v0.7.0`; they are internal audit attestations only — they do not reply to the customer, mutate the quote, portal, or versions, or claim delivery, acceptance, booking, or payment; a missed link is a one-step-short audit trail, not a lost or corrupted one; authenticated hosted staff acceptance pending) | `functions/changeRequestRecord.js`, `functions/index.js` (`recordChangeRequestParse`, `linkChangeRequestResolutionVersion`), `firestore.rules`, `src/lib/changeRequestRecordClient.js`, `src/components/ChangeRequestPanel.jsx`, `src/App.jsx`, `src/lib/__tests__/changeRequestRecord.server.test.js`, `src/components/__tests__/changeRequestRecordAction.test.jsx`, `src/rules/__tests__/firestore.rules.test.js` |
| 50 | Pilot NOW and Event Room decision presentation: bounded Home evidence is restated as decision cards; quote detail adds proposal completeness, advisory decisions, and an evidence-bounded accepted/booked cascade receipt chain | Implemented (deployed in `v0.6.0` with the production build gates enabled; public route reachability verified and authenticated staff acceptance pending; presentation only, with no inferred delivery, payment, booking, or readiness) | `src/components/NowView.jsx`, `src/components/nowPresentation.js`, `src/components/EventWorkspaceView.jsx`, `src/components/decideStackPresentation.js`, `src/components/cascadePresentation.js`, focused component/selector tests, production-flag Playwright matrix |
| 51 | Deterministic CREATE and progressive guest certainty: browser-only intent extraction fills the ordinary draft after review, while approximate/ranged guest counts show a draft-only band priced through the existing client calculator | Implemented (deployed in `v0.6.0` with the production build gates enabled; authenticated staff acceptance pending; saved authority remains the exact recorded count and server pricing) | `src/components/CreateIntake.jsx`, `src/components/intentExtraction.js`, `src/components/pricingBand.js`, `src/components/LiveBreakdown.jsx`, focused tests, production-flag Playwright matrix |
| 52 | Pilot guided selling and command grammar: existing recommendations become decision cards and plain-language staff commands share the deterministic client-change grammar with preview-before-apply staging | Implemented (deployed in `v0.6.0` with the production build gates enabled; authenticated staff acceptance pending; staged draft edits only, normal save remains authority) | `src/components/WizardSteps.jsx`, `src/components/guidedSellingPresentation.js`, `src/components/PilotCommandBar.jsx`, `src/components/changeRequestParse.js`, focused tests, production-flag Playwright matrix |
| 53 | Fail-closed staff margin presentation from tenant-recorded package, item, rental, add-on, and applicable labor costs, with optional target policy | Implemented (deployed in `v0.6.0` with the production build gate enabled; authenticated staff acceptance pending; unavailable when any required input is absent, never estimated or customer-facing) | `src/components/marginPresentation.js`, `src/components/LiveBreakdown.jsx`, `src/components/__tests__/marginPresentation.test.js`, production-flag Playwright matrix |
| 54 | Catalog cost entry and margin advisory, and margin's spread across every other pricing surface built after `v0.6.0`: staff record optional tenant item/labor costs and a target margin % policy in Catalog Admin, blank always meaning not-recorded rather than $0; row 53's fail-closed presentation and a below-target commercial advisor card compute from these costs, and the same fail-closed `buildMarginPresentation` now also prices the CREATE band-pricing range, Scenario Compare's good/better/best cards and comparison table, and the change-request/command-bar impact preview — each independently unavailable until every line it covers has a recorded cost | Implemented (deployed in `v0.7.0` behind `VITE_PILOT_MARGINS_ENABLED`; costs are staff-only catalog data and never reach any customer-facing projection; Catalog Admin preserves explicit save reconciliation, revision-conflict, reload-failure recovery, hosted starter recovery, manual setup, import, and session-only workspace bypass paths; no new callable, export, or pricing authority; authenticated hosted staff acceptance pending) | `src/data/mockCatalog.js`, `src/hooks/useCatalogData.js`, `src/components/AdminCatalogModal.jsx`, `src/components/marginPresentation.js`, `src/components/LiveBreakdown.jsx`, `src/components/pricingBand.js`, `src/components/QuoteCompareModal.jsx`, `src/components/changeRequestParse.js`, `src/components/PilotCommandBar.jsx`, `src/components/ChangeRequestPanel.jsx`, `src/data/__tests__/mockCatalog.costFields.test.js`, `src/hooks/__tests__/useCatalogData.savePlan.test.js`, `src/components/__tests__/marginPresentation.test.js`, `src/components/__tests__/adminCatalogStarterChoice.test.jsx`, `src/components/__tests__/adminCatalogSaveCapabilityState.test.jsx`, `src/components/__tests__/pricingBand.test.js`, `src/components/__tests__/liveBreakdownBand.test.jsx`, `src/components/__tests__/quoteCompareModalMargin.test.jsx`, `src/components/__tests__/changeRequestParse.test.js`, `src/components/__tests__/pilotCommandBar.test.jsx`, `src/components/__tests__/changeRequestPanel.test.jsx` |
| 55 | Customer decision room: the exact-token portal presents the existing customer-safe projection as the customer-facing half of Client 360. One broad event-story canvas holds Event and Package & menu context, compact Optional additions, and collapsed assumptions/terms; one secondary decision rail holds the exact decision state, total, required deposit, expiry, response, mutation feedback, and authority-gated question continuation. Contextual **Ask a question** actions open the one existing conversation composer and acknowledge whether starter text was staged, an existing draft was preserved, a prior send still needs reconciliation, or the thread is read-only/unavailable. Staff-marked add-ons and rentals remain bounded name-and-price suggestions; choosing one prepares a reversible `Please add X.` line in the ordinary change-request note. Only a line generated during the current session can be removed by the same control, and switching to accept or decline discards still-generated addition lines with an explicit acknowledgement. Customer-authored words are preserved. The public route no longer inherits the absent staff navigation offset. Browser-local exact-token fallback mirrors the canonical bounded terms and option shape. | Implemented (source/local candidate requiring both `VITE_PILOT_DECISION_ROOM_ENABLED` and default-off `VITE_AMBIENT_UI_ENABLED`; the released `v0.7.0` subset remains when only Decision Room is enabled. No new callable, message field, catalog exposure, direct quote mutation, pricing authority, or trust boundary. Options do not change selection, totals, revision, payment, booking, or provider evidence; customers must review and send the note, staff must review it, and a trusted save remains authoritative. Focused portal coverage passes 49/49. Dedicated local browser proof passes 5/5 at 390, 768, 1440, and exact-source 1487x1058 with axe, 44px targets, overflow, clipping, collision, and proposal-nonmutation checks; the exact source/implementation comparison received a local 9.5/10 GO review. Exact-head CI, deployment, connected exact-token behavior, production data, provider outcomes, recipient use, and human acceptance remain open.) | `src/App.jsx`, `src/components/CustomerPortalView.jsx`, `src/components/customerPortalView.css`, `src/components/QuoteConversationPanel.jsx`, `src/lib/customerDecisionRoom.js`, `src/lib/quoteStore.js`, `src/data/mockCatalog.js`, `src/hooks/useCatalogData.js`, `src/components/AdminCatalogModal.jsx`, `functions/quoteCreation.js` (`buildPortalDecidableOptions`), `src/styles.css`, `e2e/customer-decision-room.spec.js`, `src/lib/__tests__/customerDecisionRoom.test.js`, `src/lib/__tests__/quoteStore.portal.test.js`, `src/components/__tests__/customerPortalAskAbout.flagOff.test.jsx`, `src/components/__tests__/customerPortalAskAbout.flagOn.test.jsx`, `src/components/__tests__/quoteConversationPanelPrefill.test.jsx`, `src/data/__tests__/mockCatalog.costFields.test.js`, `src/hooks/__tests__/useCatalogData.savePlan.test.js`, `src/lib/__tests__/quoteCreation.server.test.js` |
| 56 | Model-assisted intent parse lane (owner-approved, OpenAI + Anthropic): the pure core module — config gate (enabled flag, provider allowlist including `auto`, per-provider default models, and optional cheap-first `provider:model` candidate order), bounded request sanitization, deterministic strict-JSON prompt, demand profiling with per-request completion-token budgets, provider request builders that never read the environment, bounded auto-route retry/escalation, and untrusted-output validation allowlisting fact fields and forcing every surviving fact to low confidence with a model source — now ships together with its full user-reachable surface: the `functions/` runtime twin, the staff-only same-organization `parseIntentDraft` callable, a client boundary distinguishing the designed dormant reply from a retryable provider outage, and a Model assist button in the CREATE canvas whose suggestions each require explicit per-fact Confirm before touching the draft | Implemented (deployed dormant in `v0.7.0`; remains unavailable until an administrator enables `INTENT_PARSER_ENABLED`, selects a provider mode, and binds the required Secret Manager key or keys; until then the button reports the lane off and typed structuring is untouched; persists nothing; `model-assisted-intent-parse` contract revision 3 with full six-state evidence, `stale` excepted as impossible for a stateless parse; hosted staff/provider acceptance pending) | `functions/index.js` (`parseIntentDraft`), `functions/intentParserCore.cjs`, `src/lib/intentParserCore.cjs`, `src/lib/intentParseClient.js`, `src/components/CreateIntake.jsx`, `src/lib/__tests__/intentParser.server.test.js`, `src/components/__tests__/createIntakeModelAssist.test.jsx`, `docs/INTENT_INTAKE_ADR.md` |
| 57 | Stronger deterministic CREATE reader with a reversible applied-intake handoff, built to an owner-directed "strongest on the market" bar and hardened by two rounds of adversarial verification that executed the real code rather than reasoning abstractly: staff counts (servers/chefs/bartenders, digits/word-numbers/ranges, role synonyms); time ranges filling both start time and computed service hours with cross-midnight support; reversed guest phrasing ("party of 50"); reversed dates ("the 12th of September"); and confirm-only relative weekdays. A pristine New quote leads with CREATE instead of a competing empty-draft command bar; Pilot draft commands appear after work exists or explicit invocation. After applying extracted facts, source evidence collapses into a compact summary, focus moves to its visible heading, and Review intake restores the source field without applying a second time. | Implemented (source/local candidate; browser-only, no provider, no I/O; every verifier-confirmed false positive or misfire — contact-hours context, month-day digit theft, meridiem-wraparound, month-prefix word collisions, relative-weekday precedence — is fixed and locked in as a named regression test; the model-assist lane's fact allowlist gained the same fields in both core-module copies; collapsed presentation and focus transitions do not save or reprice; current 390/1440 browser proof and focused tests are local, not hosted or human acceptance) | `src/App.jsx`, `src/components/CreateIntake.jsx`, `src/components/intentExtraction.js`, `src/components/__tests__/createIntake.test.jsx`, `src/components/__tests__/intentExtraction.test.js`, `src/lib/intentParserCore.cjs`, `functions/intentParserCore.cjs`, `src/styles.css`, `e2e/customer-centered-workspace.spec.js`, `e2e/ambient-intelligence-object-verification.spec.js` |
| 58 | Event-shape memory (design §4.10, first memory slice): once a CREATE reading yields both an event type and a guest count, a flag-gated deterministic aggregate of the tenant's own accepted/booked quote history — same event type, same fixed guest band — reports median servers/chefs/bartenders and half-hour-rounded hours, plus any rental in a strict majority of matches, as a provenance-labeled suggestion | Implemented (source/local candidate; `VITE_PILOT_MEMORY_ENABLED` default off and deliberately not production-bound yet, unlike the other eight pilot gates; no AI, no cross-tenant learning — `getQuoteHistory` is already tenant-scoped server-side; below a minimum sample of 3 the honest reply is "not enough history yet"; applying writes only staffing/hours to the draft, rentals stay a read-only mention so an existing selection is never silently overwritten; `event-shape-memory` contract, six-state evidence, `stale` excepted as impossible for an always-fresh read) | `src/lib/eventShapeMemory.js`, `src/components/CreateIntake.jsx`, `src/styles.css`, `src/lib/__tests__/eventShapeMemory.test.js`, `src/components/__tests__/createIntakeEventShapeMemory.flagOn.test.jsx`, `src/components/__tests__/createIntakeEventShapeMemory.flagOff.test.jsx` |
| 59 | Authoritative operational staffing and Staff workspace: exact-revision staffing requirements, bounded same-tenant operational profiles with `operator_recorded` availability, direct `operator_confirmed` assignments, explicit coverage gaps, cross-date schedule fences, and immutable profile/plan receipts; plus an admin-only private record for contact/photo, qualifications, availability preferences, rates, travel, briefing defaults, attendance summaries, reliability and notes. Revision-bound briefings can be printed or handed off to the default email app. Administrators can preview and manually dispatch one exact-assignment invitation to the verified private email; signed provider events and bearer-link accept/decline acknowledgements appear as separate evidence. The Staff presentation now uses a compact command header, dense 64px photo roster, one primary operational state per person, assignment-first detail, readiness/evidence rail, a collapsed full-record editor, and separate mobile roster/detail states. | Implemented with a verified isolated-staging Firebase-all receipt and still independently gated behind global `OPERATIONAL_STAFFING_AUTHORITY_ENABLED`, tenant `operationalStaffingAuthorityEnabled`, and presentation `VITE_OPERATIONAL_STAFFING_ENABLED`. The protected operator accepts bounded numeric tenant IDs plus only the approved `mm05366-sandbox` founder pilot; it does not default a tenant. Provider dispatch still requires the approved Resend sender and token secret; admins configure records and invitations, admins and sales staff read/apply plans, and local fallback stays `local_draft`. The photo-rich `local_fixture` directory exists only under explicit local authentication bypass and performs no Firebase or provider write. Provider acceptance is not delivery, and acknowledgement is not attendance or payroll evidence. Production v0.16 deployment, exact-tenant activation, recipient use, hosted authenticated behavior, and human acceptance remain unproven. | `functions/operationalStaffingAuthority.js`, `functions/operationalStaffingRuntime.js`, `functions/staffDirectoryAuthority.js`, `functions/staffInvitationAuthority.js`, `functions/index.js` (staffing, directory, invitation, response, and shared signed-webhook routing), `src/lib/operationalStaffingClient.js`, `src/lib/staffDirectoryClient.js`, `src/lib/staffInvitationClient.js`, `src/components/OperationalStaffingPanel.jsx`, `src/components/StaffWorkspace.jsx`, `src/components/StaffInvitationResponsePage.jsx`, `src/components/AmbientLivingOpportunity.jsx`, `firestore.rules`, focused server/client/component/rules/emulator tests |
| 60 | Ambient orientation, NOW, Opportunities, Events recovery, Clients, Library, exact arrivals, Living Opportunity, intelligent objects, sensory feedback, deterministic Pilot, customer-decision-room foundations, purpose-bearing editorial grammar, accepted/booked receipt domains, and equivalent mobile manipulation (AIUI-01–50) | Implemented in the post-`v0.8.1` source candidate behind the governed Ambient/Pilot presentation boundaries; 50/50 items are material and 0/50 are formally closed. Canonical routes, role gates, tenant scope, server pricing, catalog save authority, exact-token portal authority, and provider evidence boundaries remain unchanged. The current completion slice separates Opportunities/Event Room/action-controller concerns, registers the shared editorial surface grammar, keeps customer acceptance, contract, payment, BEO, authoritative staffing, and closeout evidence distinct, and sends swipe/drag/button/keyboard reordering through the same unsaved-review path. Now, Opportunities, and Events share one outcome-led unavailable-state primitive: raw provider text and duplicate evidence warnings stay hidden, each state names what remained unchanged, one retry dominates, and supported work can continue. Opportunities keeps source detail collapsed under About this view; Now uses one explicit grid column so phone-width recovery cannot inherit a Staff-only second column; Events removes its competing header refresh and read-unavailable panel while retaining the staff read context under a collapsed About this view disclosure. Populated Events views also collapse partial/stale/truncated source diagnostics, lead Event Focus with event basics, preserve recorded lifecycle versus operational-readiness semantics, state the live-operations boundary once, and suppress active Control Room/Replay affordances until that authority exists. Exact live/replay links return to Event Focus instead of offering another unavailable destination. Events still handles completed-empty reads and refuses to substitute another event for a missing exact deep link. Duplicated focused route implementations, the replaced Command Center, redundant search palette, and legacy Clients table are absent from the Ambient build graph while the compatibility app remains intact; all other AIUI-48 deletion waits for parity, accepted rollback, release, and promotion evidence. NOW limits itself to bounded priorities; Opportunities keeps proposal, pricing, customer, and operational state separate; Clients and Library retain their exact scoped reads and guarded authorities. The Living Opportunity orders recorded Workflow attention, missing required event/proposal facts, advisory staffing review, and optional phone enrichment in that sequence; a staffing primary opens the exact read-only context and restores focus without changing staffing or readiness truth. Proposal completeness now measures required fields only, while customer phone remains separately visible as recommended contact quality and does not block descriptive prepare/send eligibility. Payment and Proposal inspectors preserve their separate evidence models, expose their scrollable details as named keyboard-focusable regions, keep the exact opportunity visible, demote repeated reason/consequence copy behind a keyboard-operable **Why this view** disclosure, suppress the duplicated lower-body copy only in that compact wrapper, and then hand off to the existing role-safe Quote administration surface with the exact quote retained, opened, focused, and filtered; Escape restores the originating review control, and the handoff itself changes no quote, lifecycle, delivery, request, settlement, or provider state. Trusted proposal controls continue to recheck role, exact saved revision, authoritative pricing, customer email, portal, provider configuration, and idempotency. Pilot v1 remains limited to navigation, query, draft mutation, and simulation. No deployment, connected production-data result, provider outcome, full assistive-technology acceptance, retirement authorization, or human acceptance is claimed. | `src/App.jsx`, `vite.config.js`, `src/components/WorkspaceRoute.jsx`, `src/components/WorkspaceShell.jsx`, `src/components/WorkspaceRecoveryState.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/components/LiveOperationsPlanningViews.jsx`, `src/components/AmbientNowView.jsx`, `src/components/AmbientOpportunitiesStream.jsx`, `src/components/AmbientCustomerDirectoryView.jsx`, `src/components/AmbientClientsView.jsx`, `src/components/AmbientLibraryRoute.jsx`, `src/components/AmbientLivingOpportunity.jsx`, `src/components/AmbientMoneyContext.jsx`, `src/components/AmbientProposalContext.jsx`, `src/components/ambientLivingOpportunityPresentation.js`, `src/components/eventWorkspacePresentation.js`, `src/components/decideStackPresentation.js`, `src/components/ambient/ContextSurface.jsx`, `src/components/AmbientOperationalReceipts.jsx`, `src/components/AmbientGlobalPilotSurface.jsx`, `src/components/PilotCommandBar.jsx`, `src/components/CustomerPortalView.jsx`, `src/lib/quoteWorkflow.js`, `src/lib/ambientProposalObject.js`, `src/lib/ambientOpportunityStream.js`, `src/lib/ambientMoneyObject.js`, `src/lib/commandCenterEvidence.js`, `src/lib/quoteHistoryController.js`, `src/lib/ambientSurfaceGrammar.js`, `src/lib/ambientOperationalReceipts.js`, `src/lib/ambientGestureController.js`, `src/lib/ambientLegacyRetirement.js`, Ambient object/Pilot helpers and contexts, focused unit/component/browser tests, `docs/AMBIENT_WORKSPACE_ARRIVAL_ADR.md` |
| 61 | Public marketing and system-overview routes: hospitality-first acquisition at `/`, a full-bleed QuotePilot commercial layer paced by native viewport scroll chapters, copy-led product storytelling without screen captures, a saved dark product/operating-system overview at `/system`, responsive navigation, product explanations, and guarded entry into the staff or buyer flows | Implemented (source/local cinematic presentation candidate; the commercial remains muted by default with explicit playback/sound controls, reduced-motion pauses on the poster frame, and customer changes stay proposed until human review. Public route reachability was previously deployed and verified; hosted rendering, marketing effectiveness, and human acceptance of this presentation remain separate.) | `src/components/MarketingPage.jsx`, `src/components/SystemMarketingPage.jsx`, `src/App.jsx`, `src/landing.css`, `src/marketing.css`, `public/videos/quote-pilot-commercial.mp4`, `public/videos/quote-pilot-commercial-captions.vtt`, `public/videos/quote-pilot-commercial-poster.webp`, `e2e/marketing-page.spec.js` |
| 62 | Progressive Web App safe recovery shell: stable install identity, 192/512 maskable icons, production service-worker registration, namespaced versioned recovery cache, allowlisted same-origin build-asset caching, and truthful offline navigation fallback | Implemented in the source candidate; the older scaffold remains deployed. The candidate never caches app/API/quote routes, fabricates authenticated Firebase data, or queues trusted mutations; six deterministic worker/manifest tests, four responsive axe/browser checks, and a real minified production-preview offline navigation pass are green. Hosted installation/relaunch and physical-device acceptance remain human/external. | `index.html`, `public/manifest.webmanifest`, `public/sw.js`, `public/offline.html`, `public/brand/quotepilot-mark-192.png`, `public/brand/quotepilot-mark-512.png`, `src/main.jsx`, `src/lib/__tests__/pwaContracts.test.js`, `e2e/pwa-installability.spec.js` |
| 63 | Operations Audit: admin-only same-tenant delivery health, seven-day integration trend, role counts, and role-stamped sensitive-action history with retry candidates kept distinct from resend proof | Implemented (deployed; server-derived operational evidence only, not external CRM activity, provider delivery, or human acceptance) | `functions/operationsAudit.js`, `functions/index.js` (`getOperationsAuditSnapshot`), `src/lib/commerceOps.js`, `src/components/IntegrationOpsModal.jsx`, `src/lib/__tests__/operationsAudit.server.test.js` |
| 64 | Motion, sound, and reduced-motion preferences: centralized optional cue routing with persisted workspace mute, tokenized one-shot interaction motion, and CSS/JavaScript reduced-motion guards that preserve final state without making sound or animation operational evidence | Implemented and deployed in exact `v0.15.0`; hosted interaction and human acceptance remain separate and unverified. | `src/components/soundKit.js`, `src/components/shimmerChime.js`, `src/components/wizardMotion.css`, `src/components/scheduleMotion.css`, `src/components/portalCeremony.css`, `src/components/ShimmerReveal.jsx`, `src/components/DigitRoll.jsx`, `src/components/WorkspaceShell.jsx`, `src/components/ambient/ambientFeedback.js`, focused motion/sound tests |
| 65 | Stripe Connect isolated control-plane foundation: explicit owner authority and backfill; named, separately pinned Firebase codebases; provider-disabled staging infrastructure; a current-role authority projection; redacted status and replay-stable owner handoff contracts; exact `connect-control` repository and HMAC-scoped limits; an immutable edge-command/leased-worker receipt bridge; private post-provider quarantine evidence; and an injected Accounts v2 Sandbox adapter with exact platform/mode preflight | Partial (source/local only). Every edge action must match a receipt-bound authority projection of the current enabled, verified admin set and canonical owner; projections expire within ten minutes. Owner onboarding also requires recent authentication, an exact expected App Check application contract, revision/generation/payload bindings, and one active deterministic ten-minute POST handoff whose bearer value is never stored. The repository reserves generation, authority digest, and a 30-day recovery deadline, while the immutable command's `qpcmd` value is the sole provider idempotency identity. The leased worker rechecks authority/state before provider access and the repository rechecks again after account creation before binding. Post-create validation failure, authority drift, or collision retains provider identity and occurrence only in private quarantine evidence; exact retry after command-receipt interruption reconstructs that quarantine without another Stripe call. Account Link disclosure also requires a post-provider authority/state receipt; `provider_withheld` returns only recovery and never exposes the Stripe URL. Exact rates are refresh 6/principal and 6/organization per five minutes with a ten-second organization interval, begin 6/principal and 10/organization per day, and handoff 3/principal per 15 minutes and 10/organization per day. The adapter verifies the exact platform account and Sandbox balance before access, validates provider-shaped merchant application timestamps, and requires both card-payment and payout capability activity before `ready`. `functions-connect/index.js` still exports nothing; the manifest remains unbound/provider-disabled, and no authority publisher, edge/worker runtime, App Check enforcement/consumption, cloud resource, credential, callable, HTTP route, connected account, Account Link, Stripe request, deployment, hosted Sandbox UAT, production behavior, or human acceptance exists. | `functions/organizationAuthority.js`, `scripts/organization-owner-backfill-plan.mjs`, `scripts/backfill-organization-owner.mjs`, `functions-connect/`, `config/stripe-connect/staging-foundation.json`, `firebase.json`, `firebase.connect.staging.json`, `firestore.connect-control.rules`, `infra/stripe-connect/`, `scripts/check-stripe-connect-foundation.mjs`, `scripts/check-stripe-connect-infra.mjs`, `scripts/check-stripe-connect-onboarding.mjs`, `src/lib/__tests__/stripeConnectAuthorityProjection.test.js`, `src/lib/__tests__/stripeConnectCommandBridge.test.js`, `src/lib/__tests__/stripeConnectCommandOrchestration.test.js`, `src/lib/__tests__/stripeConnectRuntimeAdapters.test.js`, explicit default-codebase deploy scripts, focused tests, `docs/STRIPE_CONNECT_PROGRAM.md` |
| 66 | Owner/admin team role authority: one in-flow Team access surface infers the exact same-organization role, explains why the change is available, its consequence, the do-nothing result, and confidence/provenance before an outcome-named command. Canonical owners manage admin and sales authority; non-owner admins manage sales authority only. The trusted callable binds exact verified email, expected role, one replay-stable request, immutable private receipt, and custom-claim synchronization. | Implemented (source/local only). Password and Google accounts can establish a fresh five-minute authentication proof. App Check is wired as opt-in reCAPTCHA Enterprise monitoring with environment-specific public-key configuration; enforcement and limited-use token replay protection remain off until hosted observation and explicit promotion. Emulator acceptance proves owner/admin boundaries, idempotent replay, owner-demotion denial, receipt privacy, and claim synchronization. No provider registration, hosted role change, deployment, production-data mutation, or human acceptance is claimed. | `functions/organizationRoleAuthority.js`, `functions/index.js` (`getOrganizationRoleRoster`, `mutateOrganizationRole`), `src/components/OrganizationRoleAuthorityPanel.jsx`, `src/components/IntegrationOpsModal.jsx`, `src/lib/authClient.js`, `src/lib/firebase.js`, `src/lib/organizationService.js`, `firestore.rules`, `scripts/customer-centered-authority-emulator-acceptance.mjs`, focused server/client/component/rules tests |
| 67 | Deployment-owned owner SMS provider choice: admin-safe, non-secret runtime-field completeness and one bounded diagnostic over the existing one-way owner-alert rail, with explicit `none`, `twilio`, or `pingram` runtime selection. Quote and payment events reserve a private transactional outbox; one worker claim permits one provider call; organization-scoped versioned HMACs protect destinations; signed Pingram callbacks enter a durable reprocessable inbox; and evidence keeps request acceptance, indeterminate dispatch, carrier delivery, failure, opt-out hold, and recipient-device receipt distinct. | Implemented and deployed in exact `v0.15.0` with `NOTIFICATIONS_SMS_PROVIDER=pingram` and generation `pingram-2026-08-14-a`, but operationally off pending acceptance. No customer SMS or two-way inbox is implied. Provider credential presence is never inferred, the worker fails closed when its bound secret is unavailable, a claimed or indeterminate send is never automatically resent, and signed unsubscribe or exact inbound STOP creates an indefinite cross-provider hold with no browser or callable clear path. Endpoint registration, a provider-accepted call, carrier delivery, sender/compliance acceptance, controlled recipient UAT, hosted behavior, and human acceptance remain unverified. | `functions/index.js` (`getIntegrationSetupStatus`, `sendIntegrationTestSms`, `notifyOwnerNewQuote`, provider workers/reconciliation, `pingramSmsWebhook`), `functions/ownerSmsAttempts.js`, `functions/pingramSms.js`, `src/components/SmsProviderPanel.jsx`, `src/components/IntegrationOpsModal.jsx`, `src/lib/commerceOps.js`, `scripts/materialize-functions-env.mjs`, `scripts/owner-sms-emulator-acceptance.mjs`, `firestore.rules`, focused provider/attempt/panel/materializer/rules tests |
| 68 | Ambient Opportunity workspace plus connected Quote Workspace compatibility/rollback. With the v0.16 Ambient profile enabled, authenticated `/app/quotes/:quoteId` renders the approved Opportunity workspace and contextual Quick Updates over the exact event. Quick Updates stages one allowlisted service-style draft locally, guards every destructive dismissal path, and reviews a server-derived projection of the requested and material pricing, staffing, version, proposal, portal, lifecycle, and dependency effects. A Firebase-backed draft may use one simulation/revision/catalog/policy-fenced save through existing calculation/persistence authority; browser-local, sent, and viewed records hand off to the full editor. Success requires the exact write receipt, a server-only same-tenant quote read, and refreshed opportunity-list state; no staffing, pricing, tax, margin, proposal, client, catalog, or dependency rule is recreated in the panel. The dinner-table Quote Workspace retains exact saved-quote identity, Activity & Save Health, persisted activity, freshness, completeness, and guarded action handoffs on its two compatibility paths and as the Ambient-off exact-route rollback. | Implemented (exact frozen local candidate presentation; `/app/quote-workspace` and `/app/quote-workspace-concept` render the connected Quote Workspace for authenticated admin/sales staff, while the Ambient-enabled ordinary exact route belongs to the Opportunity workspace. Unresolved auth stays behind the normal boundary, non-staff principals receive a role boundary, and a missing exact quote never substitutes another. The Activity drawer remains read-only and Quick Updates mutates nothing before explicit review/save through the trusted path; neither replaces server validation, pricing authority, permissions, version history, or governed Proposal/Payment arrivals. The integrated Phase 1 candidate has passed full local validation; an exact hosted Firebase persistence trace, hosted deployment, authenticated staging review for `mm05366-sandbox`, and owner acceptance remain separate evidence) | `src/components/WorkspaceRoute.jsx`, `src/components/QuoteWorkspaceConceptPage.jsx`, `src/components/QuoteWorkspaceActivityDrawer.jsx`, `src/components/QuickUpdatesPanel.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/lib/quickUpdatesState.js`, `src/lib/quoteStore.js`, `src/components/quoteWorkspaceConcept.css`, `src/components/quoteWorkspaceActivityDrawer.css`, `src/components/quickUpdatesPanel.css`, `src/components/WorkspaceShell.jsx`, `src/components/__tests__/QuickUpdatesPanel.test.jsx`, `src/components/__tests__/quoteHistoryQuickUpdatesAuthority.test.jsx`, `src/components/__tests__/workspaceRoutePortalGuard.test.jsx`, `src/components/__tests__/quoteWorkspaceConcept.test.jsx`, `src/components/__tests__/workspaceShell.test.jsx`, `src/lib/__tests__/quickUpdatesState.test.js`, `src/lib/__tests__/commercialChangeAuthorityIntegration.server.test.js`, `scripts/customer-centered-authority-emulator-acceptance.mjs`, `e2e/steward-workbench.spec.js`, and `e2e/v16-calm-four-acceptance.spec.js` |
| 69 | Catalog-owned proposal presentation and Quote Pulse margin/cost context: Pricing admins choose bounded proposal font scale, safe logo/monogram letterhead, brand readiness, guided-rule coverage, operator-authored proposal intro/closing copy, and cost/margin coverage; trusted quote create/edit snapshots persist document font scale, proposal copy, and a private commercial cost snapshot; a normalized supplied pricing snapshot missing that private evidence is enriched without replacing its exact pricing totals or authority label; Proposal Composer applies saved brand/font/copy presentation and shows proposal-polish plus complete saved cost/margin only as staff context | Implemented (source/local only; uses existing catalog save/reconciliation and margin gate, adds no pricing authority or customer cost exposure; local fallback records do not prove hosted server authority; invalid logo schemes fail closed and indirect Google image-result links normalize to their direct HTTPS asset; exported/customer-facing proposals receive only brand/logo/font/copy presentation, never margin or internal costs; hosted staff acceptance pending) | `src/lib/brandLogoUrl.js`, `src/lib/commercialSnapshot.js`, `src/lib/proposalDocumentPreferences.js`, `src/data/mockCatalog.js`, `src/lib/pricingContracts.js`, `functions/quoteCreation.js`, `functions/index.js`, `src/lib/quoteStore.js`, `src/lib/proposalPayload.js`, `src/lib/proposalExport.js`, `src/components/AdminCatalogModal.jsx`, `src/components/ProposalComposer.jsx`, `src/components/proposalComposer.css`, `src/components/ambientLibraryRoute.css`, `src/components/eventTemplatesEditor.css`, `src/lib/__tests__/ambientPricingPreview.test.js`, `src/lib/__tests__/brandLogoUrl.test.js`, `src/lib/__tests__/pricingContracts.test.js`, `src/lib/__tests__/quoteStore.versioning.test.js`, `src/components/__tests__/proposalComposerSaveReadiness.test.js`, `src/lib/__tests__/proposalPayload.test.js`, focused proposal/catalog tests |
| 70 | QuotePilot Offer presentation over the existing Package Workspace: a compact stable-ID Offer navigator and broad selected-Offer workspace lead with identity, price, quoting availability, included components, derived margin, and one deterministic next action; detailed cost/contribution, readiness evidence, immutable identity, revision, pricing confirmation, and quote behavior remain under secondary disclosures. Recorded choice groups project read-only without adding browser authoring or native quote selection. Staged searchable/category-filtered inclusion selectors, readiness-gated activation, Offer-scoped revert, dependency-aware confirmed delete, responsive Offer switching, full-viewport mobile selection, and one in-flow staged save bar remain on `Library -> Offers`. | Implemented locally at `b593fe4d2c45ff5db20bafe205461157952b9ab4` over the existing Package records and authority. Local Ambient Library evidence passes 17/17, the Calm Four regression passes 10/10 with `VITE_OPERATIONAL_STAFFING_ENABLED=true`, and exact 390/768/1440 plus combined visual comparisons received a **9.5/10 — GO** local review. Full unit, build, bundle, capability/document governance, and CWV checks pass; the aggregate release lane remains blocked by six absent local Firebase web configuration values. The current package schema, per-person pricing, selected-at-$0 inclusion behavior, catalog revision fencing, and managed-menu/save separation remain unchanged; no Bundle entity, backend authority, or quote-pricing contract was added. Authenticated hosted roles, deployment, production data, assistive-technology acceptance, and human acceptance remain open. | `src/components/AdminCatalogModal.jsx`, `src/components/PackageWorkspace.jsx`, `src/lib/packageWorkspaceModel.js`, `src/lib/catalogWriteShapes.js`, `src/lib/__tests__/packageWorkspaceModel.test.js`, `src/lib/__tests__/packageWorkspacePricingParity.test.js`, `src/components/__tests__/packageWorkspace.test.jsx`, `src/components/__tests__/adminCatalogSaveCapabilityState.test.jsx`, `src/components/__tests__/adminCatalogStarterChoice.test.jsx`, `src/components/ambientLibraryRoute.css`, `src/styles.css`, `e2e/ambient-library.spec.js`, `docs/PACKAGE_WORKSPACE.md`, `docs/design/library-commercial-convergence-checkpoint.md` |
| 71 | Private Steward validation, control, and Difficult Question Desk shadow foundation: fixed task/request/source contracts, same-tenant and revision fencing, US-only/role/content/prompt-attack policy, secret and sensitive-personal-data rejection, deterministic commercial-number provenance, unsafe-rendering rejection, canonical serialization, expiring tamper-evident Decision Packets, pseudonymous audit metadata, bounded retention/deletion plans, incident recovery evidence, kill/rollback gates, explicit private-record browser denial, one fixed `draft_response` compiler that minimizes typed context, requests no tools/background/storage, inventories every claim, and fails back to manual work, a current owner/admin-and-participant consent gate, pinned 100-case corpus, digest-only human-comparison receipts, non-authoritative promotion evidence, and a discoverable read-only quote-workspace status panel whose live state exposes only manual recovery | Partial (source/local security, shadow-evaluation, and unavailable-state UI foundation only; 62 focused server tests, 22 focused UI tests, 3 real-route Playwright checks, and all 76 Firestore emulator tests pass. The CommonJS compiler/evaluation modules remain unimported and unexported by runtime code. The staff Quote Workspace renders no model prose, uses no provider, keeps Steward handoff disabled, and enables only the ordinary manual-message route. There is no callable, configured provider request, Steward persistence, pricing calculation, payment, configuration, customer send, deployment, background inference, or production capability. The 100 cases are a pinned synthetic corpus, not 100 completed human reviews. Concrete provider transport/credential review, canonical context reads, private runtime/persistence, consenting silent pilot execution, human review, hosted rules evidence, billing, production evidence, and human acceptance remain open.) | `src/lib/steward/contracts.cjs`, `src/lib/steward/policy.cjs`, `src/lib/steward/validate.cjs`, `src/lib/steward/controls.cjs`, `src/lib/steward/difficultQuestionDesk.cjs`, `src/lib/steward/difficultQuestionEvaluation.cjs`, `src/components/StewardWorkbench.jsx`, `src/components/QuoteWorkspaceConceptPage.jsx`, `firestore.rules`, `src/lib/__tests__/stewardContracts.server.test.js`, `src/lib/__tests__/stewardPolicy.server.test.js`, `src/lib/__tests__/stewardDecisionPacket.server.test.js`, `src/lib/__tests__/stewardControls.server.test.js`, `src/lib/__tests__/stewardDifficultQuestionDesk.server.test.js`, `src/lib/__tests__/stewardDifficultQuestionEvaluation.server.test.js`, `src/components/__tests__/stewardWorkbench.test.jsx`, `src/components/__tests__/quoteWorkspaceConcept.test.jsx`, `e2e/steward-workbench.spec.js`, `src/rules/__tests__/firestore.rules.test.js` |
| 72 | Proposal Composer truthful preset and blocker-review semantics: an inherited package/service-style starting point remains `Preset` until both controls are explicitly reviewed or the quote is saved; known save requirements produce an exact `Review N blockers` action that opens Quote Pulse and focuses the requirement list without invoking persistence, including when a true save is disabled | Implemented (source/local candidate; the ordinary save, server validation, pricing, versioning, role, and tenant authorities are unchanged; focused unit tests and a real browser-local four-blocker-to-saved-draft walkthrough pass, while hosted Firebase behavior and human acceptance remain open) | `src/App.jsx`, `src/components/ProposalComposer.jsx`, `src/components/proposalComposerPresentation.js`, `src/components/proposalComposer.css`, `src/components/__tests__/proposalComposerPresentation.test.js`, `src/components/__tests__/proposalComposerSaveReadiness.test.js` |
| 73 | Product Truth Observability: deterministic read-only reconciliation of Git identity/divergence, canonical production claims, capability inventory/gate results, development-evidence coverage, optional bounded reachability, stable drift findings, and three owner decisions through text/JSON status and fail-closed gate commands | Implemented (source/local developer-infrastructure candidate; `status:product` remains report-successful when drift exists, `check:product-drift` exits 1 for blocking contradictions and 2 for malformed required input, snapshots stay ignored, and CI invocation is advisory. The digest is a projection only; it does not prove deployment identity, authenticated behavior, provider/production outcome, or human acceptance. Exact CI observation, comprehension review, freshness calibration, and any required-gate promotion remain human/external.) | `scripts/product-truth-observability.mjs`, `scripts/summarize-development-evidence.mjs`, `src/lib/__tests__/productTruthObservability.test.js`, `.github/workflows/ci-quality.yml`, `docs/adr/ADR-0002-product-truth-observability.md`, `docs/design/product-truth-observability-design.md` |
| 74 | Session-only cross-route staff task continuity plus the first authoritative closure adapter: exact ranked follow-up actions from Now, Opportunities, and Client 360 retain a principal-, role-, and organization-bound presentation thread into Workflow; exact arrival remains distinct from completion; a Firebase completion resolves only after a matching server-only same-organization quote read, bounded internal confirmation, and absence from fresh Attention; every local or ambiguous outcome remains proofless and retry reads without repeating the write | Implemented as a source/local candidate. Four browser cases prove exact three-source navigation, responsive/session continuity, zero-write inspection, and dirty-draft guarding; a fifth proves an intentional browser-local follow-up save changes only the named quote/version history and fails closed as **Needs confirmation**. Mocked component coverage exercises the resolved server-readback branch, but no connected Firebase authorization/readback, provider non-invocation, cross-route invalidation, hosted behavior, deployment, production data, assistive-technology comprehension, or human acceptance is claimed. | `src/App.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/WorkspaceTaskJourneyNotice.jsx`, `src/lib/workspaceTaskJourney.js`, `src/components/__tests__/salesWorkflowAttentionFocus.test.jsx`, `src/components/__tests__/workspaceTaskJourneyNotice.test.jsx`, `src/lib/__tests__/workspaceTaskJourney.test.js`, `src/lib/__tests__/appInteractionRecovery.test.js`, `e2e/staff-app-cross-route-task-loop.spec.js`, `docs/design/staff-app-ui-cohesion-audit.md`, `docs/acceptance/staff-app-ui-cohesion-acceptance-matrix.md` |
| 75 | Privacy-bounded native staff-route return context: exact Opportunities, Clients, Client 360, and Library round trips retain safe filters, disclosure, focus, and settled scroll in the current tab while native Back/Forward remains the only route stack. The history token carries only canonical paths, allowlisted enum queries, opaque surface IDs, and runtime/organization/principal scope; free text, cursors, disclosures, scroll, focus, exact Library targets, and drafts stay memory-only. Library editors use distinct same-URL entries and one busy/dirty dismissal authority for explicit and browser traversal. | Implemented as a source/local candidate. Seventeen Chromium-admin cases cover the three-route 390/768/1440px matrix plus real Clients pagination, Client 360, nested contextual Library, delayed quote reads, two reload boundaries, invalid/foreign recovery, and direct dirty-editor Back: exact focus and scroll within 8px, exact Forward targets, no record substitution, 44px controls, no overflow, semantic landmark/heading order, zero scoped Axe violations on restored route surfaces, and no serious or critical whole-page violations on destinations. Non-expiry fixtures retain byte-equivalent quote/history/catalog state and issue no unexpected non-GET request. The return-context layer adds no write authority; the pre-existing administrator quote-history read may still persist automatic expiry under its existing lifecycle authority. Unit/component coverage rejects forged, non-adjacent, stale-runtime, cross-organization/principal, unknown-route, and invalid-query tokens and proves owner-keyed guard fallback. Forced colors, 200% zoom, Firefox/WebKit, real assistive technology, authenticated hosted data, deployment, production behavior, and human acceptance remain open. | `src/App.jsx`, `src/hooks/useBrowserLocation.js`, `src/context/WorkspaceNavigationContext.jsx`, `src/lib/workspaceReturnContext.js`, `src/lib/workspaceArrivalContract.js`, `src/components/QuoteHistoryModal.jsx`, `src/components/AmbientOpportunitiesStream.jsx`, `src/components/AmbientCustomerDirectoryView.jsx`, `src/components/AmbientClientsView.jsx`, `src/components/CustomerWorkspaceView.jsx`, `src/components/AmbientLibraryRoute.jsx`, `src/components/AdminCatalogModal.jsx`, focused unit/component tests, `e2e/staff-app-return-context.spec.js`, `e2e/ambient-library.spec.js`, `docs/DESIGN_SYSTEM.md`, `docs/USER_MANUAL.md`, `docs/design/staff-app-ui-cohesion-audit.md`, `docs/acceptance/staff-app-ui-cohesion-acceptance-matrix.md` |
| 76 | Dedicated same-runtime staff action-feedback capability: a presentation-only registry fences `pending`, `succeeded`, `recovery`, `uncertain`, and `cancelled` to the exact organization, principal, role, action, attempt, generation, and affected object. One Calm Four feedback rail shares a non-overlapping continuity stack with the independent Current task rail, owns one atomic announcement, states changed/unchanged facts, and offers at most one safe action. Canonical `ready`, `submitting`, `uncertain`, `reconciliation`, `receipt`, `error`, and `recovery` markers make its visible mutation states structurally testable. The first and only adapter is exact tracked Workflow follow-up completion. Exact authoritative proof is always required; when the attempt still owns Current task, Workflow cannot render Confirmed until App persists that task's closure, while older feedback may reconcile independently without changing or claiming completion of a newer or missing task. | **Partially implemented as a source/local candidate.** The pure registry, staff-only scope-bound provider across both authorized workspace branches, visual region, and first Workflow adapter are present. Focused coverage exercises strict identity/transition fences, exact-revision terminal acknowledgement with deterministic focus return, non-dismissible uncertainty with inspect/reconcile-only actions, malformed-presentation fail-closed behavior, fixed-copy privacy, preflight focus, required-feedback begin refusal before dispatch, unresolved-capacity refusal, immutable attempt ownership, late-promise and staff-identity-change cleanup, auth-loading scope teardown, exact return after task tracking stops, announcement cleanup, matching-task persistence, independent older-feedback reconciliation, and duplicate-write prevention even when completion is unchecked. The dedicated local Chromium-admin matrix covers Now at 390×844, Opportunities at 768×900, and Client 360 at 1440×1000 with `VITE_E2E_LOCAL_REVIEW_FIXTURES=false`; six same-context before/after captures cover the responsive continuity stack and corrected tablet identity wrap. Success remains bound to the existing Firebase result and exact same-organization server-only readback. A matching Current task must persist closure first; an older feedback attempt leaves any newer or missing task untouched and omits the task-completion fact. Ambiguous outcomes freeze a second write, cannot be dismissed or converted directly to evidence-free recovery, and permit only read-only inspection or reconciliation. Ordinary authorized route changes retain the same-runtime record, full reload clears it, organization/principal/role or unresolved-auth state invalidates it, and customer/portal/unresolved-auth/denied-role branches never mount its provider. The Workflow adapter supplies product-owned fixed action/message/outcome copy, a bounded quote-number object label, and opaque IDs without passing its note, customer/staff email, thrown provider text, token-like material, or raw record; generic pattern rejection is defense in depth, not arbitrary-prose provenance. Quick Updates, Catalog, and all other staff mutations are not migrated, so recommendation 17 remains open. No new Firebase, Firestore, provider, workflow, pricing, or persistence authority is introduced; final result counts belong to the immutable completion handoff, and connected, hosted, production, cross-browser, 200% zoom, forced-colors, assistive-technology, and human acceptance remain open. | `src/App.jsx`, `src/components/WorkspaceRoute.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/components/WorkspaceActionFeedbackNotice.jsx`, `src/components/workspaceActionFeedbackNotice.css`, `src/components/workspaceTaskJourneyNotice.css`, `src/context/WorkspaceActionFeedbackContext.jsx`, `src/lib/workspaceActionFeedback.js`, `src/components/__tests__/salesWorkflowAttentionFocus.test.jsx`, `src/components/__tests__/workspaceActionFeedbackNotice.test.jsx`, `src/components/__tests__/workspaceRoutePortalGuard.test.jsx`, `src/context/__tests__/WorkspaceActionFeedbackContext.test.jsx`, `src/lib/__tests__/appInteractionRecovery.test.js`, `src/lib/__tests__/workspaceActionFeedback.test.js`, `e2e/staff-app-action-feedback.spec.js`, `docs/capability-surfacing-contracts.json`, `docs/DESIGN_SYSTEM.md`, `docs/USER_MANUAL.md`, `docs/design/staff-app-ui-cohesion-audit.md`, `docs/acceptance/staff-app-ui-cohesion-acceptance-matrix.md` |

| 77 | Production Resend acceptance test: verified platform administrators receive one Integrations Ops control for a server-owned message to an exactly confirmed controlled `quietpilot.us` recipient. The server persists a private request receipt before one provider call, rejects unsupported/custom fields, uses one stable provider idempotency key, returns the bounded provider message receipt, blocks reuse of the same request, and projects the action into Operations Audit without its provider identifier. | Implemented and deployed in exact v0.16.5. Focused server and component tests cover the platform-admin, exact-domain, exact-confirmation, secret-isolation, write-before-contact, duplicate-prevention, ambiguous lockout, receipt-privacy, and mutation-state contracts. One production request by the verified platform administrator returned `provider_accepted`, locked the visible control, produced one bounded audit row, and reached Resend `delivered`. Same-tenant admin negative visibility passed without changing the production platform-admin allowlist. Recipient inbox arrival, message-content review, full non-mutation proof, ordinary customer-email readiness, spam placement, and human acceptance remain open. | `functions/resendAcceptanceTest.js`, `functions/index.js` (`sendResendAcceptanceTestEmail`, `getOperationsAuditSnapshot`), `functions/operationsAudit.js`, `src/lib/resendAcceptanceClient.js`, `src/components/EmailProviderAcceptancePanel.jsx`, `src/components/IntegrationOpsModal.jsx`, `src/lib/__tests__/resendAcceptanceEmail.server.test.js`, `src/lib/__tests__/operationsAudit.server.test.js`, `src/components/__tests__/emailProviderAcceptancePanel.test.jsx`, `PROJECT_STATUS.md` |

| 78 | QP-UXR-002 Clear the Deck decision resolution: each exact pending approval or retained Decision Debt item becomes a business-readable decision record, hands off to the exact Workflow object, preserves context beside role-gated controls, requires existing-authority readback before confirmation, and returns to reconciled source context. | Implemented on the `v0.18.0` release candidate. The projection consumes existing Workflow and Decision Debt evidence and canonical approval eligibility; it does not resolve Decision Debt, execute the approved action, or create pricing, payment, approval, or provider authority. Exact same-organization server-only readback and request-bound proof are required. Uncertainty prevents blind duplicate decisions; stale, partial, failed, or contradictory evidence withdraws approval. Focused and responsive local evidence passes; exact-SHA CI, connected production behavior, hosted roles, provider outcomes, and human acceptance remain separate. | `src/lib/decisionResolutionPresentation.js`, `src/components/LiveOperationsPlanningViews.jsx`, `src/components/SalesWorkflowModal.jsx`, `src/App.jsx`, `src/lib/workspaceTaskJourney.js`, `src/lib/workspaceReturnContext.js`, `src/components/WorkspaceTaskJourneyNotice.jsx`, `src/components/WorkspaceActionFeedbackNotice.jsx`, focused unit/component tests, `e2e/ambient-intelligence-object-verification.spec.js` |
| 79 | QP-UXR-003 Commercial Priority Loop: Opportunities and Now compose exact quote/event/customer identity, saved commercial value, separate lifecycle/proposal/booking/payment facts, supported significance, timing, consequence, and one current next action; exact Workflow round trips reconcile only the same object and require a current read before retained mutation controls remain eligible. | Implemented on the `v0.18.0` release candidate. One shared pure projection reuses the bounded tenant quote read, canonical saved totals, proposal readiness, Workflow order, arrival, task, return-context, and reconciliation contracts. It performs no list repricing, produces no blended score, preserves valid recorded zero, and leaves missing or malformed money/payment/booking evidence unavailable. Local exact-round-trip and responsive evidence passes; connected Firebase reconciliation, production data, provider outcomes, and human acceptance remain separate. | `src/lib/ambientOpportunityStream.js`, `src/lib/commercialEvidencePresence.js`, `src/components/nowPresentation.js`, `src/components/AmbientNowView.jsx`, `src/components/AmbientOpportunitiesStream.jsx`, `src/lib/commandCenterEvidence.js`, `src/lib/workspaceReturnContext.js`, `src/components/SalesWorkflowModal.jsx`, `src/components/QuoteHistoryModal.jsx`, `src/lib/quoteStore.js`, `src/App.jsx`, focused unit/component tests, `e2e/ambient-opportunities-stream.spec.js`, `e2e/staff-app-cross-route-task-loop.spec.js` |
| 80 | QP-UXR-004 Commitment-to-Execution Loop: accepted/booked Events become a commitment index; Event Focus briefs the exact commercial commitment and saved plan; Control Room composes known gaps, planned run of show, recorded checklist, preserved source evidence, and one existing-authority next action; Replay preserves the missing-ledger boundary. | Implemented on the `v0.18.0` release candidate using the existing bounded quote snapshot, saved totals, allowlisted lifecycle/payment evidence, Workflow priority, exact governed BEO and staffing reads, tenant-calendar timing, exact Schedule arrival, and read-only run-of-show/checklist projection. Only selected Control Room requests operational authorities. Unknown evidence stays unknown. No live-event aggregate, readiness score, phase, issue, attendance, actual-cost, completion, or immutable replay authority was added. Local three-width route/focus/accessibility proof passes; hosted and human acceptance remain separate. | `src/components/LiveOperationsPlanningViews.jsx`, `src/components/eventWorkspacePresentation.js`, `src/lib/eventRunOfShow.js`, `src/lib/ambientOpportunityStream.js`, `src/lib/kitchenBeoClient.js`, `src/lib/operationalStaffingClient.js`, `src/lib/workspaceArrivalContract.js`, `src/components/StaffEvidenceRail.jsx`, `src/App.jsx`, `src/styles.css`, focused unit/component tests, `e2e/commitment-to-execution-loop.spec.js` |
| 81 | QP-UXR-005 Event Preflight: a committed event's selected Control Room separates evidence-supported satisfied facts, known attention, and unknown/unavailable truth without inventing a readiness score. | Implemented on the `v0.18.0` release candidate by composing saved lifecycle, exact acceptance/current-revision relationship, allowlisted payment facts, final-count checklist, BEO freshness/invalidation, operational staffing coverage, bounded Workflow priority, and decidable bounded Schedule conflicts. Commercial, BEO, and staffing positives are fenced to the current snapshot, quote, and revision; missing, stale, partial, truncated, malformed, mismatched, or undecidable evidence never passes. Inventory, actual attendance, live phase, and live issues remain unavailable. Focused and responsive local evidence passes; connected authenticated readback, production data, and human acceptance remain separate. | `src/components/EventPreflightPanel.jsx`, `src/components/eventPreflightPresentation.js`, `src/components/LiveOperationsPlanningViews.jsx`, `src/components/EventScheduleModal.jsx`, `src/App.jsx`, `src/styles.css`, focused tests, `e2e/event-preflight.spec.js` |
| 82 | Quote Edit responsive domain context: the existing Quote plan keeps Event, Customer, Experience, Staffing, and Commercials attached to the dominant proposal object as a sticky left rail at desktop and compact-desktop widths, then adapts to a horizontal navigator at tablet and phone widths. | Implemented on the `v0.18.0` release candidate after direct owner acceptance evidence exposed the missing compact-desktop context. It changes presentation only: quote state, completeness, pricing, save, role, tenant, revision, and domain authorities are unchanged. Browser assertions pass at 1440, 1008, 768, and 390 pixels. | `src/components/ProposalComposer.jsx`, `src/components/proposalComposer.css`, `e2e/proposal-composer.spec.js` |
| 83 | QP-UXR-006 | **Undefined / owner decision required.** No canonical plan, task record, or owner-approved specification defines the feature. No behavior is implemented or claimed. A bounded recommendation is recorded separately and remains unapproved. | `docs/QP_UXR_002_006_OWNER_REVIEW.md` |
| 84 | Corrected Ingredient Inventory Phase 2: organization-scoped ingredient/location definitions, strict fractional base-unit quantities, immutable opening-stock movements, independent exact purchase-cost observations, rebuildable current stock/cost states, transaction receipts, and bounded realtime workspace and per-ingredient projections. The operational route guides administrators through location, ingredient, opening stock, and optional cost evidence without treating either axis as readiness for the other. | **Complete as a default-off local source candidate.** The route is wired into both production build graphs and remains hidden outside its exact workspace, browser, tenant, Firebase, and administrator gates. A demo-only emulator proves callable Auth/App Check, shared Firestore contention, concurrent idempotency, request substitution denial, one-winner opening stock, and stock/cost independence. The earlier equipment-first Phase 1 remains an honest historical checkpoint, but whole-unit reusable-resource, interval availability, rental mapping, checkout/return, turnaround, and damage/repair behavior are superseded and absent from the active surface. This slice does not yet claim recipes, menu/event food cost, stock allocation, consumption, COGS, deployment, tenant activation, production behavior, or human acceptance. | `docs/INVENTORY_AUTHORITY_ADR.md`, `functions/inventoryIngredientCore.cjs`, `functions/inventoryAuthority.js`, `functions/index.js`, `firestore.rules`, `scripts/inventory-ingredient-emulator-acceptance.mjs`, `src/lib/inventoryAuthorityClient.js`, `src/components/InventoryWorkspace.jsx`, `src/App.jsx`, `src/LegacyApp.jsx`, `src/components/WorkspaceShell.jsx`, focused core/runtime/client/component/rules/emulator/build-graph tests |
| 85 | Ingredient Inventory Phase 3: exact standard-unit conversion, immutable ingredient-specific purchase-pack definitions, catalog-fenced versioned recipes attached to canonical menu items, pure rational recipe costing, bounded reverse dependencies, and materialized current menu-cost projections. Inventory owns pack declarations; Library owns recipe editing and reads small realtime projections rather than rebuilding cost from raw ledgers or receipts. | **Complete as a default-off local source candidate.** Administrator recipe publication uses the existing receipt/revision transaction authority and never writes catalog selling price or manual cost. Missing cost produces a labeled partial or unavailable result; missing yield, incompatible conversion, invalid reference, or mixed currency cannot become a complete cost. Ingredient changes stale and idempotently refresh only reverse-indexed dependent menus. Sales may read the bounded safe menu-cost projection but cannot read inventory stock or publish recipes. Dirty, pending, uncertain, committed-awaiting-projection, rejected-with-attempt, and realtime-conflict states preserve the exact Library editor and prevent context loss until recovery. Recipe costing does not require stock and this phase does not yet claim quote/event portions, food-cost estimates, shortages, allocation, consumption, COGS, deployment, tenant activation, production behavior, or human acceptance. | `docs/INVENTORY_AUTHORITY_ADR.md`, `functions/inventoryRecipeCore.cjs`, `functions/inventoryAuthority.js`, `firestore.rules`, `scripts/inventory-ingredient-emulator-acceptance.mjs`, `src/lib/inventoryAuthorityClient.js`, `src/components/InventoryWorkspace.jsx`, `src/components/InventoryRecipeEditor.jsx`, `src/components/AdminCatalogModal.jsx`, `src/components/AmbientLibraryRoute.jsx`, `src/App.jsx`, focused core/runtime/client/component/rules/emulator tests |
| 86 | Ingredient Inventory Phase 4: immutable event ingredient demand compiled from exact saved quote/menu identity, explicit output quantities, tenant-timezone-derived event timing, and pinned recipe revisions; shared ingredients retain per-menu contribution while aggregating into independent demand, projected-cost, and consumable-availability rails. | **Complete as a default-off local source candidate.** Same-tenant administrators and sales may preview without writing; only administrators may record through the receipt/revision-fenced callable. The exact event projection uses metadata-aware `onSnapshot()` and distinguishes loading, cached, pending, stale, unavailable, uncertain, preview, and recorded evidence. It never guesses portions from guest or billing quantity, never lets shortage invalidate a valid cost (or missing cost hide a shortage), never changes catalog price/manual cost, and never allocates stock. This phase does not claim allocation, receiving, consumption, COGS, deployment, tenant activation, production behavior, or human acceptance. | `functions/inventoryEventDemandCore.cjs`, `functions/inventoryAuthority.js`, `functions/index.js`, `firestore.rules`, `scripts/inventory-ingredient-emulator-acceptance.mjs`, `src/lib/inventoryAuthorityClient.js`, `src/hooks/useEventIngredientProjection.js`, `src/components/EventIngredientProjectionPanel.jsx`, `src/App.jsx`, `src/LegacyApp.jsx`, focused compiler/runtime/client/component/rules/emulator/build tests |
| 87 | Ingredient Inventory Phase 5: durable receiving plus date-independent consumable allocation, partial shortage top-up, and release. Exact ingredient/location fences serialize competing events; current plans and immutable plan revisions retain requirement, quote, recipe, quantity, and receipt provenance without changing physical stock until consumption. | **Complete as a default-off local source candidate.** Receiving appends physical movement and exact known-or-unknown cost evidence; it may establish only an absent planning-cost basis and never invents weighted average or replaces existing missing/contradictory evidence. Only administrators may allocate accepted/booked current quote revisions with complete demand. Allocation and release recheck exact commercial, recipe, requirement, plan, stock, and fence authority transactionally; sales receive read-only exact projections. Metadata-aware listeners keep pending receipt, committed projection, cached, stale, shortage, and uncertain states distinct. This phase does not claim consumption, waste, authoritative COGS, deployment, tenant activation, production behavior, or human acceptance. | `functions/inventoryIngredientAllocationCore.cjs`, `functions/inventoryIngredientCore.cjs`, `functions/inventoryAuthority.js`, `firestore.rules`, `scripts/inventory-ingredient-emulator-acceptance.mjs`, `src/lib/inventoryAuthorityClient.js`, `src/hooks/useEventIngredientProjection.js`, `src/components/InventoryWorkspace.jsx`, `src/components/EventIngredientProjectionPanel.jsx`, `src/App.jsx`, `src/LegacyApp.jsx`, focused core/runtime/client/component/rules/emulator/build tests |
| 88 | Ingredient Inventory Phase 6: preserved historical requirement/cost evidence, bounded source-directed invalidation, independent demand/cost/availability/allocation freshness, explicit stale-hold reconciliation, and read-only ingredient consequences in Commercial Change. | **Complete as a default-off local source candidate.** Exact quote, recipe, and menu-cost transitions stale only indexed dependent event projections; old trigger delivery fails closed after source advancement. Active holds remain effective until explicit administrator reconciliation or release. Reconcile atomically releases prior fence quantities and reallocates the current recorded requirement with immutable intermediate/final plan revisions and an idempotent receipt. Commercial Change composes an exact saved `onSnapshot()` projection with a server preview to show independent ingredient-cost and per-unit stock deltas outside commercial authorization/apply behavior. This phase does not consume stock, change selling price, claim valuation/COGS, deploy, activate a tenant, prove production behavior, or establish human acceptance. | `functions/inventoryIngredientReconciliationCore.cjs`, `functions/inventoryAuthority.js`, `functions/index.js`, `firestore.rules`, `src/lib/inventoryAuthorityClient.js`, `src/lib/commercialInventoryConsequences.js`, `src/hooks/useEventIngredientProjection.js`, `src/components/EventIngredientProjectionPanel.jsx`, `src/components/CommercialChangeImpactPanel.jsx`, `src/App.jsx`, `src/LegacyApp.jsx`, focused core/runtime/client/component/rules/emulator/build tests |
| 89 | Ingredient Inventory Phase 7: event-bound consumption and waste evidence, explicit allocation settlement, correction revisions, physical-stock deltas, and planned-versus-actual quantity plus saved-planning-basis cost comparison. | **Complete as a default-off local source candidate.** The accepted/booked event Control Room uses two exact metadata-aware `onSnapshot()` reads: the pinned allocation plan and a separate execution projection. Administrators explicitly enter complete fractional consumed/waste totals and acknowledgement; sales receive the same projection read-only. The server transaction preserves other holds, removes the event hold, decrements on-hand exactly once, writes immutable execution and movement evidence, and supports retry-safe full-replacement corrections without recreating allocation. `settled` remains distinct from `released`; a zero-net consumption/waste reclassification creates evidence without a stock movement. Cost comparison uses only complete immutable saved-plan evidence and is labeled planning-basis intelligence. Multi-observation valuation remains unresolved, so authoritative actual COGS and inventory value remain unavailable. This does not deploy, activate a tenant, prove production behavior, or establish human acceptance. | `functions/inventoryIngredientExecutionCore.cjs`, `functions/inventoryIngredientCore.cjs`, `functions/inventoryIngredientAllocationCore.cjs`, `functions/inventoryAuthority.js`, `functions/index.js`, `firestore.rules`, `scripts/inventory-ingredient-emulator-acceptance.mjs`, `src/lib/inventoryAuthorityClient.js`, `src/hooks/useEventIngredientExecutionProjection.js`, `src/components/EventIngredientUsagePanel.jsx`, `src/components/LiveOperationsPlanningViews.jsx`, `src/App.jsx`, focused core/runtime/client/hook/component/rules/emulator/build tests |

Stripe Connect completion-boundary note for row 65: the deploy-dormant
repository foundation is complete under its fixed Accounts v2 merchant model.
`check:stripe-connect-foundation`, `check:stripe-connect:infra`, and
`check:stripe-connect:onboarding` pass, and the read-only live staging preflight
is `READY` for the exact Firebase project, Web app, and protected
`connect-control` database. Row 65 remains `Partial` because the next work is a
human/external saved-plan review and authorized apply, identity/manifest
reconciliation, App Check observation and promotion, restricted Sandbox
credential binding, runtime exports, provider execution, hosted UAT, and human
acceptance. No additional source-only runtime slice may bypass those gates.

Steward completion-boundary note for row 71: the deploy-dormant security,
compiler, consent/evaluation, hidden-output workbench, and manual-recovery
foundation is repository-complete before its private-runtime gate. Eight
focused files pass 84 tests, the secret scan passes, and three responsive
real-route checks pass. Row 71 remains `Partial` because provider/credential and
privacy/billing review, canonical context reads, private persistence,
consenting silent execution, 100 actual human reviews, hosted rules evidence,
deployment, and human acceptance remain human/external. Model output and
Steward handoff remain unavailable; no source-only adapter may fabricate those
gates.

Ambient attendance source note for row 60: the Guest-count inspector now
consumes the pure `attendance-state-v1` read model and orders **Saved priced
count**, **Best attendance evidence**, and a source-backed **Open decision**
before dependencies. It shares the existing completed exact-quote Decision Debt
read without another callable, ignores stale/error/loading/mismatched reads,
keeps malformed future evidence in a bounded review state, and routes an exact
final-count item only to its existing Workflow destination. This is source/local
presentation with no attendance persistence, confirmation, repricing, staffing,
reservation, proposal, BEO, closeout, payment, or provider authority.

Ambient read-recovery source note for row 60: **Now**, **Opportunities**, and
**Events** use the shared `WorkspaceRecoveryState` for unavailable first reads.
Each state hides raw provider text and duplicate warnings, names the unchanged
record boundary, exposes one retry and one supported continuation, and keeps
completed-empty and missing-exact-route outcomes separate. Opportunities and
Events retain bounded source context behind **About this view**. Populated
Events leads with event basics, distinguishes recorded lifecycle from live
operational readiness, and does not advertise Control Room or Replay while
their authority is unavailable. This is source/local presentation only; it
adds no quote, event, workflow, provider, role, or tenant authority.

Ambient Conversation source note for row 60: the long Conversation inspector now
uses the same opt-in **Why this view** arrival disclosure as Payment and Proposal.
Its five message-evidence rails, current next action, change-request state, and
follow-up state remain visible in the ordinary scroll region; only repeated
reason/consequence rows are suppressed in that compact wrapper. This changes no
message, read-receipt, workflow, lifecycle, provider, or pricing authority.
Direct source is `src/components/AmbientConversationContext.jsx` with its
Living Opportunity call site and focused unit/Playwright coverage.

Ambient Payment/Proposal administration source note for row 60: both
continuations now use canonical quote-scoped browser history and preserve the
originating object as **Payment** or **Proposal**. The destination stays pending
through the connected Quote History read, resolves only after the exact row is
present and **Quote administration** receives focus, and suppresses adjacent
saved-quote, commercial-dependency, Decision Debt, and general Ambient panels
for that exact arrival. The connected Firebase emulator holds the read for five
seconds and verifies pending/ready state, exact filtering, focus, and 390/1440
containment. No payment, proposal, delivery, lifecycle, pricing, provider, role,
or tenant authority changes.

Ambient NOW source note for row 60: when both the default-off Ambient and NOW
presentation gates are enabled, `AmbientNowView` replaces the denser card
dashboard with at most three already-ranked priorities from the current bounded
staff snapshot. It withholds caught-up language for stale, partial, unknown, or
truncated reads and when recorded payment steps remain, and limits **Quiet
progress** to timestamp-backed internal workflow receipts. The ordinary NOW
surface remains the flag-off rollback. This is `v0.8.0` candidate source only;
it adds no Firebase, provider, payment, pricing, role, or mutation authority and
has no hosted or human-acceptance evidence.
| 71 | Commercial Truth Loop evidence exporter: read-only tenant-scoped Firestore reader plus projection of already-read authoritative quote, acceptance-receipt, ledger, and cost-snapshot documents into the canonical `truthloop-evidence-bundle-v2` reconciliation bundle, with per-section provenance, explicit availability classification (`available`, `missing`, `not_applicable`, `not_yet_available`, `blocked_by_integration`, `contradictory`, `schema_drift`), schema-drift refusal, producer interfaces for processor payout settlement, declared fee schedules, and post-event consumption, and an evidence-coverage report separating engineering, integration, and business-policy blockers | Implemented (source/local + emulator evidence only. The reader assembles quote, acceptance-receipt, active-version, change-request-resolution, and organization-settings documents for one explicitly named organization; there is no all-tenant read and no collectionGroup query, a cross-tenant document aborts the read rather than being skipped, and an explicit field allowlist withholds portal keys, buyer tokens, and provider secrets. The Admin SDK bypasses Firestore rules, so containment is explicit-scope and allowlist enforced rather than rule-enforced. The payout producer stays inert behind the Stripe Connect stopping point and cannot be made to emit provider evidence; declared-cost capture and receipt-verified export now exist, while undeclared fee schedules, undeclared overrun policy and provider settlement remain explicit blockers, so this does not establish a production `fullyReconciled` record. Findings remain `observation_only` staff observations and are never customer output, accounting truth, repricing, or permission to charge, accept, book, or settle. Firestore reads, any operator surface, hosted verification, provider evidence, deployment, and human acceptance remain pending.) | `scripts/reconciliation-evidence-export.mjs`, `evidence/src/exporterCore.mjs`, `evidence/src/availability.mjs`, `evidence/src/provenance.mjs`, `evidence/src/canonical.mjs`, `evidence/src/schemaDrift.mjs`, `evidence/src/coverage.mjs`, `evidence/src/producers/*.mjs`, `docs/truthloop-evidence-contract.json`, `src/lib/__tests__/commercialEvidenceExporter.test.js`, `src/lib/__tests__/commercialEvidenceExporter.adversarial.test.js`, `evidence/src/firestoreReader.mjs`, `scripts/truthloop-export-emulator-acceptance.mjs`, `src/lib/__tests__/commercialEvidenceReader.test.js`, `docs/COMMERCIAL_TRUTH_LOOP_ADR.md`, `docs/COMMERCIAL_TRUTH_LOOP_DESIGN.md` |

## Implementation Chronology

Dates below are Git author dates unless a merge/integration distinction is
stated. IDs may recur when a later cohort materially hardened or expanded an
earlier feature. Detailed history remains canonical in `CHANGELOG.md`.

| Source / integration cohort | Git evidence | Functions served | Feature IDs represented |
|---|---|---|---|
| 2026-02-26 | `1ce0a73` | Initial quote builder, live totals, catalog/history, reporting, and proposal/PDF foundation | 4–8, 14, 27 |
| 2026-02-27 | `3b5f902`, `fa4e6a7` | Scenario comparison, recommendations, and editable pricing administration | 10, 19–20 |
| 2026-03-08 | `d628cc4` | Verified-role authentication, customer portal foundation, and PWA shell | 12, 18, 62 |
| 2026-03-09–11 | `219fc4d`, `7e82941`, `1222c4a`, `35a1868`, `7a99d13`, `9b455ae`, `d82ee9f`, `b767f20`, `95105c9` | Branding, booking/contract operations, schedule, integrations, diagnostics, guided selling, and deposit initiation | 11, 13, 15–17 |
| 2026-03-17–20 | `69323d0`, `466c775`, `413d9b4`, `33889ad`, `c4ab915`, `f9980f0` | Firestore menu, five-step/admin consolidation, flags, multi-tenancy, authoritative pricing, versions, and QA | 1–4, 7, 9, 22 |
| 2026-03-21–27 | `712f124`, `aaad80b`, `4f3da37` | Portal rotation/UX and hardened tenant provisioning | 12, 18, 25 |
| 2026-04-02 authored; 2026-05-29 merged | `a18cfa6`, `3d0fffc` | Canonical menu sync, staffing parity, and guarded permanent quote deletion | 2, 8, 15 |
| 2026-08-03–05 | `dc460e3`, `1ae6c23`, `d2747c6` | Public product surfaces, workflow/approvals, import, provisioning, authoritative delivery, and starter catalogs | 8, 23–25, 61 |
| 2026-08-04 authored; integrated through 2026-08-06 | `e725e61`, `5f2915b`, `b27560c`…`d6518eb` | Separate deposit/final-balance payment rails and invoice-first buyer onboarding | 8, 26 |
| 2026-08-05–06 | `2a812ab`, `ed9a6f2`, `1886c19`, `c389f65`, `2ab6b5f`, `1b02cea`, `41e0a27` | Proposal acceptance, analytics, Operations Audit, BEO, customer projection, themes, and portal conversation | 12, 14, 21, 27–28, 63 |
| 2026-08-08–09 | `a539d00`…`d1b5d6b`; first `v0.4.0`, production workspace flag in `v0.4.2` | Customer-centered shell/360/search, commercial intelligence, governed operations, and Event Workspace integration | 29–47 |
| 2026-08-10 | `3cca8cc` (`v0.5.0`) | Event Workspace and central Event Messaging Station | 47–48 |
| 2026-08-10 | `c9819f9`; deployed as `4f4e00d` (`v0.6.0`) | Post-competitive Pilot decision, CREATE, guided-command, margin, and sensory presentation layer | 49–53, 64 |
| 2026-08-11 | `fb0aacc` (`v0.7.0`, deployed through governed Firebase `all` run `31529170963` and Vercel run `31530050353`) | Structured-request follow-up, catalog costs, decision room, model/deterministic intake, and event-shape memory | 49, 54–58 |
| Observed 2026-08-12 | Uncommitted working tree; no Git commit or tag | Privacy-bounded Ambient analytics extension, authoritative staffing, Ambient workspace/orientation, and preference extensions | 14, 59–60, 64 |
| Observed 2026-08-13 | Source branches; no Connect deployment or provider evidence | Explicit organization-owner authority, owner/admin team role authority, recent-auth proof, App Check monitoring contract, deploy-empty Stripe Connect runtime isolation, and apply-disabled staging infrastructure source | 18, 65–66 |

## Functional-Domain Index

| Functional domain | Function served | Feature IDs |
|---|---|---|
| Platform, identity, safety, and governance | Tenant model/context, authentication, security/audit depth, validation, provisioning, capability traceability, install shell, owner/admin team access authority, and isolated Connect control-plane foundation | 1, 17–18, 21–22, 25, 32, 62, 65–66 |
| Catalog, pricing, intake, and commercial intelligence | Menu/admin, authoritative calculations, builder guidance, dependency/change authority, margin, parsing, and memory | 2–5, 9–10, 19–20, 33, 38, 42–43, 45, 51–54, 56–58 |
| Quote, proposal, and customer relationship | Quote lifecycle/versioning, decision portal, customer projection/360/search/rebook/measures, structured change records, and decision-room requests | 6–8, 12, 28, 30, 34–36, 49, 55 |
| Booking, events, workflow, and operations | Booking/schedule, diagnostics, Workflow/import, BEO, routed workspace, timing/run-of-show/closeout, Event Workspace/Messaging, staffing, Ambient operations, and audit | 11, 15–16, 23–24, 27, 29, 31, 39–41, 44, 47–48, 59–60, 63 |
| Revenue, providers, reporting, and acquisition | Provider operations, commercial reporting, buyer onboarding, Autopilot preview/operations, owner SMS evidence, and public marketing/system overview | 13–14, 26, 37, 46, 61, 67 |
| Decision experience and accessibility | Pilot NOW/Event Room presentation plus motion, sound, and reduced-motion preferences | 50, 64 |

## Guided Flow (Where It Lives)
- Wizard flow entry and steps: `src/App.jsx`
- Wizard step model + validation + template defaults: `src/lib/wizardUi.js`
- Step components: `src/components/WizardSteps.jsx`
- Guided recommendation engine: `src/lib/recommendations.js`
- Guided selling toggle and rules config: `src/components/AdminCatalogModal.jsx`
- Default guided rules and normalization: `src/data/mockCatalog.js`

## Notes
- Non-dev fail-fast catalog behavior is implemented in `src/hooks/useCatalogData.js` and surfaced by blocking UI in `src/App.jsx`.
- New-tenant quote creation remains gated until a named positive-price package,
  an event type, and explicit pricing review are saved. Authoritative pricing
  rejects unconfirmed settings.
- Catalog persistence compares the originally loaded fingerprints and patches
  only changed records/settings; stale edits and identifier collisions require
  a reload instead of overwriting remote changes.
- Trusted Firebase edits re-price from current tenant configuration and
  atomically update quote/portal state with a new immutable version. Reopen and
  permanent cleanup are admin-callable operations; direct quote/portal deletes
  are denied, and terminal commercial evidence cannot be reopened or
  overwritten.
- Quote History derives expiry without a staff write for non-admin readers. An
  admin expiry persists only status, lifecycle, and update time to the quote and
  matching portal in one rules-enforced batch; a quote-only or portal-only
  transition is denied. Unresolved deliveries stay visible with persistence
  deferred until provider review, and one malformed legacy portal cannot blank
  the history list. The admin `Reopen` action restores an eligible expired quote
  as a draft with a new portal issuance.
- Firebase payment-request email, contract conversion, portal rotation, and
  permanent deletion require an exact approved request. Functions record the
  server-owned execution outcome in the quote workflow and in an admin-readable,
  server-write-only organization audit record.
- Stripe deposit and final-balance requests use separate exact approvals and
  payment rails. Deposit scope binds the quote revision, portal issuance,
  recipient, currency, and amount. Final balance additionally requires a
  booked contract and verified provider-paid deposit, derives the remainder
  from authoritative totals, and binds the contract and deposit evidence. A
  versioned ledger plus `payment.finalBalance` projection prevents one rail
  from rewriting the other.
  Both resumable server operations register a prepared Session with no
  browser-readable link, keep QuotePilot's URL copy only in a client-denied
  private dispatch record, and publish to the quote/portal only after durable
  email-provider acceptance. Ambiguous creation or send outcomes reuse the
  exact approval and executing-admin/provider identities; durable acceptance
  resumes publication without another send, while definite failure requires
  safe checkout neutralization before a new approval. Direct standalone
  checkout creation and browser payment-evidence writes fail closed.
  Explicit `STRIPE_MODE` must match the key and provider objects. Four signed
  Checkout Session events drive monotonic, payment-kind-isolated state, and
  same-tenant admins can reconcile the exact server-recorded Session for each
  rail. This source has no hosted Stripe test/live acceptance. Refund
  initiation/status and dispute handling remain manual or unimplemented.
- Generic Resend and quote Stripe API/webhook credentials are strict Firebase
  Secret Manager bindings on only their consuming Functions; the generic
  webhook does not receive the API key. The Functions dotenv materializer
  rejects all three values. Fresh provider rotation, ordered webhook overlap,
  exact hosted UAT, and revocation of the known exposed/cached values remain a
  hard release blocker.
- The public `/start` path uses the existing `tonicatering` Firebase project but
  remains an independently disabled Stripe test rail. Browser route and CTA
  flags require a syntactically valid non-placeholder public Turnstile site key;
  Cloudflare setup and human review remain separate evidence. The server
  separately verifies exact hosts/actions, reserves a request-scoped HMAC-keyed
  create lease before identity/order reads, consumes the public status network
  lease once per request before its first buyer-order read, and holds its
  Turnstile, buyer Stripe, and rate-limit secrets in Firebase Secret Manager.
  Exact create retries recharge
  network capacity without a duplicate email charge during the 24-hour
  reservation; only a prior signed-void order may be superseded after that
  email window.
  `createBuyerAccessInvoice` creates a true fixed $1 Hosted Invoice Page, and
  the dedicated buyer API client and webhook endpoint are pinned to Stripe API
  version `2024-06-20`; the generic quote Stripe client remains unchanged. Only
  the four supported signed, deduplicated invoice events may establish payment
  state. `invoice.paid` prepares the organization, neutral settings, Starter
  workspace plan entitlements, provisioning record, and pending invitation, but
  no user membership, admin role, claims, or access. `activation_sent` requires
  durably recorded onboarding-email provider acceptance. Independently,
  token-bound `provisioning` with `workspaceReady=true` offers a manual `/app`
  exact-email Firebase verification path and stops automatic polling without
  claiming Resend acceptance, membership, claims, or access. Only an exact
  matching verified Firebase email may consume the invite, and only `active`
  is access-ready.
  Controlled test-mode markers require exclusion from live revenue and paid-
  customer reporting. The quote-payment mode, credentials, `stripeWebhook`,
  deposit, and final-balance rails remain isolated. This is branch/source
  evidence, not Turnstile, Stripe, Firebase-delivery, hosted, or live-launch
  acceptance.
- Firebase quote email is bound to the saved revision and portal issuance. Only
  server-recorded provider acceptance owns the `sent` transition, and only
  acceptance for the exact current valid issuance activates its portal. If the
  provider accepts a message whose issuance is invalid or expired, QuotePilot
  retains that evidence as `requires_rotation` while keeping the portal
  inactive; guarded rotation and a separate accepted send are required.
- A definite provider failure whose safe retry window has expired starts a
  fresh delivery generation. An ambiguous outcome remains locked for same-key
  retry or audited provider review, and known server-observed acceptance cannot
  be reconciled as no-send. Generic staff updates cannot claim `sent` or
  `viewed`; the customer portal owns actual view evidence.
- Copy Portal and the PDF portal link require evidence for the exact current
  issuance and remain withheld after rotation. Legacy portal projections
  without `deliveryEvidence` fail closed and must recover through an approved
  resend or truthful provider reconciliation; projection backfill never
  fabricates acceptance evidence.
- Proposal acceptance is callable-owned and revision-preconditioned. The server
  requires the displayed portal projection to match the complete saved quote,
  records typed signer and versioned consent evidence, stores all receipt money
  in integer USD minor units, hashes the signed proposal snapshot, and creates
  a tenant-readable/server-write-only receipt. Firestore denies direct browser
  acceptance; payment and booking remain separate states.
- Menu backfill for `pricingType` + `active` is implemented in `scripts/seed-firestore-menu.mjs`.
- Multi-tenant org scoping is authoritative for protected catalog and quote
  writes. Direct quote creation is denied in Firestore; trusted Functions
  create canonical drafts, and existing write/version flows require an
  org-scoped quote instead of auto-migrating legacy global data.
- Feature flags for optional surfaces (portal/schedule/integrations/diagnostics/reporting/compare/guided selling) are normalized in `src/App.jsx` and managed in `src/components/AdminCatalogModal.jsx`.
- Firebase browser smoke coverage now includes both auth+rules and authoritative-pricing lanes under `e2e/`.

## Event Operating Spine — bounded first slice

| Capability | Source implementation | Boundary |
|---|---|---|
| Event Operating Spine phase ledger | `functions/eventOperations.js`, `functions/index.js`, `src/lib/eventOperationsClient.js`, `src/components/EventOperationsPanel.jsx`, `src/components/LiveOperationsPlanningViews.jsx` | Default-off source slice: same-tenant administrators initialize an exact accepted/booked event ledger and record only `prepared -> in_progress -> completed`; sales can read the bounded snapshot. Exact command receipts support uncertain-attempt reconciliation. |

The server requires both `EVENT_OPERATING_SPINE_ENABLED` and the exact tenant's
protected `organizations/{orgId}/settings/config.eventOperatingSpineEnabled` field. The frontend additionally requires
`VITE_EVENT_OPERATING_SPINE_ENABLED`. Browser administrators cannot activate the
tenant setting. Records and receipts are server-private; customer portals receive
none of this operational evidence. The existing immutable accepted source remains
commercial authority. The read view exposes only the latest receipt, not complete
Replay history. Phase completion is an operator record, not readiness, actual
attendance, labor, purchasing, closeout review, provider, payment, or settlement
proof. These remain separate or unavailable. This entry records source scope;
local validation and hosted, production, and human acceptance are separate evidence
in `PROJECT_STATUS.md`. No tenant activation or deployment is implied.

## Event Operating Work Journal — bounded checkpoints and issues

| Capability | Source implementation | Boundary |
|---|---|---|
| Event Operating Work Journal | `functions/eventOperatingWork.js`, `functions/index.js`, `src/lib/eventOperatingWorkClient.js`, `src/lib/eventOperatingMutationGuard.js`, `src/components/EventOperatingWorkPanel.jsx` | Same-tenant administrators record or reopen four fixed checkpoints and open, resolve, or reopen a bounded issue log against one initialized exact-source event ledger. Sales can read. Independent work policy, work revision, and immutable work receipts preserve the original phase ledger and policy. |

Fixed checkpoint identifiers are `venue_access`, `team_briefing`,
`service_handoff`, and `pack_down`.

Journal changes are allowed in every initialized event phase, including
`completed`, and never reopen or otherwise change the phase. Missing journal
state remains unavailable until the first successful work command; a checkpoint
without an operator record is not treated as complete. Issue IDs are assigned by
the server. The log retains at most 25 issues, including resolved issues; normal
and urgent severity are fixed when an issue is opened. Notes are limited to 240
characters and required for opening, resolving or reopening an issue and reopening
a checkpoint. A checkpoint record may omit its note.

The existing environment and exact tenant gates apply. Work state and receipts
remain server-private, with bounded staff projections and no customer-portal
exposure. A pending or uncertain phase command excludes a new journal command,
and a pending or uncertain journal command excludes a new phase command. Each
command reconciles its own exact receipt before another mutation is permitted.
Checkpoint and issue records are operator statements, not staffing confirmation,
attendance, actual labor/purchasing, readiness, delivery, closeout review, payment,
or settlement evidence. Full Replay remains unavailable; declared actuals have a separate surface below. This is a
source/local implementation boundary; hosted, production, provider, and human
acceptance remain separate.


## Event Operating Actuals — declared costs and completeness

| Capability | Source implementation | Boundary |
|---|---|---|
| Event Operating Actuals | `functions/eventOperatingActuals.js`, `functions/index.js`, `src/lib/eventOperatingActualsClient.js`, `src/lib/eventOperatingMutationGuard.js`, `src/components/EventOperatingActualsPanel.jsx` | Same-tenant administrators record, correct, void, and declare labor, purchasing, and other costs against an initialized exact-source event ledger. Sales read bounded projections. Actuals policy, revision, state, and immutable receipts are independent of phase and work authority. |

Costs are explicitly declared nonnegative integer USD cents, bounded at
1,000,000,000 cents per entry. Labor also records 1–10,080 whole duration minutes
and an operator-declared role category; no wage, rate, staffing assignment, or
attendance is inferred. Purchasing records spend, not physical consumption.
The journal retains at most 50 entries over its lifetime, including terminal voids.
Corrections replace the typed entry fields while preserving its ID and category;
corrections and voids require a reason. Text is limited to 240 characters.

Each category starts `not_declared`. Record, correction, or void resets that
category to `partial` and clears its current completeness declaration. An
administrator separately declares `complete`, `not_applicable`, or `partial`.
Complete may explicitly confirm zero; not applicable requires no active entries.
Captured totals are provisional until all three categories are complete or not
applicable. Unknown or partial categories must never become zero by default.

Existing global and exact tenant gates apply, with private state and receipts,
sales reads, and administrator commands. Pending or uncertain phase, work, and
actuals commands exclude each other. No actuals command changes accepted quotes,
phase or work policy, delivery, closeout, payment, or settlement authority. A
trusted read-only evidence export and operator-declared tolerance policy remain
separate integration work. This documents source scope and local validation,
without tenant activation, hosted verification, or human acceptance.


## Event Operating Replay — anchored operational receipts

Slice D adds an exact-event, read-only operational history for admin and sales
staff under the existing event gates. Replay merges verified phase, checkpoint
and issue, and declared-cost receipts into bounded pages pinned to the first
read's channel heads. Newer writes require refresh; stale accepted sources,
gaps, corrupt receipts, and missing parent evidence fail closed. It shows
bounded actor, time, target and before/after records without private receipt
bodies. Current staffing, BEO, dependency and run-of-show references retain
their domain authority and are not historical execution evidence.

The declared-cost exporter verifies exact private acceptance, phase, actuals,
and category declaration receipts before emitting aggregate USD costs. Missing
or partial capture remains unavailable; explicit zero remains zero. Recorded
costs can support contribution comparison but cannot establish physical
consumption, attendance, payroll, delivery, payment or settlement. Overrun
comparisons additionally require explicit declared tolerance with actor/time
provenance; coverage includes this policy prerequisite.

Source implementation has local unit, emulator, responsive browser and build
evidence. Hosted and human acceptance remain separate. The canonical
contract is [Slice D](TENANT_OPERATING_MODEL_ADR.md#slice-d--operational-replay-and-declared-cost-evidence).


## Tenant Workflow Configuration Studio

The administrator-only Library editor supports bounded workflow draft editing,
validation, exact publication preview, typed confirmation, immutable tenant
versions and reason-bound retirement. Allowed fields are task roles, names and
instructions, due/escalation timing, fixed manual handoff references, and
optional explicit cost-review thresholds and comparison tolerances. Existing
Library navigation and dirty-dismissal guards apply. Unknown program fields,
code, recipients, URLs and role escalation are rejected.

A source-controlled seed is explicit version evidence for new instances while
the tenant has never published. Publication changes future instance bindings;
it does not rebase existing events. Retirement preserves immutable version
records and existing bindings while blocking new bindings until another
publication. Current tenant, role, email and runtime gates apply to every
transaction. Exact request identity and immutable lifecycle receipts support
uncertain-outcome recovery without duplicate publication.

Source, local automated tests, real emulator scenarios and responsive browser
fixtures are validated. Final populated rollback qualification passes; current rollout boundaries remain in Project Status;
no hosted, tenant activation or human acceptance claim is made.

## Event Workflow Coordination

The exact-event Control Room shows the pinned definition, task ownership,
workflow/task due and escalation projections, fixed manual handoff guidance,
and receipt-bound acknowledgements. Admin or the enabled assigned task role
may acknowledge a coordination obligation; the existing domain write roles
remain unchanged. Cost review is administrator-only and requires complete
explicit actuals at or above the configured threshold, including known zero
at a zero threshold. A later actuals revision makes an old acknowledgement
historical.

A separate migration preview and typed confirmation bind the current instance,
source and target publication. Compatibility initially preserves workflow and
domain policy, all existing task keys, acknowledged task meaning, and review
and comparison policy. Four event mutation owners share an unresolved-request
exclusion. Phase coordination stores only trusted outcome references alongside
its own immutable receipts; A/B/C policies and receipt bytes remain unchanged.
A second thin post-event-review fixture exercises the generic contract without
shipping an additional commercial workflow pack.

The read-only exporter can supply exact pinned tenant comparison policy with
publisher and time evidence. Missing, seed, invalid or undeclared policy stays
unavailable independently of actual-cost capture. Coverage distinguishes
structural producer support from record-specific evidence availability.


## Bounded Catering Workflow Packs

| Pack | Discoverable surface | Governed outcome |
|---|---|---|
| Quote review | Commercial change review and its workflow policy panel | Exact tenant approval policy is sealed into the existing simulation, authorization and apply chain; tasks cannot approve changes |
| Final guest count | Living Opportunity Guest count inspector and current customer portal | Staff request and customer/staff response remain separate from the applied pricing count; applying a booked-event amendment requires renewed acceptance |
| Event execution | Control Room and existing event policy panel | Tenant checkpoint prerequisites and urgent-issue blocks narrow original phase/work commands while preserving their receipts |
| Closeout follow-up | Post-event closeout review and its workflow policy panel | Tenant responsibility and follow-up timing compose the original closeout record; rebooking remains a reviewed draft handoff |

The shared policy panel exposes pinned publication, tasks and explicit compatible
migration. Studio permits only each pack's bounded fields. Schema 1 event
compatibility remains available; new schema 2 packs require explicit publication.
Neither publication nor acknowledgement activates a tenant or performs a domain
action. Private attendance journals and adapter proof deny all browser access.
Source, browser integration and populated restart rollback qualification pass;
real tenant pilot and rollout boundaries remain in Project Status.

## Tenant Workflow Migration Inventory

An explicitly invoked offline tool classifies bounded exact-source inputs and
reports safe identifiers, policy pins and blocked reasons. It cannot mutate
records or create historical evidence. The isolated staging EventSpine release
profile prepares explicit server/browser configuration; tenant activation and
hosted/operator evidence remain separate. These are developer/operator tools,
not customer actions or automatic migration approval.

## Local Tenant Workflow Rehearsal

A developer-only launcher starts disposable Auth/Firestore/Functions emulators,
verifies all four packs and opens the real application with synthetic admin and
sales sign-in. Normal authentication, role gates and native authority remain in
force. Library exposes Business workflows; exact quote/event routes are printed.
The whole-app fixture is restricted to a fixed demo project and loopback
services, with provider sends disabled. The shared Firebase browser-gate seed
also fails closed unless both Auth and Firestore are loopback emulators for a
`demo-*` project; its active portal timestamps follow the test clock while
explicit expiry fixtures remain fixed. This is local rehearsal infrastructure
and establishes no hosted tenant activation, real customer action or human
acceptance. Setup and stop instructions live in
[README](../README.md#local-tenant-workflow-rehearsal).

## Tenant-scoped workflow runtime

The production workflow profile can admit RagnaKoK Inc (`mm05366-sandbox`) while
global workflow and Commercial Change authority flags remain off. The connected
admin Studio and native actions still require tenant settings and current role
checks. Other organizations remain unavailable even if their settings are changed.
Deployment, tenant activation, draft publication and event migration are separate
operations; see [the runtime activation procedure](LAUNCH_RUNBOOK.md#ragnakok-tenant-workflow-release).


RagnaKoK test access also admits operational staffing and Revenue Autopilot
preparation through the same exact-tenant runtime guard. The staffing tenant
setting remains required. Revenue policy configuration remains explicit; no
outreach policy is invented or published during activation. The global scheduler
receives no tenant scope and stays disabled, and the outbound-send gate stays
off. Other tenants cannot use these scoped authorities. Existing explicit
operator email actions retain their normal authorization and confirmations.
