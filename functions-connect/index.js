"use strict";

// The Connect codebase is intentionally deploy-empty during the authority and
// infrastructure foundation. Provider-facing exports are added only after the
// isolated database, IAM, App Check, manifest, and hosted Sandbox gates exist.
module.exports = Object.freeze({});
