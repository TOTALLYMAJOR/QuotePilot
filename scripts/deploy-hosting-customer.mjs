#!/usr/bin/env node

throw new Error(
  "Customer Hosting deployment is retired from repository code. Use a separately owned, qualified trusted deployer; this command never invokes a provider CLI."
);
