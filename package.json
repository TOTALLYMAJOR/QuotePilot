{
  "name": "react-firebase-quote-wizard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest",
    "test:rules:firestore": "npx firebase-tools emulators:exec --only firestore \"vitest run src/rules/__tests__/firestore.rules.test.js\"",
    "test:e2e": "bash ./scripts/run-playwright.sh test",
    "test:e2e:headed": "bash ./scripts/run-playwright.sh test --headed",
    "test:e2e:firebase": "node ./scripts/run-firebase-e2e.mjs",
    "test:e2e:firebase:authoritative": "node ./scripts/run-firebase-e2e-authoritative.mjs",
    "check:env": "node ./scripts/check-env.mjs",
    "seed:menu:firestore": "node ./scripts/seed-firestore-menu.mjs",
    "migrate:multi-tenant": "node ./scripts/migrate-to-multi-tenant.mjs",
    "check:docs:governance": "node ./scripts/check-doc-governance.mjs",
    "check:perf:bundle": "node ./scripts/check-bundle-budget.mjs",
    "check:perf:cwv": "lhci autorun --config=./.lighthouserc.json",
    "deploy:firebase:functions": "npx firebase-tools deploy --only functions",
    "deploy:firebase:hosting": "npm run build && npx firebase-tools deploy --only hosting:app",
    "deploy:firebase:hosting:customer": "node ./scripts/deploy-hosting-customer.mjs",
    "deploy:firebase": "npm run build && npx firebase-tools deploy --only hosting:app,firestore,functions",
    "deploy:vercel": "npx vercel --prod",
    "deploy:vercel:build": "npm run build && npx vercel --prod --prebuilt"
  },
  "dependencies": {
    "firebase": "^11.0.0",
    "jspdf": "^4.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "@lhci/cli": "^0.14.0",
    "@playwright/test": "^1.54.2",
    "@vitejs/plugin-react": "^5.2.0",
    "vite": "^7.3.1",
    "vitest": "^4.0.18"
  }
}
